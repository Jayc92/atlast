{
  "name": "@atlast/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain schemas and types (Zod single source of truth per ADR-0005) — M1 S1 domain schemas per ADR-0014 as amended by ADR-0019.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "vitest": "4.1.10"
  }
}
