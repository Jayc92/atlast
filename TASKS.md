# Atlast — Tasks

The single place in-flight work is tracked ([GUARDRAILS.md § 4](GUARDRAILS.md#4-documentation-standards)). Tasks are grouped by milestone ([docs/milestones.md](docs/milestones.md)); each milestone requires explicit authorization — **M0 is complete (2026-07-22). M1 implementation was explicitly authorized 2026-07-23** (plan approved, ADRs 0014–0023 Accepted; 0014/0015 amended by ADR-0019 and ADR-0022, 0017 amended by ADR-0020, 0016 amended by ADR-0021 and ADR-0023, 0018/0019 amended by ADR-0023) and is **active, slice-gated: S1 is complete (merged via PR #7, 2026-07-29); S2 is complete (human-approved 2026-07-29, merged via PR #10, 2026-07-30); the S2 checkpoint/HANDOFF protocol merged via PR #11 at `a7a997d` (2026-07-30); S3 is complete (independently reviewed and approved, merged via PR #13 at `003bccc`, 2026-07-30, GitHub Actions `verify` passing; closeout checkpoint merged via PR #14 at `1eca85f`, 2026-07-31). S4 is complete (authorized via PR #15 with ADR-0021 accepted through the same PR; implemented, independently reviewed and remediated, merged via PR #16 at `63bdfab`, 2026-07-31, GitHub Actions `verify` passing). S5 is complete (human-authorized in principle 2026-07-31; ADR-0022 explicitly accepted by Joseph Carfagno 2026-08-05 after independent review, acceptance record merged via PR #18 at `f50f0d7`; implemented under accepted ADR-0022, independently reviewed with no blocking findings — the reviewer reran `./scripts/verify.sh` successfully — and merged via PR #19 at `0923e9c`, 2026-08-05, GitHub Actions `verify` passing; checkpoint `m1-s5-reconciliation-engine-merged`, [HANDOFF.md](HANDOFF.md)). The S6 pre-release architecture and authorization review is complete: [ADR-0023](docs/adr/0023-m1-snapshot-and-in-memory-store-semantics.md) was explicitly accepted by Joseph Carfagno on 2026-08-05 after three independent-review correction passes, settling all nine identified S6 design gaps (amending ADR-0016/0018/0019 via metadata-only notices) — ADR-0023 is the binding S6 clarification and remains Accepted. **S6 was then explicitly authorized by Joseph Carfagno on 2026-08-05** in a separate authorization decision (recorded in the S6 entry below): S6 is the only active authorized implementation slice, **effective only after the documentation PR recording this authorization merges to `main` and `main` is synchronized locally with a clean working tree**. Authorization does not approve future S6 implementation output — that output still requires independent review, verification, PR approval, and merge.** S7–S8 remain gated and unauthorized, each released explicitly only after the preceding slice is merged to `main` and its checkpoint recorded ([HANDOFF.md](HANDOFF.md)). **M2 and later milestones remain unauthorized.**

**Legend:** `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked (note the blocker)

---

## M0 — Safe Project Foundation (complete — 2026-07-22)

### Phase A — Documentation (approved 2026-07-22)

- [x] README.md — project overview and documentation map
- [x] PROJECT_SPEC.md — vision, goals, principles, scope, non-goals
- [x] docs/architecture.md — architecture philosophy and conceptual design (draft)
- [x] docs/milestones.md — authorized milestone sequence M0–M5 with exit criteria
- [x] GUARDRAILS.md — engineering/coding/repository/documentation/testing standards
- [x] CLAUDE.md — AI assistant working instructions
- [x] TASKS.md — this file
- [x] Human review and approval of the documentation set — _approved 2026-07-22 alongside ADRs 0001–0013 ([docs/adr/README.md](docs/adr/README.md))_
- [x] Non-goals ([PROJECT_SPEC.md § 7](PROJECT_SPEC.md#7-non-goals--what-atlast-will-not-become)) explicitly reviewed and accepted — _part of the 2026-07-22 documentation approval_
- [x] Owners assigned to open questions in [docs/architecture.md § 7](docs/architecture.md#7-open-questions) — _all four questions assigned 2026-07-22 to Joseph Carfagno (Founder/Maintainer), each with a named future decision gate (M1 graph/evidence, M1 storage/evidence, and M1 query API ADRs, plus the M4 planning ADR); ownership is accountability for producing the ADR, not approval of any answer_

### Phase B — Foundation build (complete — authorized 2026-07-22, finished 2026-07-22)

- [x] M0 tooling ADRs (monorepo tooling, lint/format/type-check/test/build/browser-check stack) drafted and human-approved — _ADRs 0001–0013 in [docs/adr/](docs/adr/README.md), formally approved 2026-07-22; acceptance authorizes M0 Phase B scaffolding only_
- [x] TypeScript monorepo established — _root pnpm/Node config and workspace skeleton committed; package-manager bootstrap operational (`scripts/bootstrap.sh` verifies Node 24 + pinned pnpm and runs a frozen install) and `pnpm-lock.yaml` generated; TypeScript 6.0.3 pinned at the workspace root; repository-wide linting (`pnpm lint`, ESLint + typescript-eslint strict type-aware config per ADR-0006) and formatting (`pnpm format` / `pnpm format:check`, Prettier per ADR-0007) operational; recursive package-level type checking operational (`pnpm typecheck` per ADR-0002, covering the four shared package shells plus `apps/api`, `apps/web`, and `tests/acceptance` including their colocated tests). The inert standalone `tests/unit` and `tests/integration` workspace placeholders (manifest-only, no tests or configs) were removed 2026-07-22 in favor of the accepted colocated-test convention: unit tests (ADR-0008) live with the code they exercise (`apps/web` component tests, and the API suite in `apps/api`, whose `fastify.inject()` tests are the current in-process API contract tests per ADR-0009), while browser acceptance (ADR-0010) keeps its dedicated `tests/acceptance` workspace for its distinct Playwright runner and service lifecycle; the `tests/*` workspace glob still discovers any future substantive test workspace_
- [x] Web application shell — _React + Vite client-rendered SPA in `apps/web` per ADR-0003: strict-TypeScript foundation page communicating the product vision and M0 status (no topology, graph exploration, search, routing, or query behavior — gated on M1/M2); dev and preview servers bind to `127.0.0.1` only; on load the page requests relative `/api/health`, proxied by Vite to the API shell's `GET /health` on `127.0.0.1:3001` (no CORS change in `apps/api`), validates the payload, and renders deterministic checking/online/unavailable states with visible degradation on failure or malformed responses; unit tests (Vitest + jsdom + Testing Library, stubbed `fetch`, no network, no fixed sleeps) cover primary content and the online/unavailable states; builds to git-ignored `dist/`; responsive at desktop and narrow-mobile widths with semantic HTML, visible focus, and reduced-motion support. All non-browser verification (lint, format, typecheck, tests, build, frozen-lockfile install, dev+preview proxy smoke test) passed 2026-07-22; reviewer browser inspection completed 2026-07-22, covering desktop at 1280x720, narrow mobile at 390x844, the connected and unavailable API states, no horizontal overflow, and no browser console warnings; the automated Playwright browser-acceptance harness (ADR-0010) now exists in `tests/acceptance`, running the primary shell journey against the built API and built web bundle in desktop (1280x720) and mobile (390x844) Chromium projects_
- [x] Backend API shell — _Fastify shell in `apps/api` per ADR-0004: `GET /health` with an explicit response schema returning `{"status":"ok","service":"atlast-api"}`; binds to `127.0.0.1` only with no code path to any other interface (no auth per the M0 exemption in [GUARDRAILS.md § 1.4](GUARDRAILS.md#14-security)); application construction (`src/app.ts`) separated from network startup (`src/server.ts`) so the Vitest suite drives it via `fastify.inject()` with zero sockets (ADR-0008/0009); dev loop uses Node 24 native type stripping (`node --watch`, no tsx, per ADR-0011); builds to git-ignored `dist/`; explicit startup-failure handling and clean SIGINT/SIGTERM shutdown; root `pnpm test` / `pnpm build` recursive scripts added. No graph, topology, evidence, entity, relationship, discovery, or query behavior — that remains gated on M1_
- [x] Shared packages structure — _compile-only package shells for `packages/shared`, `packages/graph-model`, `packages/connectors`, and `packages/ui`: per-package strict tsconfigs, `typecheck` scripts, and empty `export {}` entrypoints. These are **empty M0 package boundaries only** — domain schemas/contracts and the graph model remain gated on M1 authorization, connector implementation on M5, and UI components on the authorized web/UI work_
- [x] Automated linting, formatting, type checking, tests, builds, browser acceptance checks — _all six command families operational and passing 2026-07-22: `pnpm lint` (ESLint per ADR-0006), `pnpm format:check` (Prettier per ADR-0007), `pnpm typecheck` (recursive tsc per ADR-0002, now including `tests/acceptance`), `pnpm test` (Vitest unit suites per ADR-0008 plus the Playwright acceptance suite per ADR-0010, discovered recursively), `pnpm build` (recursive), and the browser acceptance suite in `tests/acceptance` (Chromium-only, desktop 1280x720 + mobile 390x844 projects, self-booting built API + built web preview via Playwright `webServer`, web-first assertions, no fixed sleeps, artifacts retained only on failure and git-ignored). `scripts/verify.sh` as the single entry point remains a separate open task below_
- [x] `scripts/verify.sh` populated as the single verification entry point — _populated 2026-07-22 per ADR-0013: seven fail-fast stages in cheapest-first order (git whitespace validation, `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, non-browser Vitest suites, `pnpm build`, Playwright browser acceptance run exactly once); resolves the repository root so it runs from any directory; installs or upgrades no dependencies, never runs formatting in write mode, and does not intentionally modify tracked files, though its build and test stages create or update generated git-ignored artifacts such as `dist/`, `test-results/`, and Playwright failure reports (bootstrap and the one-time Chromium download are documented prerequisites, not stages); full pipeline passing locally 2026-07-22. CI wiring landed as the separate CI task below_
- [x] CI runs `scripts/verify.sh` and passes — _GitHub Actions workflow ([.github/workflows/verify.yml](.github/workflows/verify.yml)) per ADR-0013: one read-only Ubuntu `verify` job triggered by pull requests targeting `main`, pushes to `main`, and manual dispatch; installs the pinned Node (`.nvmrc`) and pnpm (`packageManager`), runs `scripts/bootstrap.sh` (frozen-lockfile install), installs the pinned Playwright Chromium with Linux dependencies, then runs exactly `./scripts/verify.sh`; third-party actions pinned to full commit SHAs on Node 24 action runtimes (PR #2 eliminated the Node 20 runtime deprecation warnings); no secrets, write permissions, artifacts, or deployments. Verified remotely 2026-07-22: PR #2 run [29977769566](https://github.com/Jayc92/atlast/actions/runs/29977769566) (success, 1m36s, zero annotations) and the final `main` run [29977876658](https://github.com/Jayc92/atlast/actions/runs/29977876658) at commit `783e95c` (success, 1m25s, zero annotations); the local verification contract also passes at the same commit_
- [x] Synthetic-data-only guarantee verified: repo holds no external connections or credentials — _static boundary audit of all Git-tracked content at commit `4111d24` completed and passed 2026-07-22: all authorized M0 product/runtime network paths are loopback-only (permitted developer tooling exceptions — package-manager and browser-download connections — are documented in the audit), the single environment variable affects only a local port, no credentials/sensitive files/employer material found, no product dependency provides external-system integration capability, fixtures documentation-only and fictional. Full evidence, method, exceptions, and stated limitations in [docs/audits/m0-synthetic-boundary-audit.md](docs/audits/m0-synthetic-boundary-audit.md); re-audit is warranted when M1 fixture data or the M5 connector lands_

---

## M1 — Synthetic Topology Model (implementation authorized 2026-07-23 — active, slice-gated)

### Planning (complete — approved 2026-07-23)

- [x] [docs/m1-plan.md](docs/m1-plan.md) — M1 architecture and implementation plan — _approved 2026-07-23 as the M1 implementation baseline_
- [x] ADR-0014 core topology domain model — _Accepted 2026-07-23_
- [x] ADR-0015 deterministic identity reconciliation — _Accepted 2026-07-23_
- [x] ADR-0016 temporal graph and snapshots — _Accepted 2026-07-23_
- [x] ADR-0017 M1 query API surface — _Accepted 2026-07-23_
- [x] ADR-0018 M1 storage strategy (mandatory per ADR-0012) — _Accepted 2026-07-23_
- [x] [ADR-0019](docs/adr/0019-subject-identity-and-assertion-claims.md) subject identity and assertion claims — _Accepted 2026-07-23 by human review; resolves the ADR-0014/ADR-0015 contradiction (typed subjects vs. coexisting conflicting type claims) by amending only those clauses (metadata-only notices on 0014/0015); fixes the S1/S2/S6 validation-layer split; unblocks S1 below without authorizing S2+_

### Implementation (authorized 2026-07-23 — active, slice-gated per [docs/m1-plan.md § 4](docs/m1-plan.md))

**M1 implementation was explicitly authorized by human decision on 2026-07-23.** The authorization permits executing the approved plan **one slice at a time, each independently reviewed**:

- [x] **S1 — Domain schemas in `packages/shared`** (Evidence incl. safe-integer `recordedSequence`, identity-only subjects and claim union per ADR-0014 as amended by [ADR-0019](docs/adr/0019-subject-identity-and-assertion-claims.md), content-addressed GraphAssertion revisions, identifiers, `schemaVersion`, schema-rejection tests; schema validation only — no repository lookups, per ADR-0019 § 4) — _implemented 2026-07-29 and **human-reviewed and approved 2026-07-29** (remediation pass applied same day before approval: normalized Evidence observation union with source-scoped relationship endpoints, per-claim confidence in conflict structures, kind-consistency checks for competing claims and near-matches, evidence-linked rule traces validated against provenance, Evidence-identifier uniqueness in collections, `z.json()` as the JSON single source): Zod 4.4.3 schemas + inferred types in `packages/shared/src/` with 171 colocated rejection-first Vitest tests, full `scripts/verify.sh` passing; Zod introduction justified at PR per ADR-0005 and [docs/m1-plan.md § 12](docs/m1-plan.md). **Merged to `main` through PR #7 on 2026-07-29**_
- [x] **S2 — Repository interfaces + contract-test suite skeleton** — **authorized 2026-07-29 (released after S1 merged via PR #7)**; **implemented, ADR-0020 remediation and final integrity corrections applied (sub-item below), human-reviewed and approved 2026-07-29, and merged to `main` through PR #10 on 2026-07-30 with GitHub Actions CI passing**. Delivered: the async `EvidenceStore`/`TopologyGraphStore` repository interfaces with strict read contracts (no unbounded or partially pinned read is expressible); complete resolved (asOf, horizon, derivationVersion) metadata plus `schemaVersion` structurally on every graph-read result family including `SnapshotSummary`; subject/assertion binding (no result may pair a subject with an assertion about a different subject); entity-only inventory with match-by-any-visible-claim `entityType` filter semantics (ADR-0020 § 1–2); identifier-only search with locale-independent ASCII normalization (ADR-0020 § 3); the relationship endpoint referential-integrity obligation (ADR-0019 § 4, defined here, proven by the implementing slice); and the storage-agnostic contract-test suite skeleton with **23 contract cases**, **297 shared-package tests passing**, and the full `scripts/verify.sh` gate passing. Scope: **async repository interfaces in `packages/shared`** carrying the **full M1 repository contract** — reads pinned by (asOf, horizon, derivationVersion); bounded collection reads and bounded traversal; Evidence lookup and evidence-chain contracts; subject and GraphAssertion revision reads; conflict and ambiguity retrieval; the relationship endpoint referential-integrity obligations (per ADR-0019 § 4, defined here and proven by the implementing slice) — plus a **reusable, storage-agnostic contract-test suite skeleton**. S2 does **not** authorize: an in-memory or database repository implementation (S6); fixture creation (S3); canonical serialization or hashing (S4/S6); temporal algorithms (S4); reconciliation (S5); API routes (S7); frontend changes; or any S3–S8 or M2+ work
  - [x] **S2 contract remediation per [ADR-0020](docs/adr/0020-m1-inventory-and-search-semantics.md) (Accepted 2026-07-29) — applied 2026-07-29**: the architecture ambiguity S2 review found in ADR-0017's inventory/search wording is resolved by accepted ADR-0020, and its contract corrections are now implemented in `packages/shared`: a strict `entityInventoryFilterSchema` (one optional `entityType` reusing the S1 classification token; empty object = unfiltered; unknown fields — including any "status" — rejected); the bounded `listEntities` repository read replacing `listSubjects` (entity-only inventory per ADR-0020 § 1, `EntityPage`/`entityReadResultSchema` so a Relationship subject cannot validate in an inventory result, match-by-any-visible-claim filtering under the resolved snapshot identity); `searchQuerySchema` now normalizing at the contract boundary by the exact ADR-0020 ASCII A–Z→a–z map (no locale-sensitive lowercasing, no Unicode folding, U+0130 unchanged, non-ASCII preserved; `normalizeSearchQuery` exported) with `searchSubjects` documented as matching complete canonical identifiers only; and 6 new storage-agnostic contract cases (conflicting-type match-by-each-claimed-type with all revisions and conflict markers retained in-band, unclaimed-type non-matching, asOf-validity-pinned filter evaluation, filter schema rejection, identifier-only search including claim-content non-matching, locale-independent normalization) — seed-adequacy preconditions fail loudly (missing conflicting-type or relationship scenarios throw, never pass vacuously; the S3 catalog scenarios 3 and 6 must supply them). The existing cases were migrated to `listEntities` inventory semantics (referential integrity still proven through ordinary reads — no diagnostic method restored). A final integrity-correction pass (2026-07-29) added two structural refinements: **subject/assertion binding** — `subjectReadResultSchema` and `entityReadResultSchema` now reject any assertion whose `subjectIdentifier` differs from the containing subject's identifier (precise per-assertion issue paths; rejection tests for entity and relationship results; one new contract case proving the binding across listEntities, searchSubjects in both namespaces, getSubject, and traverse) — and **snapshot `schemaVersion`** — `snapshotSummarySchema` now requires `schemaVersion` from the shared source of truth per ADR-0017 (missing/malformed/unknown-version rejection tests; the snapshot reproducibility contract case validates the complete summary schema and the echoed pinned identity; snapshots remain pinned-only, no latest mode). Every `TopologyGraphStore` result family now structurally carries `schemaVersion`. **Contract suite: 23 cases (16 prior + 6 ADR-0020 + 1 binding). Package tests: 297 passing in 11 files. Full `scripts/verify.sh` passing 2026-07-29.**
- [x] **Checkpoint `m1-s2-repository-contracts-merged` — canonical handoff documentation** — [HANDOFF.md](HANDOFF.md) (the model-neutral checkpoint and resume document) created and the checkpoint protocol added to [CLAUDE.md](CLAUDE.md) — _prepared and locally verified 2026-07-30; **merged to `main` through PR #11 at `a7a997d` on 2026-07-30 — checkpoint closed**_
- [x] **S3 — Fixture suite v1 in `fixtures/demo-company/`** — **explicitly human-authorized by Joseph Carfagno on 2026-07-30 and implemented 2026-07-30**; delivered the deterministic seven-scenario Evidence catalog, declared timestamp/horizon/as-of/derivation-version seeds, required valid and rejection edge cases, fact-versus-future-outcome documentation, and one existing-schema-only catalog validation suite. The 2026-07-30 independent-review remediation made scenario and invalid entries self-describing; made catalog keys, paths, and disk-file declarations strict; added non-vacuous per-scenario adequacy, raw Relationship endpoint-integrity, exact edge-seed, manifest-driven rejection path/code, and forbidden-derived-output checks. All 20 valid records parse individually and as one collection with globally unique sequences. **Verification:** 311 shared-package tests pass in 12 files; shared typecheck, repository formatting, lint, `git diff --check`, and the complete unmodified `./scripts/verify.sh` pipeline pass. **Scope remained bounded:** no production schema/contract, dependency, lockfile, storage, normalization, reconciliation, temporal, snapshot, API, frontend, connector, S4+, or M2+ change was made. **Independently reviewed and approved, and merged to `main` through PR #13 at `003bccc` on 2026-07-30 with GitHub Actions `verify` passing — checkpoint `m1-s3-fixture-suite-merged` ([HANDOFF.md](HANDOFF.md)). Closing S3 did not authorize S4; S4 was subsequently and separately released on 2026-07-31 (see below).**
- [x] **S4 pre-release clarification — [ADR-0021](docs/adr/0021-jcs-canonicalization-clarifications.md)** — **complete: accepted by Joseph Carfagno on 2026-07-31 after independent review.** ADR-0021 corrects accepted ADR-0016's canonical-serialization wording (RFC 8785 property names sort as raw UTF-16 code units, not Unicode code points; generic JCS preserves explicit `null` consistent with the merged S1 `jsonValueSchema`, with the omitted-vs-null rule a payload-builder rule, not a serializer rule), fixes the S4-helper / S5–S6 payload-builder composition boundary, and closes the three review questions (token stays `jcs-rfc8785`; validity helpers evaluate membership only; native string comparison only through the explicit locale-free comparator). ADR-0016 carries a metadata-only amendment notice; its accepted text is unchanged. **Acceptance resolved the S4 pre-release blocker.**
- [x] **S4 — Temporal foundations in `packages/graph-model`** — **explicitly human-authorized by Joseph Carfagno on 2026-07-31; ADR-0021 accepted the same day; release effective 2026-07-31 after the authorization PR #15 merged at `98d2abf` and `main` synchronized. Implemented 2026-07-31, independently reviewed and remediated, and merged to `main` through PR #16 at `63bdfab` on 2026-07-31 with GitHub Actions `verify` passing — checkpoint `m1-s4-temporal-foundations-merged` ([HANDOFF.md](HANDOFF.md)). Closing S4 did not authorize S5.** Delivered, all within the authorized boundary: the single explicit locale-free UTF-16 code-unit comparator (`compareUtf16CodeUnits`); Evidence total ordering by `observedAt` then `recordedSequence` with a pure copied-array sort (`compareEvidenceByTotalOrder`, `sortEvidenceByTotalOrder`); horizon selection by `recordedSequence ≤ horizon` with loud invalid-horizon rejection (`selectEvidenceAtHorizon`, `assertValidEvidenceHorizon`); half-open validity-interval membership evaluation on explicit canonical timestamps, evaluation only (`isTimestampWithinValidity`); RFC 8785 canonical serialization through the merged S1 `jsonValueSchema` public boundary with recursive lone-surrogate rejection, explicit-null preservation, undefined/sparse-hole/BigInt/function/symbol/NaN/infinity rejection, raw-UTF-16 property ordering, generic array-order preservation, and BOM-free compact UTF-8 (`toCanonicalJsonValue`, `canonicalizeToJcsString`, `canonicalizeToUtf8Bytes`); pure copied-array collection-ordering helpers (`sortIdentifiers`, `sortByIdentifier`); and lowercase-hex SHA-256 digest primitives over canonical bytes via `node:crypto` (`sha256HexOfBytes`, `sha256HexOfCanonicalJson`). The 2026-07-31 independent-review remediation pass then: routed `isTimestampWithinValidity` interval validation through the shared `validityIntervalSchema` (equal-bound, reversed, malformed, and unknown-field intervals now rejected loudly — no second interval schema created); removed every `?? null` fallback from the canonical serializer's recursive paths (an impossible internal `undefined` now throws an explicit TypeError instead of converting); made `sortByIdentifier` decorate–sort–undecorate so the extractor runs exactly once per element with stable equal-identifier ordering; and added the exact RFC 8785 § 3.2.3 seven-property sorting vector (carriage return, "1", U+0080, ö, €, 😀, דּ) with its prescribed code-unit output asserted byte-for-byte. **Verification (2026-07-31, post-remediation):** 82 graph-model tests pass in 5 files (RFC 8785 vectors including the exact § 3.2.3 seven-property fixture, UTF-16 code-unit-vs-code-point ordering, deep null preservation and no-undefined-to-null proof, rejection-first boundary cases, interval-contract rejection, extractor-call-count and stable-sort proofs, number/encoding edge cases, horizon and half-open interval boundaries, FIPS SHA-256 vectors, repeated-call byte identity, shuffled-input determinism, deep caller-non-mutation); graph-model typecheck, repository formatting, lint, `git diff --check`, and the complete unmodified `./scripts/verify.sh` pipeline pass (shared 311, api 1, web 5, graph-model 82 tests; browser acceptance 2/2). **Paths changed:** `packages/graph-model/**` (package.json gained `@atlast/shared` workspace dependency and the already-pinned Vitest/`@types/node` dev dependencies; tsconfig/vitest config map `@atlast/shared` to its workspace TypeScript source) and the deterministic `pnpm-lock.yaml` importer block for those declarations — no new third-party dependency, no version upgrade, no change to `packages/shared`, fixtures, reconciliation, confidence/freshness, `m1-v1`, GraphAssertion or snapshot payload builders, interval derivation, replay, storage, repositories, API, frontend, or connectors. **Independently reviewed, approved, and merged through PR #16 at `63bdfab` (2026-07-31) with GitHub Actions `verify` passing; local `./scripts/verify.sh` passed before merge — checkpoint closed; any later slice release remains pending explicit human authorization.** **Authorized paths (recorded for history; the slice is closed):** `packages/graph-model/**`; `pnpm-lock.yaml` solely for deterministic lockfile changes caused by the authorized package declarations (`@atlast/shared` as a workspace dependency; the already-pinned repository versions of Vitest and `@types/node` as development dependencies if required; Node.js built-in modules — **no new third-party runtime dependency or version upgrade**); and TASKS.md solely for factual S4 progress reporting. **Authorized content** (per [docs/m1-plan.md § 4](docs/m1-plan.md) and accepted ADR-0021): Evidence total ordering by `observedAt` then `recordedSequence`; pure, immutable/non-mutating Evidence ordering helpers; horizon-selection helpers using `recordedSequence ≤ horizon`; half-open validity-interval **membership evaluation** on explicit timestamps (no interval creation, derivation, closure, merging, splitting, or mutation); RFC 8785 canonical serialization per ADR-0021 (public runtime validation through the merged S1 `jsonValueSchema`, recursive well-formed-Unicode validation, exact locale-free UTF-16 property ordering, explicit `null` preservation, loud invalid-input rejection, generic array-order preservation); pure copied-array identifier-ordering helpers; SHA-256 canonical digest primitives with lowercase hexadecimal output; and focused unit tests (official RFC vectors, boundary, determinism, purity, caller-non-mutation). **S4 must NOT implement:** changes to `packages/shared` or the S1/S2 contracts; changes to `fixtures/demo-company`; reconciliation or identity matching; confidence or freshness computation; derivation policy `m1-v1`; GraphAssertion derivation or GraphAssertion payload builders; validity-interval derivation; snapshot construction or snapshot payload builders; snapshot replay, checksums-on-snapshots, or storage; repository implementations; API routes; frontend behavior; connectors; real-system access; or any S5+ or M2+ work.
- [x] **S5 pre-release clarification — [ADR-0022](docs/adr/0022-m1-reconciliation-policy-and-assertion-derivation.md)** — **complete: explicitly accepted by Joseph Carfagno on 2026-08-05 after independent review.** ADR-0022 is the binding `m1-v1` reconciliation specification, closing the implementation-critical gaps accepted ADR-0015 left open: the complete `m1-v1` policy document and its exact constants and alias data, exact normalization and type-free identifier-construction semantics, alias/ambiguity rules (no dangling near-match references), event-time standing-claim reconciliation with standing-source-filtered provenance, confidence arithmetic, validity-interval derivation with complete revision history, the closed schema-valid rule-trace vocabulary, the exact content-addressing payload through the S4 primitives, the pure reconciliation function boundary, the freshness helper (negative age rejected), and the exact S5/S6 boundary. ADR-0014 and ADR-0015 carry metadata-only amendment notices (accepted decision text preserved). **Acceptance resolved the S5 pre-release blocker.**
- [x] **S5 — Reconciliation engine (`m1-v1` derivation policy)** — **complete: human-authorized in principle 2026-07-31; ADR-0022 accepted 2026-08-05, releasing S5 as the only released implementation slice; release effective after the acceptance record merged via PR #18 at `f50f0d7` and `main` synchronized.** **Implemented 2026-08-05 under accepted ADR-0022 — independently reviewed with no blocking findings identified; the independent reviewer reran the complete `./scripts/verify.sh` successfully; PR #19 passed GitHub Actions `verify` and merged to `main` at `0923e9c` on 2026-08-05 — checkpoint `m1-s5-reconciliation-engine-merged` ([HANDOFF.md](HANDOFF.md)). Closing S5 did not authorize S6; the S6 pre-release architecture and authorization review subsequently completed with ADR-0023's acceptance on 2026-08-05 (see the S6 entry below), which itself does not authorize S6 implementation.** Delivered, all within the ADR-0022 § 14 boundary (`packages/graph-model/src/**` only; no manifest, lockfile, or dependency change): the deeply frozen `M1_V1_DERIVATION_POLICY` with every exact ADR-0022 § 1 literal (`derivation-policy.ts`); pinned six-step identity normalization (NFC once, ASCII-only lowercase, the literal whitespace set, collapse-to-hyphen, single-pass affix stripping, loud grammar rejection) plus type-free Entity/Relationship identifier construction (`identity-normalization.ts`); the pure event-time reconciliation engine (`reconciliation.ts`) — validated Evidence in, atomic equal-`observedAt` steps, latest standing claim per source, standing-source-filtered provenance (withdrawn support never lingers), exact IEEE-754 confidence, alternatives-only symmetric conflicts, both-subjects-exist ambiguity with the exact reason text, half-open validity with complete closed-and-open revision history, schema-valid closed-vocabulary rule traces, content-addressed `atlast:assertion:<digest>` construction through the S4 RFC 8785/SHA-256 primitives, every output validated through the merged shared schemas, deterministic identifier-sorted output, no caller mutation, no clock/fs/network/randomness; and the query-time freshness classifier with deterministic negative-age RangeError (`freshness.ts`). **Verification (2026-08-05):** 129 graph-model tests pass in 9 files (47 new S5 tests: policy literals/deep-freeze/consistency; normalization fixture vectors, locale independence, explicit whitespace, empty/non-ASCII rejection; freshness anchors and negative-age rejection; scenario 1 atomic corroboration; scenario 2 H1/H2 stability; scenario 3 conflict symmetry; scenario 5 H7/H8 ambiguity without dangling references; scenario 6 revision chain, duplicate-source counting, late-old horizon safety, disappearance-closes-nothing; the constructed ADR-0022 invariant 7 withdrawn-support vector; engine-wide schema validation, content-address recomputation, citation-subset and no-future-leakage checks, shuffled-input replay determinism, byte identity, non-mutation, invalid-collection/horizon rejection, deterministic ordering, no `detail` field); graph-model typecheck, repository formatting, lint, `git diff --check`, and the complete unmodified `./scripts/verify.sh` pipeline pass (shared 311, api 1, web 5, graph-model 129; browser acceptance 2/2). Fixtures were read, never modified. _PR #19 merged 2026-08-05 with GitHub Actions `verify` passing; checkpoint closed._ **S5 was limited to the exact accepted ADR-0022 boundary** (§§ 1–14): the `m1-v1` derivation policy; normalization and stable identifier construction; aliases and ambiguity; event-time standing-claim reconciliation; standing-source-filtered provenance; confidence and conflict derivation; assertion validity and complete revision history; schema-valid rule traces; content-addressed GraphAssertion construction; pure reconciliation; freshness classification; and focused tests — **only within the ADR-0022 § 14 authorized paths** (`packages/graph-model/src/**` and TASKS.md for factual progress; no manifest or lockfile change; no new dependency). **S5 must NOT implement:** changes to `packages/shared` or S1/S2 contracts; fixture JSON; repository/storage implementations; snapshot construction, replay, or checksums; query/API routes; frontend; connectors; infrastructure; or any S6–S8 or M2+ work.
- [~] **S6 — Snapshot layer + in-memory stores; contract tests pass** — **explicitly authorized by Joseph Carfagno on 2026-08-05.** The S6 pre-release architecture and authorization review completed first: [ADR-0023](docs/adr/0023-m1-snapshot-and-in-memory-store-semantics.md) — the review packet auditing the eighteen assigned S6-readiness behaviors against the accepted ADRs and merged S1/S2/S4/S5 contracts — was **explicitly accepted by Joseph Carfagno on 2026-08-05 after three independent-review correction passes** (the first: cursor-bound continuation semantics, identity-scoped referential integrity, caller-input protection and output isolation, the exact `m1-v2` seed proof obligation, empty-Evidence-store behavior, machine-readable error `code`/`reason` literals, status accuracy; the second: complete semantic horizon validity with `HORIZON_AFTER_CURRENT_WATERMARK` rejection, the exact `ZodError`-versus-`EvidenceAppendError` boundary with atomicity across both failure kinds, known-but-not-visible assertion revisions as identity-scoped `UnknownIdentifierError`, exact `readonly` error property contracts; the third: graph versus Evidence cursor kinds and `INVALID_CURSOR` versus `CURSOR_BINDING_MISMATCH`), settling all nine identified S6 design gaps and amending ADR-0016, ADR-0018, and ADR-0019 via metadata-only notices (accepted decision text preserved). **ADR-0023 is the binding S6 clarification and remains Accepted, unchanged.** ADR-0023's acceptance did not itself authorize S6; **the separate, explicit S6 authorization decision this entry records was made by Joseph Carfagno on 2026-08-05**, releasing S6 as the **only active authorized implementation slice** — **effective only after the documentation PR recording this authorization merges to `main` and `main` is synchronized locally with a clean working tree; no S6 implementation file may be created before that.** **Authorization does not approve future S6 implementation output**: that output still requires implementation under accepted ADR-0023, independent review, passing local `./scripts/verify.sh` verification, PR approval with GitHub Actions `verify` passing, and merge. S6 is not implemented, reviewed, approved, or merged — it is authorized to begin. **Authorized S6 implementation scope** (per [ADR-0023 § 10](docs/adr/0023-m1-snapshot-and-in-memory-store-semantics.md) and [docs/m1-plan.md § 4](docs/m1-plan.md)): `packages/graph-model/src/**` only — snapshot construction; snapshot identity, payload, and checksum construction; the in-memory `EvidenceStore` and `TopologyGraphStore` implementations; the required injected `Clock` type and derivation-version lookup; graph and Evidence cursor binding and continuation validation; identity-scoped relationship referential-integrity enforcement; the repository error taxonomy from ADR-0023 § 9; registration of the existing S2 repository contract suite using an injected fixture loader; and colocated focused tests — plus TASKS.md solely for factual S6 progress reporting. **S6 must NOT change or implement:** `packages/shared` or any S1/S2 schema, type, interface, or contract; `fixtures/demo-company/**`; any package manifest or lockfile; any new dependency or dependency upgrade; `apps/api` or S7 query/API behavior; `apps/web` or frontend behavior; connectors, authentication, infrastructure, deployment, or real-system access; `m1-v2` or any second derivation policy; `scripts/verify.sh` or `scripts/bootstrap.sh`; or any S7, S8, M2, or later work. S1–S5 remain complete; S7–S8 and M2+ remain gated and unauthorized._ **Progress note (2026-08-10, factual, not a completion claim):** within the authorized scope, `packages/graph-model/src/**` now contains the S6-A repository/snapshot foundations, the S6-B in-memory `EvidenceStore`, the S6-C1 pure snapshot-construction core, the S6-C2a snapshot resolver, and the S6-C2b in-memory `TopologyGraphStore` (all seven frozen methods — `getSubject`, `getAssertionRevision`, `listEntities`, `searchSubjects`, `traverse`, `getEvidenceChain`, `getSnapshotSummary` — composed on the snapshot resolver, with graph cursor issuance/binding/continuation, query-time freshness, deterministic ordering, bounded traversal, and Evidence-chain construction), plus their colocated tests (349 graph-model tests passing in 18 files, following an independent-review remediation pass on the S6-C2b implementation — graph cursor cross-operation replays now reject as `CURSOR_BINDING_MISMATCH` rather than `INVALID_CURSOR`; cursor bindings are validated before any snapshot resolution for both pinned and cursor-authoritative requests; `traverse` now returns the visible Relationship subject on every followed hop alongside the reached Entity, per ADR-0020; a terminal-position `getEvidenceChain` cursor now rejects as `INVALID_CURSOR` rather than risking a schema-violating empty result; and `getEvidenceChain` cursors are now bound to a distinct Evidence-total-order token rather than the identifier-ascending token). The complete `./scripts/verify.sh` pipeline passes locally. The S2 storage-agnostic contract-test suite is not yet registered against these implementations (S6-D, still to come), and none of this work has been independently reviewed, approved, or merged.

**Progress note (2026-08-10, factual, not a completion claim):** S6-D — registration of the frozen, completely unmodified `registerRepositoryContractSuite` (from `@atlast/shared`) against the committed `InMemoryEvidenceStore`/`InMemoryTopologyGraphStore` pair — is implemented in a new colocated `packages/graph-model/src/contract-suite.test.ts`, which loads the S3 `demo-company` fixture catalog through an injected, deterministic test-only loader (the same `fileURLToPath`/`readFileSync` pattern `reconciliation.test.ts` already uses) and supplies a `RepositoryFactory` that builds a fresh, isolated repository pair per contract case with an explicit fixed injected `Clock`. All 23 frozen contract cases in `repositoryContractCases` execute and pass unmodified against the real S6 implementations, with no case copied, altered, skipped, wrapped, renamed, or selectively registered, and no change to `packages/shared`, fixtures, or any accepted ADR. No implementation defect was exposed; the S6-C2b remediation from the preceding checkpoint already satisfied every behavioral obligation the suite proves. Total graph-model tests: 372 passing in 19 files. The complete `./scripts/verify.sh` pipeline passes locally. None of this work has been independently reviewed, approved, or merged.

**Progress note (2026-08-10, factual, not a completion claim):** an independent release-candidate audit of the complete S6 branch delta (`git diff origin/main...HEAD`) against accepted ADR-0023 found one source-hygiene defect, now corrected: `packages/graph-model/src/snapshot-resolver.ts`'s `identityCacheKey` function contained two literal NUL bytes (used as a field separator inside its cache-key template literal) that caused Git to classify the entire file as binary — invisible to `git diff`, PR review, and text search tooling, though functionally inert at runtime (both sides of every cache-key comparison contained the same bytes). The two NUL bytes were replaced with a `|` separator; no other line in the file changed. No behavioral, contract, or test defect was found elsewhere in the audited scope (repository error taxonomy, Clock injection, derivation-version lookup, graph/Evidence cursor binding and continuation, snapshot-checksum payload, semantic horizon validity, identity-scoped referential integrity, `InMemoryEvidenceStore`, snapshot construction, `SnapshotResolver`, `InMemoryTopologyGraphStore`'s seven methods, the S6-D contract-suite registration, and the public `index.ts` export surface). Post-fix verification: graph-model 372/372 tests passing in 19 files; graph-model typecheck clean; `pnpm format:check` and `pnpm lint` clean repository-wide; `git diff --check origin/main...HEAD` clean; the complete `./scripts/verify.sh` pipeline (all 7 stages, including browser acceptance 2/2) passes. None of this work has been independently reviewed, approved, or merged; S6 remains unpublished pending human approval.

- [!] S7 — Query API v1 routes + integration tests — _gated on slice release_
- [!] S8 — Acceptance additions (if shell changes), M1 boundary re-audit, documentation closeout — _gated on slice release_

M1 exit criteria remain as approved ([docs/m1-plan.md § 11](docs/m1-plan.md), [docs/milestones.md](docs/milestones.md)):

- [ ] Model and query API run wholly from fixtures in CI with no external dependencies
- [ ] Every fact in the graph is traceable to its synthetic evidence via the API
- [x] Graph/evidence representation decisions recorded as ADRs and human-approved — _ADRs 0014–0019 Accepted 2026-07-23 (0014/0015 amended by ADR-0019)_
- [ ] No-real-systems constraint verified: M1 boundary re-audit passes (S8)

## M2 — Interactive Topology Interface (gated — not authorized)

- [ ] Graph exploration UI: navigation, search, entity detail
- [ ] Provenance/confidence/freshness view for every displayed fact
- [ ] Snapshot/history playback
- [ ] Browser acceptance checks for primary journeys

## M3 — Operational Health Overlays (gated — not authorized)

- [ ] Overlay model + synthetic state generator (healthy, degraded, down, disconnected, expiring certificate, latent downstream risk)
- [ ] Health-in-context queries and UI overlay toggles
- [ ] Unknown-entity overlays surface as gaps, not phantom nodes

## M4 — Change-Impact Simulation (gated — not authorized)

- [ ] Deterministic impact query API with change-type semantics
- [ ] Ranked blast radius with evidence path per claim
- [ ] Impact views in UI
- [ ] Synthetic scenario accuracy harness in CI
- [ ] Gate check: deterministic engine validated **before** any LLM-generated reasoning is considered

## M5 — Read-Only Local Kubernetes Connector (gated — not authorized)

- [ ] Read-only adapter targeting a disposable local cluster (e.g., Kind) only
- [ ] Adapter emits the same normalized evidence format as fixtures
- [ ] Freshness degradation verified when cluster dies/deletes
- [ ] Employer/production cluster connection structurally impossible without spec amendment

---

## Post-M5 (uncommitted)

Predictive AI, multi-cloud integrations, multi-source enterprise reconciliation, and advisory remediation recommendations — see [docs/milestones.md § Post-M5](docs/milestones.md#post-m5-directional-uncommitted). Executing remediation remains a permanent non-goal.

## Parking Lot

Ideas raised but not scheduled. Nothing here is committed; graduation requires a milestone home and spec compliance.

- (empty)
