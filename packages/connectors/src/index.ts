/**
 * Package boundary for `@atlast/connectors`, created during M0 to establish
 * the workspace structure and type-checking infrastructure only.
 *
 * The discovery connector implementation (read-only, evidence-emitting)
 * arrives in M5, once that milestone is explicitly authorized.
 */
export {};
