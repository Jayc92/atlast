# Architecture Decision Records

Binding decisions are made only through human-approved ADRs ([GUARDRAILS.md § 1.3](../../GUARDRAILS.md#13-change-discipline)). Format: `NNNN-short-title.md` with Context, Problem, Decision, Alternatives Considered, Tradeoffs, Consequences, Risks, fit rationale, and change conditions. Statuses: **Proposed → Accepted → Superseded** (or **Rejected**).

## Index

### Accepted (M0 — approved 2026-07-22)

| ADR                                          | Decision                                                                                                                 | Status   |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------- |
| [0001](0001-monorepo-package-manager.md)     | Monorepo package manager — pnpm                                                                                          | Accepted |
| [0002](0002-monorepo-task-runner.md)         | Task runner — pnpm workspace scripts (no dedicated runner)                                                               | Accepted |
| [0003](0003-frontend-framework.md)           | Frontend framework — React + Vite (client-rendered SPA)                                                                  | Accepted |
| [0004](0004-backend-api-framework.md)        | Backend API framework — Fastify                                                                                          | Accepted |
| [0005](0005-shared-validation-and-typing.md) | Shared validation/typing — strict TypeScript + Zod in a shared package                                                   | Accepted |
| [0006](0006-linting.md)                      | Linting — ESLint + typescript-eslint (type-aware)                                                                        | Accepted |
| [0007](0007-formatting.md)                   | Formatting — Prettier                                                                                                    | Accepted |
| [0008](0008-unit-testing.md)                 | Unit testing — Vitest                                                                                                    | Accepted |
| [0009](0009-integration-testing.md)          | Integration testing — in-process API contract tests over fixtures                                                        | Accepted |
| [0010](0010-browser-acceptance-testing.md)   | Browser acceptance testing — Playwright                                                                                  | Accepted |
| [0011](0011-local-development-runtime.md)    | Local development runtime — Node.js LTS, no containers                                                                   | Accepted |
| [0012](0012-initial-graph-storage.md)        | Initial graph storage (M0 only) — in-process model over fixtures, behind repository interfaces; M1 storage ADR mandatory | Accepted |
| [0013](0013-ci-philosophy.md)                | CI philosophy — CI runs exactly `scripts/verify.sh`, hermetically                                                        | Accepted |

**Approval note (2026-07-22):** ADRs 0001–0013 were formally approved by human review. Acceptance authorizes **M0 Phase B scaffolding only** — it does not authorize M1 or later milestone work, each of which requires its own explicit authorization per [docs/milestones.md](../milestones.md).

### Accepted (M1 planning decisions — approved 2026-07-23)

| ADR                                                               | Decision                                                                                                                   | Status                                     |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| [0014](0014-core-topology-domain-model.md)                        | Core topology domain model — stable subjects, content-addressed assertion revisions                                        | Accepted; amended by ADR-0019 and ADR-0022 |
| [0015](0015-deterministic-identity-reconciliation.md)             | Identity reconciliation — rules-first, deterministic, conflict-preserving                                                  | Accepted; amended by ADR-0019 and ADR-0022 |
| [0016](0016-temporal-graph-and-snapshots.md)                      | Temporal graph and snapshots — bitemporal records, derived snapshots, exact replay                                         | Accepted; amended by ADR-0021 and ADR-0023 |
| [0017](0017-m1-query-api-surface.md)                              | M1 query API surface — purpose-built, bounded, evidence-linked REST contract                                               | Accepted; amended by ADR-0020 and ADR-0024 |
| [0018](0018-m1-storage-strategy.md)                               | M1 storage strategy — retain fixture-backed in-memory storage behind the interfaces                                        | Accepted; amended by ADR-0023              |
| [0019](0019-subject-identity-and-assertion-claims.md)             | Subjects carry identity only; canonical claims own type/endpoints (amends 0014/0015)                                       | Accepted; amended by ADR-0023              |
| [0020](0020-m1-inventory-and-search-semantics.md)                 | M1 inventory/search semantics — claim-level `entityType` filter, identifier-only search (amends 0017)                      | Accepted; amended by ADR-0024              |
| [0021](0021-jcs-canonicalization-clarifications.md)               | JCS canonicalization clarifications — raw UTF-16 property ordering, null preservation (amends 0016)                        | Accepted                                   |
| [0022](0022-m1-reconciliation-policy-and-assertion-derivation.md) | M1 reconciliation policy and assertion derivation — the complete binding `m1-v1` contract (amends 0014/0015)               | Accepted; amended by ADR-0025              |
| [0023](0023-m1-snapshot-and-in-memory-store-semantics.md)         | M1 snapshot construction and in-memory repository semantics — closing the S6 gaps (amends 0016/0018/0019)                  | Accepted                                   |
| [0024](0024-m1-query-api-runtime-contract.md)                     | M1 query API runtime contract — closing the S7 HTTP-boundary and build-boundary gaps (amends 0017/0020)                    | Accepted; amended by ADR-0025              |
| [0025](0025-s7-source-alias-erasable-syntax-compatibility.md)     | S7 source-alias and erasable-syntax compatibility — narrow `IdentityNormalizationError` syntax refactor (amends 0022/0024) | Accepted                                   |

### Accepted M2 baseline (not implementation authority)

| ADR                                                       | Decision                                                                                               | Status   |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------- |
| [0026](0026-m2-browser-architecture-and-api-boundary.md)  | React SPA routing, validated query-only client, coordinated snapshot reads, and exact local proxy seam | Accepted |
| [0027](0027-m2-graph-rendering-and-accessibility.md)      | React Flow + ELK rendering/layout with a normative accessible structured topology view                 | Accepted |
| [0028](0028-m2-snapshot-navigation-and-trust-contract.md) | Bounded snapshot-anchor API, reproducible history navigation, and mandatory trust presentation         | Accepted |

**M2 approval note (2026-08-12):** Joseph Carfagno explicitly accepted ADRs 0026-0028 and approved [docs/m2-plan.md](../m2-plan.md) after independent review and correction. M2-A through M2-F were separately released and completed; checkpoint `m2-complete` merged through PR #52 at `b8cd2ee` on 2026-08-16. The M3 baseline was subsequently accepted, and M3-A was separately released and completed.

### Accepted M3 baseline (not implementation authority)

| ADR                                                       | Decision                                                                                                             | Status   |
| --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------- |
| [0029](0029-m3-overlay-model-and-temporal-semantics.md)   | Separate immutable overlay frames, deterministic direct/effective state, temporal selection, and unknown-target gaps | Accepted |
| [0030](0030-m3-health-in-context-api-contract.md)         | One bounded server-authoritative health-in-context route with explicit topology/frame coordinates                    | Accepted |
| [0031](0031-m3-overlay-presentation-and-accessibility.md) | Optional query-only graph/structured overlay presentation with non-color semantics and historical coordination       | Accepted |

**M3 approval note (2026-08-16):** Joseph Carfagno explicitly accepted ADRs 0029-0031 and approved [docs/m3-plan.md](../m3-plan.md) as the M3 implementation baseline after independent architecture review, correction, and focused re-review. M3-A was separately released, implemented, and closed through PR #56 at `a767c93`. Joseph then separately authorized M3-B only; activation requires its authorization record to merge and local `main` to synchronize cleanly. M3-C through M3-F and M4+ remain unauthorized.

### Accepted M4 baseline (not implementation authority)

| ADR                                                      | Decision                                                                                                        | Status   |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | -------- |
| [0032](0032-m4-change-impact-domain-model.md)            | Deterministic change-impact domain model, change-type semantics, and confidence-weighted widest-path ranking    | Accepted |
| [0033](0033-m4-impact-query-api-contract.md)             | One composed, bounded impact-query API route reusing the existing traversal contract                            | Accepted |
| [0034](0034-m4-impact-presentation-and-accessibility.md) | Optional, entity-scoped impact panel with precise non-color rank/change-type semantics and accessibility parity | Accepted |
| [0035](0035-m4-synthetic-accuracy-harness.md)            | Exact-match scripted-scenario accuracy harness, running under the existing verifier with no new CI stage        | Accepted |

**M4 approval note (2026-08-17):** ADRs 0032-0035 and [docs/m4-plan.md](../m4-plan.md) were drafted as the proposed M4 implementation baseline under Joseph Carfagno's 2026-08-17 authorization of M4 planning and pre-release architecture/ADR review only, independently reviewed and corrected, then **explicitly accepted by Joseph Carfagno on 2026-08-17** as the M4 implementation baseline. Acceptance becomes operational only after this record merges to `main` and local `main` is synchronized cleanly. **This acceptance does not release M4-A** — M4-A requires its own separate, explicit implementation authorization after that merge. M4 implementation remains gated until M4-A is separately released; M5+ remain unauthorized.

### Accepted M5 baseline (not implementation authority)

| ADR                                                         | Decision                                                                                                                                                       | Status   |
| ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| [0036](0036-m5-kubernetes-client-and-connector-boundary.md) | Kubernetes client library (`@kubernetes/client-node`), pinned, and the `packages/connectors/src/kubernetes/` module boundary exposing only a read-only surface | Accepted |
| [0037](0037-m5-read-only-credential-and-rbac-design.md)     | Namespace-scoped, read-only ServiceAccount/RBAC credential design, structural and live rejection proofs, and the real-system safety boundary                   | Accepted |

**M5-P approval note (2026-08-20):** ADRs 0036-0037 and [docs/m5-plan.md](../m5-plan.md) were drafted as the proposed M5-A implementation baseline under Joseph Carfagno's 2026-08-20 authorization of M5 planning and pre-release architecture/ADR review only. An independent architecture/security review found five required changes (the disposable-local-cluster definition, the CI-enforced structural read-only proof, the concrete spoof-resistant target guard, the strengthened live-ingestion acceptance proof, and an unresolved GUARDRAILS.md § 1.4 authentication-policy conflict); all were applied. The authentication-policy conflict was resolved separately as its own GUARDRAILS.md amendment (option (a): a narrowly scoped, ten-condition, self-invalidating M5-A exemption), merged through [PR #87](https://github.com/Jayc92/atlast/pull/87) at `9bbeec4` on 2026-08-20 — [GUARDRAILS.md § 1.4](../../GUARDRAILS.md#14-security) is the single source of truth for that exemption's exact conditions. With the independent review's required changes and the authentication-policy decision both resolved, ADRs 0036-0037 and [docs/m5-plan.md](../m5-plan.md) were **explicitly accepted by Joseph Carfagno on 2026-08-20** as the M5-A implementation baseline. **This acceptance does not release M5-A** — M5-A requires its own separate, explicit implementation authorization, effective only after this acceptance record merges to `main` and local `main` is synchronized cleanly. M5-B/C/D and every later milestone remain unauthorized.

### Accepted M5 storage/reconciliation remedy (not implementation authority)

| ADR                                              | Decision                                                                                                                                                                                            | Status   |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| [0038](0038-m5-reconciliation-scaling-remedy.md) | Resolve the fired ADR-0018 condition 2 by fixing the `m1-v1` reconciliation algorithm's O(n²) scaling; retain in-memory storage; reject SQLite/PostgreSQL/graph-database migration on this evidence | Accepted |

**M5 storage-reassessment and ADR-0038 approval note (2026-08-21):** [docs/audits/m0-synthetic-boundary-audit.md § 23](../audits/m0-synthetic-boundary-audit.md) measured the real M5-A workload (3,707 provenance entries on one assertion) at a **9.27-second median** single-read latency, firing ADR-0018's condition 2 (interactive latency measurably exceeding in-memory comfort) while conditions 1, 3, and 4 did not fire. Per [docs/m5-plan.md § 4.4](../m5-plan.md#4-proof-obligations), a fired condition requires a new storage-decision ADR before M5 can close — **[ADR-0038](0038-m5-reconciliation-scaling-remedy.md) is that ADR**. Direct code inspection traced the measured O(n²) cost to the accepted `m1-v1` reconciliation engine's per-step full-history rescan and per-revision content-addressing over an ever-growing provenance array (`packages/graph-model/src/reconciliation.ts`), not to the in-memory storage engine itself. ADR-0038 retains in-memory storage and requires the reconciliation algorithm's complexity to be fixed, and explicitly evaluates and rejects SQLite, PostgreSQL, and a dedicated graph database on this evidence. Following an independent-review precision pass replacing its blanket "byte-identical output" wording with an explicit enumeration of which outputs are genuinely contractual (content-addressed identifiers, canonical serialization, provenance membership/ordering, confidence, `ruleTrace`, conflict/ambiguity structure, and revision/validity-interval count) versus which are not (non-canonicalized wire-response property order, internal computation strategy), **ADR-0038 was explicitly accepted by Joseph Carfagno on 2026-08-21**. **Acceptance settles the architecture decision only — it does not itself authorize implementation.** The reconciliation-optimization implementation requires its own separate, explicit implementation authorization, effective only after this acceptance record merges to `main` and local `main` is synchronized cleanly. [ADR-0018](0018-m1-storage-strategy.md) itself is unedited and remains Accepted, unchanged, consistent with this project's amend-via-new-ADR convention. **M5 remains open; M5-B and M5-C remain unauthorized.**

**Approval note (2026-07-23):** ADRs 0014–0018 were accepted by human review as the **M1 architecture baseline**, alongside approval of [docs/m1-plan.md](../m1-plan.md). Acceptance settles the M1 planning decisions only — **it does not authorize M1 implementation**, which requires its own separate, explicit human authorization per [docs/milestones.md](../milestones.md).

**Approval note (2026-07-23, ADR-0019):** ADR-0019's identity-only subject decision was **accepted by human review**, resolving the internal contradiction between ADR-0014's typed-subject clauses and ADR-0015's coexisting-conflicting-claims requirement. It amends **only** those identified clauses (metadata-only amendment notices on ADR-0014/0015; their accepted decision text is preserved). Acceptance **unblocks Slice S1** — it does not authorize S2–S8 or M2+, which remain gated as before.

**Approval note (2026-07-29, ADR-0020):** ADR-0020 was **accepted by human architecture review**, resolving two contradictions found during S2 human review between ADR-0017's inventory/search wording and the accepted domain model (ADR-0014 as amended by ADR-0019): the undefined "filter by type and status" inventory phrase and the unimplementable "identifiers and names" search phrase. It amends **only** those two wording items (metadata-only amendment notice on ADR-0017; its accepted decision text is preserved), and complete canonical subject-identifier matching is approved for M1 search. Acceptance authorizes **only the remaining S2 contract remediation** described in ADR-0020 § 5 — it does **not** approve S2 itself, and it does **not** authorize S3–S8 or M2+, which remain gated as before.

**Approval note (2026-07-31, ADR-0021):** ADR-0021 was **accepted by Joseph Carfagno on 2026-07-31 after independent review**. It amends **only the canonical-serialization clauses of accepted ADR-0016** (metadata-only amendment notice on ADR-0016; its accepted decision text is preserved): object property names sort as raw UTF-16 code units per RFC 8785 § 3.2.3 (not Unicode code points), and generic JCS preserves explicit `null` — consistent with the merged S1 `jsonValueSchema` — while absent optional domain fields remain omitted by payload builders. Acceptance **resolves the S4 pre-release blocker**: S4 (temporal foundations in `packages/graph-model`) was human-authorized 2026-07-31 and is released, with implementation **effective only after the documentation PR recording this acceptance merges to `main` and `main` is synchronized** ([TASKS.md](../../TASKS.md), [HANDOFF.md](../../HANDOFF.md)). Acceptance does not imply approval of future S4 implementation output. **S5–S8 and M2+ remain gated and unauthorized.**

**Approval note (2026-08-05, ADR-0022):** ADR-0022 was **explicitly accepted by Joseph Carfagno on 2026-08-05 after independent review** and is the **binding `m1-v1` reconciliation specification**: the complete derivation-policy document (location, shape, constants, alias data), exact identity normalization and type-free stable-identifier construction, alias/ambiguity semantics, event-time standing-claim reconciliation with standing-source-filtered provenance, confidence arithmetic, validity-interval derivation with complete revision history, the closed schema-valid rule-trace vocabulary, the exact GraphAssertion identifying payload and content addressing through the S4 primitives, the pure reconciliation function boundary, the freshness helper, and the exact S5/S6 boundary. It **amends ADR-0014 and ADR-0015 via metadata-only notices** (their accepted decision text is preserved; on ADR-0014 it supersedes only the old illustrative `atlast:entity:service/checkout` identifier example; on ADR-0015 it is the binding completion of the previously abstract `m1-v1` policy — an amendment, not a supersession of either ADR). Acceptance **authorizes M1 Slice S5 only**, within ADR-0022 § 14, **effective only after the documentation record of this acceptance merges to `main` and `main` is synchronized locally with a clean working tree** ([TASKS.md](../../TASKS.md), [HANDOFF.md](../../HANDOFF.md)). Acceptance does not imply approval of future S5 implementation output. **S6–S8 and M2+ remain gated and unauthorized.**

**Approval note (2026-08-05, ADR-0023):** ADR-0023 — the output of the S6 pre-release architecture and authorization review (requested per [CLAUDE.md](../../CLAUDE.md) and [HANDOFF.md § 7](../../HANDOFF.md)) — was **explicitly accepted by Joseph Carfagno on 2026-08-05 after three independent-review and remediation passes** (the first corrected cursor-bound continuation semantics, identity-scoped referential integrity, caller-input protection, the `m1-v2` seed proof obligation, empty-Evidence-store behavior, machine-readable error codes/reasons, and status accuracy; the second corrected semantic horizon validity above the watermark, the `ZodError`/`EvidenceAppendError` boundary, known-but-not-visible assertion classification, and exact error property contracts; the third distinguished graph from Evidence cursors and unusable cursors from binding mismatches). It audits the eighteen assigned S6-readiness behaviors against the accepted set, finds nine already fully specified, and **settles the nine genuine, implementation-critical S6 design gaps**: clock injection and cursorless "latest" resolution, cursor-bound continuation reads for both graph and Evidence cursors, unsupported-`derivationVersion` rejection with the `m1-v2` seed's exact proof obligation, the snapshot content-addressing payload, checksum construction with complete semantic-horizon-validity and empty-snapshot semantics, identity-scoped relationship-endpoint referential-integrity enforcement, caller-input protection and returned-value mutation isolation, `appendEvidence` batch atomicity with the exact shared-schema boundary, and a repository-layer error taxonomy with closed `code`/`reason` literals and exact structural property contracts. **With acceptance, ADR-0023 amends ADR-0016 (snapshot payload/checksum construction and complete semantic horizon validity), ADR-0018 (injected-clock resolution, graph/Evidence cursor continuation semantics, append atomicity, input/output isolation, and repository errors), and ADR-0019 (identity-scoped relationship-endpoint referential-integrity enforcement) via metadata-only notices** — every amended ADR's accepted decision text is preserved verbatim. **Acceptance does not authorize M1 Slice S6 implementation**: it settles the S6 design only. S6 was subsequently and separately authorized (2026-08-05), implemented, independently reviewed, and merged (PR #23), then formally closed (PR #24) — see [HANDOFF.md](../../HANDOFF.md) and [TASKS.md](../../TASKS.md) for the current checkpoint. **S7–S8 and M2+ remain unauthorized.**

**Approval note (2026-08-11, ADR-0024):** ADR-0024 — the output of the S7 pre-release architecture and contract review (requested per [CLAUDE.md](../../CLAUDE.md) and [HANDOFF.md § 7](../../HANDOFF.md)), performed after S6's formal closure (PR #24) — was **explicitly accepted by Joseph Carfagno on 2026-08-11 after the complete independent review and correction cycle** recorded below (three independent correction passes closing the exact route-parameter matrix, the snapshot-summary HTTP envelope, the closed error-response contract, the ADR-0020 amendment framing, the Fastify repeated-key claim, asynchronous application initialization, the direct workspace-dependency decision, and the build/runtime package-entry-point strategy, including the `rewriteRelativeImportExtensions` fix). It audits the eighteen assigned S7-readiness review points against accepted ADR-0017 (as amended by ADR-0020) and the merged S2/S6 contracts, finds seven already fully specified, and **settles fifteen genuine, implementation-critical HTTP-boundary and build-boundary gaps** in its §§ 1–15. **With acceptance, ADR-0024 amends ADR-0017 (§§ 1–5, 9–13: exact route inventory, per-route parameters, request-coercion, error-mapping, and composition-root specifics ADR-0017 described only in general form) and ADR-0020 (correcting its "entity detail and traversal" phrase to "search and traversal" for how Relationship subjects reach consumers) via metadata-only notices** — every amended ADR's accepted decision text is preserved verbatim; both amendment notices are recorded in the amended ADRs themselves. **Acceptance does not authorize M1 Slice S7 implementation**: it settles the S7 architecture and runtime contract only. **No implementation slice is currently active; S7–S8 and M2+ remain gated and unauthorized** — the next permitted action is a separate, explicit S7 authorization decision recorded in [TASKS.md](../../TASKS.md).

**Approval note (2026-08-11, ADR-0025):** ADR-0025 — drafted after an actual M1 Slice S7-B implementation attempt reproduced a genuine, implementation-critical contradiction between accepted ADR-0011 (`apps/api`'s `erasableSyntaxOnly: true`) and accepted ADR-0024 § 14 step 5 (the `@atlast/graph-model` typecheck/test source alias): once `apps/api` actually imports from `@atlast/graph-model`, TypeScript checks that package's entire source under `apps/api`'s own stricter compiler options, and `packages/graph-model/src/identity-normalization.ts`'s `IdentityNormalizationError` constructor's two parameter properties fail with TS1294 — was **explicitly accepted by Joseph Carfagno on 2026-08-11** after independent review and two correction passes. **Acceptance authorizes exactly the one named `identity-normalization.ts` compatibility refactor (parameter properties replaced with `declare readonly` fields plus explicit constructor assignment) and a factual `TASKS.md` progress update** — no second, separate implementation release, since Slice S7 is already authorized and active and this ADR only widens its exact boundary for this one prerequisite fix. **Acceptance does not approve S7-A/S7-B implementation output and does not authorize S8/M2+.** **With acceptance, ADR-0025 amends ADR-0022 and ADR-0024 via metadata-only notices** (their accepted decision text preserved verbatim), recorded in both amended ADRs. **The authorized refactor is not yet implemented**: per this project's standing checkpoint discipline, it may begin only after the PR recording this acceptance merges to `main` and the primary S7 implementation branch (`feat/m1-s7-query-api`) is synchronized onto that checkpoint. Slice S7 remains the only active authorized implementation slice and **remains temporarily blocked** on this contradiction until then.

### ADR-0024 pre-acceptance review and correction record (historical)

Preserved as the record of the review and correction cycle that preceded the acceptance above; nothing below reflects ADR-0024's current (Accepted) status — the approval note above states that.

**Review note (2026-08-11, ADR-0024):** ADR-0024 is the output of the S7 pre-release architecture and contract review (requested per [CLAUDE.md](../../CLAUDE.md) and [HANDOFF.md § 7](../../HANDOFF.md)), performed after S6's formal closure (PR #24). It audits eighteen assigned S7-readiness review points against accepted ADR-0017 (as amended by ADR-0020) and the merged S2/S6 contracts, finds seven already fully specified, and proposes settling fifteen genuine, implementation-critical gaps. **An independent correction pass, applied the same review cycle, closed several items the first draft had deferred rather than settled**: an exact per-route parameter matrix; the exact snapshot-summary HTTP envelope (resolving the mismatch between ADR-0017's general envelope and the repository's flat `SnapshotSummary`); a single closed, discriminated error-response union with an exact `details` shape and status per code (replacing an open-ended `code: z.string()`/`details: z.unknown()`), including previously unmapped Fastify route-not-found and unexpected-exception cases and an explicit redaction policy; a corrected, factually accurate statement of Fastify's repeated-query-key behavior (withdrawing an incorrect claim naming `qs` as the parser); an explicit asynchronous application-initialization lifecycle guaranteeing no request can be served before fixture ingestion completes; a corrected direct-dependency decision (`apps/api` depends directly on both `@atlast/graph-model` and `@atlast/shared`, not one transitively through the other); and — following an audit of the actual current package manifests and tsconfigs — a confirmed, production-valid package-entry-point build strategy for `packages/shared` and `packages/graph-model`, since neither currently produces build output or declares an entry point, and the existing test-only source-alias convention alone cannot make a compiled `apps/api` resolve them at runtime. The review also found that accepted ADR-0020's phrase "entity detail and traversal" is factually imprecise (entity detail can never return a Relationship subject) and would require its own metadata-only amendment, in addition to ADR-0017's.

**Second review note (2026-08-11, ADR-0024, third-pass correction):** a further independent correction pass, made after reading the actual current `tsconfig.json`/`tsconfig.build.json` files and the S6 cursor implementation directly, found the prior revision had asserted, rather than proven, that its build strategy would work, and had left three further specifics incorrect or unenforceable. ADR-0024 was revised again (still **Status: Proposed**) to: (1) complete the `packages/shared`/`packages/graph-model` build configuration with `declaration: true` output and an explicit empty `paths` override during emit builds (an inherited source-alias `paths` mapping left active during a build can pull a dependency's source outside the consuming package's `rootDir`, corrupting the package boundary), extend the same override to `apps/api/tsconfig.build.json`, and require an explicit clean-build-then-run proof (`rm -rf` each package's `dist/`, `pnpm build`, `node apps/api/dist/server.js`) rather than declaring the strategy correct "by construction"; (2) remove a proposed health-only `buildApplication` mode that would have produced two different application shapes, conflicting with ADR-0009's fully-assembled-application testing requirement — `buildApplication` now always requires its full repository dependencies, and the existing `app.test.ts` is explicitly scoped as an S7 change (no longer claimed to remain byte-for-byte unchanged); (3) correct the cursor-transport policy, withdrawing an unenforceable invariant (that a cursor's _originating_ latest-or-pinned request mode could be checked) the S6 cursor payload does not encode, replacing it with the two behaviors the repository actually implements and an explicit statement that transport-mode history is not cursor-bound and is not inferred; and (4) tighten the closed error-response schema so `mismatchFields`, horizon/watermark, and derivation-version fields reuse the repository's own existing bounded schemas instead of permissive `z.string()`/`z.number()`, so `CURSOR_BINDING_MISMATCH`'s graph and evidence variants stay the two exact, disjoint shapes the repository error already defines rather than one shape with every field optional, and so the `MALFORMED_REQUEST` claim is narrowed to failures Fastify's own `setErrorHandler` actually governs, not raw Node-level HTTP parser failures. The seven-route surface (§ 1) is unchanged by this pass. **No accepted ADR was edited.** **This document remains Proposed, not Accepted, and none of its three drafts authorizes M1 Slice S7 implementation** — S7 remains gated pending its own separate, explicit human release recorded in [TASKS.md](../../TASKS.md), exactly as [HANDOFF.md](../../HANDOFF.md) and [docs/milestones.md](../milestones.md) require.

**Third review note (2026-08-11, ADR-0024, fourth-pass correction):** a further, narrowly scoped correction found that the third pass's own proposed `packages/shared/tsconfig.build.json` and `packages/graph-model/tsconfig.build.json` still fail to build: both packages combine `"allowImportingTsExtensions": true` with relative `.ts`-extensioned imports in their existing `tsconfig.json`, satisfied today only because that base config is `noEmit: true`; a build config that sets `noEmit: false` without also setting `rewriteRelativeImportExtensions` (or `emitDeclarationOnly`) satisfies none of `allowImportingTsExtensions`'s three permitted conditions, and `tsc` rejects it outright with TS5096. ADR-0024 § 14 was revised again (still **Status: Proposed**; the resolved-gap count stays at fifteen — this pass narrows § 14 further rather than adding a gap) to add `"rewriteRelativeImportExtensions": true` to both packages' `tsconfig.build.json` files only — never to either package's existing no-emit `tsconfig.json` — which simultaneously satisfies `allowImportingTsExtensions`'s constraint during emit and rewrites each package's relative `.ts` import specifiers to `.js` in the emitted output. `apps/api/tsconfig.build.json` needs no equivalent addition: `apps/api/tsconfig.json` already sets `rewriteRelativeImportExtensions: true` directly (it never uses `allowImportingTsExtensions`), so its build config already inherits the setting. The Exact S7 Boundary, build-strategy narrative, and testable invariants (§ 14) were updated to reflect this. **No accepted ADR was edited.** **This document remains Proposed, not Accepted, and none of its four drafts authorizes M1 Slice S7 implementation** — S7 remains gated pending its own separate, explicit human release recorded in [TASKS.md](../../TASKS.md), exactly as [HANDOFF.md](../../HANDOFF.md) and [docs/milestones.md](../milestones.md) require.
