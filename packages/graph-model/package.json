{
  "name": "@atlast/graph-model",
  "version": "0.0.0",
  "private": true,
  "description": "Topology graph model behind repository interfaces (ADR-0012) — implementation gated on M1 authorization.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --project tsconfig.json"
  }
}
