/**
 * Package boundary for `@atlast/graph-model`, created during M0 to establish
 * the workspace structure and type-checking infrastructure only.
 *
 * The topology graph implementation (behind repository interfaces per
 * ADR-0012) arrives in M1, once that milestone is explicitly authorized.
 */
export {};
