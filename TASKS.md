# Atlast — Tasks

The single place in-flight work is tracked ([GUARDRAILS.md § 4](GUARDRAILS.md#4-documentation-standards)). **M0, M1, and M2 are complete. M3-A through M3-D are complete; M3-D merged through PR #64 at `a2c2d92` on 2026-08-17.** Joseph Carfagno explicitly authorized M3-E only; activation awaits the combined M3-D closeout/M3-E authorization record's merge and clean local synchronization. **M3-F and M4+ remain unauthorized.**

**Legend:** `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked (note the blocker)

---

## M0 — Safe Project Foundation (complete — 2026-07-22)

### Phase A — Documentation (approved 2026-07-22)

- [x] README.md — project overview and documentation map
- [x] PROJECT_SPEC.md — vision, goals, principles, scope, non-goals
- [x] docs/architecture.md — architecture philosophy and conceptual design (draft)
- [x] docs/milestones.md — authorized milestone sequence M0–M5 with exit criteria
- [x] GUARDRAILS.md — engineering/coding/repository/documentation/testing standards
- [x] CLAUDE.md — AI assistant working instructions
- [x] TASKS.md — this file
- [x] Human review and approval of the documentation set — _approved 2026-07-22 alongside ADRs 0001–0013 ([docs/adr/README.md](docs/adr/README.md))_
- [x] Non-goals ([PROJECT_SPEC.md § 7](PROJECT_SPEC.md#7-non-goals--what-atlast-will-not-become)) explicitly reviewed and accepted — _part of the 2026-07-22 documentation approval_
- [x] Owners assigned to open questions in [docs/architecture.md § 7](docs/architecture.md#7-open-questions) — _all four questions assigned 2026-07-22 to Joseph Carfagno (Founder/Maintainer), each with a named future decision gate (M1 graph/evidence, M1 storage/evidence, and M1 query API ADRs, plus the M4 planning ADR); ownership is accountability for producing the ADR, not approval of any answer_

### Phase B — Foundation build (complete — authorized 2026-07-22, finished 2026-07-22)

- [x] M0 tooling ADRs (monorepo tooling, lint/format/type-check/test/build/browser-check stack) drafted and human-approved — _ADRs 0001–0013 in [docs/adr/](docs/adr/README.md), formally approved 2026-07-22; acceptance authorizes M0 Phase B scaffolding only_
- [x] TypeScript monorepo established — _root pnpm/Node config and workspace skeleton committed; package-manager bootstrap operational (`scripts/bootstrap.sh` verifies Node 24 + pinned pnpm and runs a frozen install) and `pnpm-lock.yaml` generated; TypeScript 6.0.3 pinned at the workspace root; repository-wide linting (`pnpm lint`, ESLint + typescript-eslint strict type-aware config per ADR-0006) and formatting (`pnpm format` / `pnpm format:check`, Prettier per ADR-0007) operational; recursive package-level type checking operational (`pnpm typecheck` per ADR-0002, covering the four shared package shells plus `apps/api`, `apps/web`, and `tests/acceptance` including their colocated tests). The inert standalone `tests/unit` and `tests/integration` workspace placeholders (manifest-only, no tests or configs) were removed 2026-07-22 in favor of the accepted colocated-test convention: unit tests (ADR-0008) live with the code they exercise (`apps/web` component tests, and the API suite in `apps/api`, whose `fastify.inject()` tests are the current in-process API contract tests per ADR-0009), while browser acceptance (ADR-0010) keeps its dedicated `tests/acceptance` workspace for its distinct Playwright runner and service lifecycle; the `tests/*` workspace glob still discovers any future substantive test workspace_
- [x] Web application shell — _React + Vite client-rendered SPA in `apps/web` per ADR-0003: strict-TypeScript foundation page communicating the product vision and M0 status (no topology, graph exploration, search, routing, or query behavior — gated on M1/M2); dev and preview servers bind to `127.0.0.1` only; on load the page requests relative `/api/health`, proxied by Vite to the API shell's `GET /health` on `127.0.0.1:3001` (no CORS change in `apps/api`), validates the payload, and renders deterministic checking/online/unavailable states with visible degradation on failure or malformed responses; unit tests (Vitest + jsdom + Testing Library, stubbed `fetch`, no network, no fixed sleeps) cover primary content and the online/unavailable states; builds to git-ignored `dist/`; responsive at desktop and narrow-mobile widths with semantic HTML, visible focus, and reduced-motion support. All non-browser verification (lint, format, typecheck, tests, build, frozen-lockfile install, dev+preview proxy smoke test) passed 2026-07-22; reviewer browser inspection completed 2026-07-22, covering desktop at 1280x720, narrow mobile at 390x844, the connected and unavailable API states, no horizontal overflow, and no browser console warnings; the automated Playwright browser-acceptance harness (ADR-0010) now exists in `tests/acceptance`, running the primary shell journey against the built API and built web bundle in desktop (1280x720) and mobile (390x844) Chromium projects_
- [x] Backend API shell — _Fastify shell in `apps/api` per ADR-0004: `GET /health` with an explicit response schema returning `{"status":"ok","service":"atlast-api"}`; binds to `127.0.0.1` only with no code path to any other interface (no auth per the M0 exemption in [GUARDRAILS.md § 1.4](GUARDRAILS.md#14-security)); application construction (`src/app.ts`) separated from network startup (`src/server.ts`) so the Vitest suite drives it via `fastify.inject()` with zero sockets (ADR-0008/0009); dev loop uses Node 24 native type stripping (`node --watch`, no tsx, per ADR-0011); builds to git-ignored `dist/`; explicit startup-failure handling and clean SIGINT/SIGTERM shutdown; root `pnpm test` / `pnpm build` recursive scripts added. No graph, topology, evidence, entity, relationship, discovery, or query behavior — that remains gated on M1_
- [x] Shared packages structure — _compile-only package shells for `packages/shared`, `packages/graph-model`, `packages/connectors`, and `packages/ui`: per-package strict tsconfigs, `typecheck` scripts, and empty `export {}` entrypoints. These are **empty M0 package boundaries only** — domain schemas/contracts and the graph model remain gated on M1 authorization, connector implementation on M5, and UI components on the authorized web/UI work_
- [x] Automated linting, formatting, type checking, tests, builds, browser acceptance checks — _all six command families operational and passing 2026-07-22: `pnpm lint` (ESLint per ADR-0006), `pnpm format:check` (Prettier per ADR-0007), `pnpm typecheck` (recursive tsc per ADR-0002, now including `tests/acceptance`), `pnpm test` (Vitest unit suites per ADR-0008 plus the Playwright acceptance suite per ADR-0010, discovered recursively), `pnpm build` (recursive), and the browser acceptance suite in `tests/acceptance` (Chromium-only, desktop 1280x720 + mobile 390x844 projects, self-booting built API + built web preview via Playwright `webServer`, web-first assertions, no fixed sleeps, artifacts retained only on failure and git-ignored). `scripts/verify.sh` as the single entry point remains a separate open task below_
- [x] `scripts/verify.sh` populated as the single verification entry point — _populated 2026-07-22 per ADR-0013: seven fail-fast stages in cheapest-first order (git whitespace validation, `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, non-browser Vitest suites, `pnpm build`, Playwright browser acceptance run exactly once); resolves the repository root so it runs from any directory; installs or upgrades no dependencies, never runs formatting in write mode, and does not intentionally modify tracked files, though its build and test stages create or update generated git-ignored artifacts such as `dist/`, `test-results/`, and Playwright failure reports (bootstrap and the one-time Chromium download are documented prerequisites, not stages); full pipeline passing locally 2026-07-22. CI wiring landed as the separate CI task below_
- [x] CI runs `scripts/verify.sh` and passes — _GitHub Actions workflow ([.github/workflows/verify.yml](.github/workflows/verify.yml)) per ADR-0013: one read-only Ubuntu `verify` job triggered by pull requests targeting `main`, pushes to `main`, and manual dispatch; installs the pinned Node (`.nvmrc`) and pnpm (`packageManager`), runs `scripts/bootstrap.sh` (frozen-lockfile install), installs the pinned Playwright Chromium with Linux dependencies, then runs exactly `./scripts/verify.sh`; third-party actions pinned to full commit SHAs on Node 24 action runtimes (PR #2 eliminated the Node 20 runtime deprecation warnings); no secrets, write permissions, artifacts, or deployments. Verified remotely 2026-07-22: PR #2 run [29977769566](https://github.com/Jayc92/atlast/actions/runs/29977769566) (success, 1m36s, zero annotations) and the final `main` run [29977876658](https://github.com/Jayc92/atlast/actions/runs/29977876658) at commit `783e95c` (success, 1m25s, zero annotations); the local verification contract also passes at the same commit_
- [x] Synthetic-data-only guarantee verified: repo holds no external connections or credentials — _static boundary audit of all Git-tracked content at commit `4111d24` completed and passed 2026-07-22: all authorized M0 product/runtime network paths are loopback-only (permitted developer tooling exceptions — package-manager and browser-download connections — are documented in the audit), the single environment variable affects only a local port, no credentials/sensitive files/employer material found, no product dependency provides external-system integration capability, fixtures documentation-only and fictional. Full evidence, method, exceptions, and stated limitations in [docs/audits/m0-synthetic-boundary-audit.md](docs/audits/m0-synthetic-boundary-audit.md); re-audit is warranted when M1 fixture data or the M5 connector lands_

---

## M1 — Synthetic Topology Model (complete — 2026-08-12)

### Planning (complete — approved 2026-07-23)

- [x] [docs/m1-plan.md](docs/m1-plan.md) — M1 architecture and implementation plan — _approved 2026-07-23 as the M1 implementation baseline_
- [x] ADR-0014 core topology domain model — _Accepted 2026-07-23_
- [x] ADR-0015 deterministic identity reconciliation — _Accepted 2026-07-23_
- [x] ADR-0016 temporal graph and snapshots — _Accepted 2026-07-23_
- [x] ADR-0017 M1 query API surface — _Accepted 2026-07-23_
- [x] ADR-0018 M1 storage strategy (mandatory per ADR-0012) — _Accepted 2026-07-23_
- [x] [ADR-0019](docs/adr/0019-subject-identity-and-assertion-claims.md) subject identity and assertion claims — _Accepted 2026-07-23 by human review; resolves the ADR-0014/ADR-0015 contradiction (typed subjects vs. coexisting conflicting type claims) by amending only those clauses (metadata-only notices on 0014/0015); fixes the S1/S2/S6 validation-layer split; unblocks S1 below without authorizing S2+_

### Implementation (authorized 2026-07-23 — active, slice-gated per [docs/m1-plan.md § 4](docs/m1-plan.md))

**M1 implementation was explicitly authorized by human decision on 2026-07-23.** The authorization permits executing the approved plan **one slice at a time, each independently reviewed**:

- [x] **S1 — Domain schemas in `packages/shared`** (Evidence incl. safe-integer `recordedSequence`, identity-only subjects and claim union per ADR-0014 as amended by [ADR-0019](docs/adr/0019-subject-identity-and-assertion-claims.md), content-addressed GraphAssertion revisions, identifiers, `schemaVersion`, schema-rejection tests; schema validation only — no repository lookups, per ADR-0019 § 4) — _implemented 2026-07-29 and **human-reviewed and approved 2026-07-29** (remediation pass applied same day before approval: normalized Evidence observation union with source-scoped relationship endpoints, per-claim confidence in conflict structures, kind-consistency checks for competing claims and near-matches, evidence-linked rule traces validated against provenance, Evidence-identifier uniqueness in collections, `z.json()` as the JSON single source): Zod 4.4.3 schemas + inferred types in `packages/shared/src/` with 171 colocated rejection-first Vitest tests, full `scripts/verify.sh` passing; Zod introduction justified at PR per ADR-0005 and [docs/m1-plan.md § 12](docs/m1-plan.md). **Merged to `main` through PR #7 on 2026-07-29**_
- [x] **S2 — Repository interfaces + contract-test suite skeleton** — **authorized 2026-07-29 (released after S1 merged via PR #7)**; **implemented, ADR-0020 remediation and final integrity corrections applied (sub-item below), human-reviewed and approved 2026-07-29, and merged to `main` through PR #10 on 2026-07-30 with GitHub Actions CI passing**. Delivered: the async `EvidenceStore`/`TopologyGraphStore` repository interfaces with strict read contracts (no unbounded or partially pinned read is expressible); complete resolved (asOf, horizon, derivationVersion) metadata plus `schemaVersion` structurally on every graph-read result family including `SnapshotSummary`; subject/assertion binding (no result may pair a subject with an assertion about a different subject); entity-only inventory with match-by-any-visible-claim `entityType` filter semantics (ADR-0020 § 1–2); identifier-only search with locale-independent ASCII normalization (ADR-0020 § 3); the relationship endpoint referential-integrity obligation (ADR-0019 § 4, defined here, proven by the implementing slice); and the storage-agnostic contract-test suite skeleton with **23 contract cases**, **297 shared-package tests passing**, and the full `scripts/verify.sh` gate passing. Scope: **async repository interfaces in `packages/shared`** carrying the **full M1 repository contract** — reads pinned by (asOf, horizon, derivationVersion); bounded collection reads and bounded traversal; Evidence lookup and evidence-chain contracts; subject and GraphAssertion revision reads; conflict and ambiguity retrieval; the relationship endpoint referential-integrity obligations (per ADR-0019 § 4, defined here and proven by the implementing slice) — plus a **reusable, storage-agnostic contract-test suite skeleton**. S2 does **not** authorize: an in-memory or database repository implementation (S6); fixture creation (S3); canonical serialization or hashing (S4/S6); temporal algorithms (S4); reconciliation (S5); API routes (S7); frontend changes; or any S3–S8 or M2+ work
  - [x] **S2 contract remediation per [ADR-0020](docs/adr/0020-m1-inventory-and-search-semantics.md) (Accepted 2026-07-29) — applied 2026-07-29**: the architecture ambiguity S2 review found in ADR-0017's inventory/search wording is resolved by accepted ADR-0020, and its contract corrections are now implemented in `packages/shared`: a strict `entityInventoryFilterSchema` (one optional `entityType` reusing the S1 classification token; empty object = unfiltered; unknown fields — including any "status" — rejected); the bounded `listEntities` repository read replacing `listSubjects` (entity-only inventory per ADR-0020 § 1, `EntityPage`/`entityReadResultSchema` so a Relationship subject cannot validate in an inventory result, match-by-any-visible-claim filtering under the resolved snapshot identity); `searchQuerySchema` now normalizing at the contract boundary by the exact ADR-0020 ASCII A–Z→a–z map (no locale-sensitive lowercasing, no Unicode folding, U+0130 unchanged, non-ASCII preserved; `normalizeSearchQuery` exported) with `searchSubjects` documented as matching complete canonical identifiers only; and 6 new storage-agnostic contract cases (conflicting-type match-by-each-claimed-type with all revisions and conflict markers retained in-band, unclaimed-type non-matching, asOf-validity-pinned filter evaluation, filter schema rejection, identifier-only search including claim-content non-matching, locale-independent normalization) — seed-adequacy preconditions fail loudly (missing conflicting-type or relationship scenarios throw, never pass vacuously; the S3 catalog scenarios 3 and 6 must supply them). The existing cases were migrated to `listEntities` inventory semantics (referential integrity still proven through ordinary reads — no diagnostic method restored). A final integrity-correction pass (2026-07-29) added two structural refinements: **subject/assertion binding** — `subjectReadResultSchema` and `entityReadResultSchema` now reject any assertion whose `subjectIdentifier` differs from the containing subject's identifier (precise per-assertion issue paths; rejection tests for entity and relationship results; one new contract case proving the binding across listEntities, searchSubjects in both namespaces, getSubject, and traverse) — and **snapshot `schemaVersion`** — `snapshotSummarySchema` now requires `schemaVersion` from the shared source of truth per ADR-0017 (missing/malformed/unknown-version rejection tests; the snapshot reproducibility contract case validates the complete summary schema and the echoed pinned identity; snapshots remain pinned-only, no latest mode). Every `TopologyGraphStore` result family now structurally carries `schemaVersion`. **Contract suite: 23 cases (16 prior + 6 ADR-0020 + 1 binding). Package tests: 297 passing in 11 files. Full `scripts/verify.sh` passing 2026-07-29.**
- [x] **Checkpoint `m1-s2-repository-contracts-merged` — canonical handoff documentation** — [HANDOFF.md](HANDOFF.md) (the model-neutral checkpoint and resume document) created and the checkpoint protocol added to [CLAUDE.md](CLAUDE.md) — _prepared and locally verified 2026-07-30; **merged to `main` through PR #11 at `a7a997d` on 2026-07-30 — checkpoint closed**_
- [x] **S3 — Fixture suite v1 in `fixtures/demo-company/`** — **explicitly human-authorized by Joseph Carfagno on 2026-07-30 and implemented 2026-07-30**; delivered the deterministic seven-scenario Evidence catalog, declared timestamp/horizon/as-of/derivation-version seeds, required valid and rejection edge cases, fact-versus-future-outcome documentation, and one existing-schema-only catalog validation suite. The 2026-07-30 independent-review remediation made scenario and invalid entries self-describing; made catalog keys, paths, and disk-file declarations strict; added non-vacuous per-scenario adequacy, raw Relationship endpoint-integrity, exact edge-seed, manifest-driven rejection path/code, and forbidden-derived-output checks. All 20 valid records parse individually and as one collection with globally unique sequences. **Verification:** 311 shared-package tests pass in 12 files; shared typecheck, repository formatting, lint, `git diff --check`, and the complete unmodified `./scripts/verify.sh` pipeline pass. **Scope remained bounded:** no production schema/contract, dependency, lockfile, storage, normalization, reconciliation, temporal, snapshot, API, frontend, connector, S4+, or M2+ change was made. **Independently reviewed and approved, and merged to `main` through PR #13 at `003bccc` on 2026-07-30 with GitHub Actions `verify` passing — checkpoint `m1-s3-fixture-suite-merged` ([HANDOFF.md](HANDOFF.md)). Closing S3 did not authorize S4; S4 was subsequently and separately released on 2026-07-31 (see below).**
- [x] **S4 pre-release clarification — [ADR-0021](docs/adr/0021-jcs-canonicalization-clarifications.md)** — **complete: accepted by Joseph Carfagno on 2026-07-31 after independent review.** ADR-0021 corrects accepted ADR-0016's canonical-serialization wording (RFC 8785 property names sort as raw UTF-16 code units, not Unicode code points; generic JCS preserves explicit `null` consistent with the merged S1 `jsonValueSchema`, with the omitted-vs-null rule a payload-builder rule, not a serializer rule), fixes the S4-helper / S5–S6 payload-builder composition boundary, and closes the three review questions (token stays `jcs-rfc8785`; validity helpers evaluate membership only; native string comparison only through the explicit locale-free comparator). ADR-0016 carries a metadata-only amendment notice; its accepted text is unchanged. **Acceptance resolved the S4 pre-release blocker.**
- [x] **S4 — Temporal foundations in `packages/graph-model`** — **explicitly human-authorized by Joseph Carfagno on 2026-07-31; ADR-0021 accepted the same day; release effective 2026-07-31 after the authorization PR #15 merged at `98d2abf` and `main` synchronized. Implemented 2026-07-31, independently reviewed and remediated, and merged to `main` through PR #16 at `63bdfab` on 2026-07-31 with GitHub Actions `verify` passing — checkpoint `m1-s4-temporal-foundations-merged` ([HANDOFF.md](HANDOFF.md)). Closing S4 did not authorize S5.** Delivered, all within the authorized boundary: the single explicit locale-free UTF-16 code-unit comparator (`compareUtf16CodeUnits`); Evidence total ordering by `observedAt` then `recordedSequence` with a pure copied-array sort (`compareEvidenceByTotalOrder`, `sortEvidenceByTotalOrder`); horizon selection by `recordedSequence ≤ horizon` with loud invalid-horizon rejection (`selectEvidenceAtHorizon`, `assertValidEvidenceHorizon`); half-open validity-interval membership evaluation on explicit canonical timestamps, evaluation only (`isTimestampWithinValidity`); RFC 8785 canonical serialization through the merged S1 `jsonValueSchema` public boundary with recursive lone-surrogate rejection, explicit-null preservation, undefined/sparse-hole/BigInt/function/symbol/NaN/infinity rejection, raw-UTF-16 property ordering, generic array-order preservation, and BOM-free compact UTF-8 (`toCanonicalJsonValue`, `canonicalizeToJcsString`, `canonicalizeToUtf8Bytes`); pure copied-array collection-ordering helpers (`sortIdentifiers`, `sortByIdentifier`); and lowercase-hex SHA-256 digest primitives over canonical bytes via `node:crypto` (`sha256HexOfBytes`, `sha256HexOfCanonicalJson`). The 2026-07-31 independent-review remediation pass then: routed `isTimestampWithinValidity` interval validation through the shared `validityIntervalSchema` (equal-bound, reversed, malformed, and unknown-field intervals now rejected loudly — no second interval schema created); removed every `?? null` fallback from the canonical serializer's recursive paths (an impossible internal `undefined` now throws an explicit TypeError instead of converting); made `sortByIdentifier` decorate–sort–undecorate so the extractor runs exactly once per element with stable equal-identifier ordering; and added the exact RFC 8785 § 3.2.3 seven-property sorting vector (carriage return, "1", U+0080, ö, €, 😀, דּ) with its prescribed code-unit output asserted byte-for-byte. **Verification (2026-07-31, post-remediation):** 82 graph-model tests pass in 5 files (RFC 8785 vectors including the exact § 3.2.3 seven-property fixture, UTF-16 code-unit-vs-code-point ordering, deep null preservation and no-undefined-to-null proof, rejection-first boundary cases, interval-contract rejection, extractor-call-count and stable-sort proofs, number/encoding edge cases, horizon and half-open interval boundaries, FIPS SHA-256 vectors, repeated-call byte identity, shuffled-input determinism, deep caller-non-mutation); graph-model typecheck, repository formatting, lint, `git diff --check`, and the complete unmodified `./scripts/verify.sh` pipeline pass (shared 311, api 1, web 5, graph-model 82 tests; browser acceptance 2/2). **Paths changed:** `packages/graph-model/**` (package.json gained `@atlast/shared` workspace dependency and the already-pinned Vitest/`@types/node` dev dependencies; tsconfig/vitest config map `@atlast/shared` to its workspace TypeScript source) and the deterministic `pnpm-lock.yaml` importer block for those declarations — no new third-party dependency, no version upgrade, no change to `packages/shared`, fixtures, reconciliation, confidence/freshness, `m1-v1`, GraphAssertion or snapshot payload builders, interval derivation, replay, storage, repositories, API, frontend, or connectors. **Independently reviewed, approved, and merged through PR #16 at `63bdfab` (2026-07-31) with GitHub Actions `verify` passing; local `./scripts/verify.sh` passed before merge — checkpoint closed; any later slice release remains pending explicit human authorization.** **Authorized paths (recorded for history; the slice is closed):** `packages/graph-model/**`; `pnpm-lock.yaml` solely for deterministic lockfile changes caused by the authorized package declarations (`@atlast/shared` as a workspace dependency; the already-pinned repository versions of Vitest and `@types/node` as development dependencies if required; Node.js built-in modules — **no new third-party runtime dependency or version upgrade**); and TASKS.md solely for factual S4 progress reporting. **Authorized content** (per [docs/m1-plan.md § 4](docs/m1-plan.md) and accepted ADR-0021): Evidence total ordering by `observedAt` then `recordedSequence`; pure, immutable/non-mutating Evidence ordering helpers; horizon-selection helpers using `recordedSequence ≤ horizon`; half-open validity-interval **membership evaluation** on explicit timestamps (no interval creation, derivation, closure, merging, splitting, or mutation); RFC 8785 canonical serialization per ADR-0021 (public runtime validation through the merged S1 `jsonValueSchema`, recursive well-formed-Unicode validation, exact locale-free UTF-16 property ordering, explicit `null` preservation, loud invalid-input rejection, generic array-order preservation); pure copied-array identifier-ordering helpers; SHA-256 canonical digest primitives with lowercase hexadecimal output; and focused unit tests (official RFC vectors, boundary, determinism, purity, caller-non-mutation). **S4 must NOT implement:** changes to `packages/shared` or the S1/S2 contracts; changes to `fixtures/demo-company`; reconciliation or identity matching; confidence or freshness computation; derivation policy `m1-v1`; GraphAssertion derivation or GraphAssertion payload builders; validity-interval derivation; snapshot construction or snapshot payload builders; snapshot replay, checksums-on-snapshots, or storage; repository implementations; API routes; frontend behavior; connectors; real-system access; or any S5+ or M2+ work.
- [x] **S5 pre-release clarification — [ADR-0022](docs/adr/0022-m1-reconciliation-policy-and-assertion-derivation.md)** — **complete: explicitly accepted by Joseph Carfagno on 2026-08-05 after independent review.** ADR-0022 is the binding `m1-v1` reconciliation specification, closing the implementation-critical gaps accepted ADR-0015 left open: the complete `m1-v1` policy document and its exact constants and alias data, exact normalization and type-free identifier-construction semantics, alias/ambiguity rules (no dangling near-match references), event-time standing-claim reconciliation with standing-source-filtered provenance, confidence arithmetic, validity-interval derivation with complete revision history, the closed schema-valid rule-trace vocabulary, the exact content-addressing payload through the S4 primitives, the pure reconciliation function boundary, the freshness helper (negative age rejected), and the exact S5/S6 boundary. ADR-0014 and ADR-0015 carry metadata-only amendment notices (accepted decision text preserved). **Acceptance resolved the S5 pre-release blocker.**
- [x] **S5 — Reconciliation engine (`m1-v1` derivation policy)** — **complete: human-authorized in principle 2026-07-31; ADR-0022 accepted 2026-08-05, releasing S5 as the only released implementation slice; release effective after the acceptance record merged via PR #18 at `f50f0d7` and `main` synchronized.** **Implemented 2026-08-05 under accepted ADR-0022 — independently reviewed with no blocking findings identified; the independent reviewer reran the complete `./scripts/verify.sh` successfully; PR #19 passed GitHub Actions `verify` and merged to `main` at `0923e9c` on 2026-08-05 — checkpoint `m1-s5-reconciliation-engine-merged` ([HANDOFF.md](HANDOFF.md)). Closing S5 did not authorize S6; the S6 pre-release architecture and authorization review subsequently completed with ADR-0023's acceptance on 2026-08-05 (see the S6 entry below), which itself does not authorize S6 implementation.** Delivered, all within the ADR-0022 § 14 boundary (`packages/graph-model/src/**` only; no manifest, lockfile, or dependency change): the deeply frozen `M1_V1_DERIVATION_POLICY` with every exact ADR-0022 § 1 literal (`derivation-policy.ts`); pinned six-step identity normalization (NFC once, ASCII-only lowercase, the literal whitespace set, collapse-to-hyphen, single-pass affix stripping, loud grammar rejection) plus type-free Entity/Relationship identifier construction (`identity-normalization.ts`); the pure event-time reconciliation engine (`reconciliation.ts`) — validated Evidence in, atomic equal-`observedAt` steps, latest standing claim per source, standing-source-filtered provenance (withdrawn support never lingers), exact IEEE-754 confidence, alternatives-only symmetric conflicts, both-subjects-exist ambiguity with the exact reason text, half-open validity with complete closed-and-open revision history, schema-valid closed-vocabulary rule traces, content-addressed `atlast:assertion:<digest>` construction through the S4 RFC 8785/SHA-256 primitives, every output validated through the merged shared schemas, deterministic identifier-sorted output, no caller mutation, no clock/fs/network/randomness; and the query-time freshness classifier with deterministic negative-age RangeError (`freshness.ts`). **Verification (2026-08-05):** 129 graph-model tests pass in 9 files (47 new S5 tests: policy literals/deep-freeze/consistency; normalization fixture vectors, locale independence, explicit whitespace, empty/non-ASCII rejection; freshness anchors and negative-age rejection; scenario 1 atomic corroboration; scenario 2 H1/H2 stability; scenario 3 conflict symmetry; scenario 5 H7/H8 ambiguity without dangling references; scenario 6 revision chain, duplicate-source counting, late-old horizon safety, disappearance-closes-nothing; the constructed ADR-0022 invariant 7 withdrawn-support vector; engine-wide schema validation, content-address recomputation, citation-subset and no-future-leakage checks, shuffled-input replay determinism, byte identity, non-mutation, invalid-collection/horizon rejection, deterministic ordering, no `detail` field); graph-model typecheck, repository formatting, lint, `git diff --check`, and the complete unmodified `./scripts/verify.sh` pipeline pass (shared 311, api 1, web 5, graph-model 129; browser acceptance 2/2). Fixtures were read, never modified. _PR #19 merged 2026-08-05 with GitHub Actions `verify` passing; checkpoint closed._ **S5 was limited to the exact accepted ADR-0022 boundary** (§§ 1–14): the `m1-v1` derivation policy; normalization and stable identifier construction; aliases and ambiguity; event-time standing-claim reconciliation; standing-source-filtered provenance; confidence and conflict derivation; assertion validity and complete revision history; schema-valid rule traces; content-addressed GraphAssertion construction; pure reconciliation; freshness classification; and focused tests — **only within the ADR-0022 § 14 authorized paths** (`packages/graph-model/src/**` and TASKS.md for factual progress; no manifest or lockfile change; no new dependency). **S5 must NOT implement:** changes to `packages/shared` or S1/S2 contracts; fixture JSON; repository/storage implementations; snapshot construction, replay, or checksums; query/API routes; frontend; connectors; infrastructure; or any S6–S8 or M2+ work.
- [x] **S6 — Snapshot layer + in-memory stores; contract tests pass** — **complete: explicitly authorized by Joseph Carfagno on 2026-08-05.** The S6 pre-release architecture and authorization review completed first: [ADR-0023](docs/adr/0023-m1-snapshot-and-in-memory-store-semantics.md) — the review packet auditing the eighteen assigned S6-readiness behaviors against the accepted ADRs and merged S1/S2/S4/S5 contracts — was **explicitly accepted by Joseph Carfagno on 2026-08-05 after three independent-review correction passes** (the first: cursor-bound continuation semantics, identity-scoped referential integrity, caller-input protection and output isolation, the exact `m1-v2` seed proof obligation, empty-Evidence-store behavior, machine-readable error `code`/`reason` literals, status accuracy; the second: complete semantic horizon validity with `HORIZON_AFTER_CURRENT_WATERMARK` rejection, the exact `ZodError`-versus-`EvidenceAppendError` boundary with atomicity across both failure kinds, known-but-not-visible assertion revisions as identity-scoped `UnknownIdentifierError`, exact `readonly` error property contracts; the third: graph versus Evidence cursor kinds and `INVALID_CURSOR` versus `CURSOR_BINDING_MISMATCH`), settling all nine identified S6 design gaps and amending ADR-0016, ADR-0018, and ADR-0019 via metadata-only notices (accepted decision text preserved). **ADR-0023 is the binding S6 clarification and remains Accepted, unchanged.** ADR-0023's acceptance did not itself authorize S6; **the separate, explicit S6 authorization decision this entry records was made by Joseph Carfagno on 2026-08-05**, releasing S6 as **the only active authorized implementation slice at the time of authorization** — **effective only after the documentation PR recording this authorization merges to `main` and `main` is synchronized locally with a clean working tree; no S6 implementation file may be created before that.** **Authorization does not approve future S6 implementation output**: that output still requires implementation under accepted ADR-0023, independent review, passing local `./scripts/verify.sh` verification, PR approval with GitHub Actions `verify` passing, and merge. S6 is now implemented, independently reviewed, approved, and merged to `main` through PR #23 at `9bf7f09` on 2026-08-10. **Authorized S6 implementation scope** (per [ADR-0023 § 10](docs/adr/0023-m1-snapshot-and-in-memory-store-semantics.md) and [docs/m1-plan.md § 4](docs/m1-plan.md)): `packages/graph-model/src/**` only — snapshot construction; snapshot identity, payload, and checksum construction; the in-memory `EvidenceStore` and `TopologyGraphStore` implementations; the required injected `Clock` type and derivation-version lookup; graph and Evidence cursor binding and continuation validation; identity-scoped relationship referential-integrity enforcement; the repository error taxonomy from ADR-0023 § 9; registration of the existing S2 repository contract suite using an injected fixture loader; and colocated focused tests — plus TASKS.md solely for factual S6 progress reporting. **S6 must NOT change or implement:** `packages/shared` or any S1/S2 schema, type, interface, or contract; `fixtures/demo-company/**`; any package manifest or lockfile; any new dependency or dependency upgrade; `apps/api` or S7 query/API behavior; `apps/web` or frontend behavior; connectors, authentication, infrastructure, deployment, or real-system access; `m1-v2` or any second derivation policy; `scripts/verify.sh` or `scripts/bootstrap.sh`; or any S7, S8, M2, or later work. S1–S6 are now complete; S7–S8 and M2+ remain gated and unauthorized._ **Progress note (2026-08-10, factual, not a completion claim — historical intermediate checkpoint, superseded by the S6 closeout below):** within the authorized scope, `packages/graph-model/src/**` then contained the S6-A repository/snapshot foundations, the S6-B in-memory `EvidenceStore`, the S6-C1 pure snapshot-construction core, the S6-C2a snapshot resolver, and the S6-C2b in-memory `TopologyGraphStore` (all seven frozen methods — `getSubject`, `getAssertionRevision`, `listEntities`, `searchSubjects`, `traverse`, `getEvidenceChain`, `getSnapshotSummary` — composed on the snapshot resolver, with graph cursor issuance/binding/continuation, query-time freshness, deterministic ordering, bounded traversal, and Evidence-chain construction), plus their colocated tests (349 graph-model tests passing in 18 files, following an independent-review remediation pass on the S6-C2b implementation — graph cursor cross-operation replays now reject as `CURSOR_BINDING_MISMATCH` rather than `INVALID_CURSOR`; cursor bindings are validated before any snapshot resolution for both pinned and cursor-authoritative requests; `traverse` now returns the visible Relationship subject on every followed hop alongside the reached Entity, per ADR-0020; a terminal-position `getEvidenceChain` cursor now rejects as `INVALID_CURSOR` rather than risking a schema-violating empty result; and `getEvidenceChain` cursors are now bound to a distinct Evidence-total-order token rather than the identifier-ascending token). The complete `./scripts/verify.sh` pipeline passed locally at this checkpoint. At this intermediate checkpoint, the S2 storage-agnostic contract-test suite had not yet been registered against these implementations (S6-D followed as the next step), and this checkpoint's work had not yet been independently reviewed, approved, or merged.

**Progress note (2026-08-10, factual, not a completion claim — historical intermediate checkpoint, superseded by the S6 closeout below):** S6-D — registration of the frozen, completely unmodified `registerRepositoryContractSuite` (from `@atlast/shared`) against the committed `InMemoryEvidenceStore`/`InMemoryTopologyGraphStore` pair — was implemented in a new colocated `packages/graph-model/src/contract-suite.test.ts`, which loads the S3 `demo-company` fixture catalog through an injected, deterministic test-only loader (the same `fileURLToPath`/`readFileSync` pattern `reconciliation.test.ts` already uses) and supplies a `RepositoryFactory` that builds a fresh, isolated repository pair per contract case with an explicit fixed injected `Clock`. All 23 frozen contract cases in `repositoryContractCases` executed and passed unmodified against the real S6 implementations, with no case copied, altered, skipped, wrapped, renamed, or selectively registered, and no change to `packages/shared`, fixtures, or any accepted ADR. No implementation defect was exposed; the S6-C2b remediation from the preceding checkpoint already satisfied every behavioral obligation the suite proves. Total graph-model tests: 372 passing in 19 files. The complete `./scripts/verify.sh` pipeline passed locally at this checkpoint. At this intermediate checkpoint, this work had not yet been independently reviewed, approved, or merged.

**Progress note (2026-08-10, factual, not a completion claim):** an independent release-candidate audit of the complete S6 branch delta (`git diff origin/main...HEAD`) against accepted ADR-0023 found one source-hygiene defect, now corrected: `packages/graph-model/src/snapshot-resolver.ts`'s `identityCacheKey` function contained two literal NUL bytes (used as a field separator inside its cache-key template literal) that caused Git to classify the entire file as binary — invisible to `git diff`, PR review, and text search tooling, though functionally inert at runtime (both sides of every cache-key comparison contained the same bytes). The two NUL bytes were replaced with a `|` separator; no other line in the file changed. No behavioral, contract, or test defect was found elsewhere in the audited scope (repository error taxonomy, Clock injection, derivation-version lookup, graph/Evidence cursor binding and continuation, snapshot-checksum payload, semantic horizon validity, identity-scoped referential integrity, `InMemoryEvidenceStore`, snapshot construction, `SnapshotResolver`, `InMemoryTopologyGraphStore`'s seven methods, the S6-D contract-suite registration, and the public `index.ts` export surface). Post-fix verification: graph-model 372/372 tests passing in 19 files; graph-model typecheck clean; `pnpm format:check` and `pnpm lint` clean repository-wide; `git diff --check origin/main...HEAD` clean; the complete `./scripts/verify.sh` pipeline (all 7 stages, including browser acceptance 2/2) passes.

**Checkpoint `m1-s6-snapshot-stores-merged` — S6 closeout.** PR #23, "feat: add M1 snapshot and in-memory repositories," squash-merged to `main` at `9bf7f09` on 2026-08-10 with GitHub Actions `verify` passing in 2m12s. S6 delivered, within its authorized scope: deterministic snapshot construction and identity, checksums, derivation-version lookup, cursor payloads, an injected `Clock`, structured repository errors, the in-memory `EvidenceStore` and `TopologyGraphStore`, bounded historical reads, pagination, traversal, and the reusable S2 repository contract suite passing end-to-end. The final independent audit found and corrected one source-hygiene defect (embedded NUL bytes in `snapshot-resolver.ts`, replaced with a `|` separator; confirmed text-safe) with no behavioral, contract, or test defect elsewhere. Final graph-model verification: 372 tests passing in 19 files; the complete unmodified `./scripts/verify.sh` passed. **S1–S6 are now complete. No implementation slice is currently active.** S7–S8 remain gated and unauthorized, each on its own explicit release recorded here — **closing S6 does not authorize S7.**

- [x] **S7 pre-release architecture and contract review — [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md)** — **review complete: drafted 2026-08-11, following S6's formal closure (PR #24), as a documentation/design audit only — no code, test, schema, fixture, manifest, lockfile, or script change.** The review audited eighteen assigned S7-readiness points against accepted [ADR-0017](docs/adr/0017-m1-query-api-surface.md) (as amended by [ADR-0020](docs/adr/0020-m1-inventory-and-search-semantics.md)) and the merged S2/S6 contracts, found seven already fully specified, and drafted [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md) — at this first-draft stage, eleven genuine, implementation-critical HTTP-boundary gaps were identified (later grown to fifteen across three independent correction passes recorded below; **ADR-0024 was subsequently accepted 2026-08-11 — see the closing progress note below**): the exact route inventory (including three named absences — no relationship-detail route, no relationship-scoped evidence-chain route, no public assertion-revision route — and `EvidenceStore.listEvidence` having no HTTP surface at all); HTTP query-string-to-`ReadMode` coercion and partial-pin rejection; exact query-parameter wire names and coercion rules; percent-encoded multi-segment path-parameter identifiers; the Evidence-lookup response envelope; the complete repository-error-to-HTTP-status mapping (including two previously unmapped cases — cursor errors and `ReferentialIntegrityError`) and a structured error-response schema; cursor-transport policy at the HTTP boundary; the Zod-to-Fastify validation mechanism (no new dependency); composition-root wiring (store lifetime, fixture loading, Clock supply, one new authorized `apps/api` → `@atlast/graph-model` dependency); and the exact `packages/shared`/`apps/api` path boundary S7 would be authorized to change. **Neither this review nor ADR-0024's subsequent acceptance itself authorized S7 implementation** — acceptance settled the S7 design only; the separate S7 authorization decision is recorded further below. [docs/adr/README.md](docs/adr/README.md) indexes ADR-0024 as Accepted. Two product-surface limitations (no relationship-scoped evidence-chain route; no relationship-detail route) are flagged in ADR-0024 for human attention, not resolved by this review._

**Progress note (2026-08-11, factual, not an acceptance):** an independent correction pass on Proposed [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md) found the first draft had deferred, rather than settled, several implementation-critical specifics; ADR-0024 was revised in place (still **Status: Proposed**) to close them, growing the resolved-gap count from eleven to fifteen: an exact per-route path/query-parameter matrix; the exact snapshot-summary HTTP envelope (resolving the mismatch between ADR-0017's general envelope and the repository's flat `SnapshotSummary`, via a new additive `packages/shared` schema); a single closed, discriminated error-response union with an exact `details` shape and HTTP status per code — replacing the open-ended `code: z.string()`/`details: z.unknown()` — including previously unmapped Fastify route-not-found and unexpected-exception (`INTERNAL_ERROR`) cases and an explicit redaction policy (`ReferentialIntegrityError` details exposed; `INTERNAL_ERROR` details always redacted); a corrected, factually accurate statement of Fastify's repeated-query-key behavior (withdrawing an incorrect claim that had named `qs` as the parser); an explicit asynchronous application-initialization lifecycle (a new `initializeApplication` factory) guaranteeing no request can be served before fixture ingestion through the async `EvidenceStore` contract completes, with startup-failure and deterministic test-injection behavior fully specified and the existing `app.test.ts` health test left unmodified; a corrected direct-dependency decision (`apps/api` depends directly on both `@atlast/graph-model` and `@atlast/shared`, not one transitively through the other); and, following an audit of the actual current `packages/shared`/`packages/graph-model`/`apps/api` manifests and tsconfigs (neither shared package currently produces build output or declares a `main`/`exports` entry point), a confirmed, production-valid package-entry-point build strategy requiring no new third-party dependency — enumerated precisely in ADR-0024's revised Exact S7 Boundary, which now also narrowly authorizes specific build-plumbing files in `packages/shared` and `packages/graph-model` (not their existing `src/**` behavior). The review additionally found that accepted [ADR-0020](docs/adr/0020-m1-inventory-and-search-semantics.md)'s phrase "entity detail and traversal" is factually imprecise and would require its own metadata-only amendment (in addition to ADR-0017's) upon any future acceptance of ADR-0024. **No accepted ADR was edited by this pass.** `docs/adr/README.md`'s ADR-0024 index entry was updated to match. **ADR-0024 remains Proposed, not Accepted; S7 remains gated and unauthorized.**

**Progress note (2026-08-11, factual, not an acceptance):** a third independent correction pass on Proposed [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md), made after reading the actual current `tsconfig.json`/`tsconfig.build.json` files and the S6 cursor implementation (`packages/graph-model/src/cursor-payload.ts`, `topology-graph-store.ts`) directly, found the second draft had asserted rather than proven its build strategy, had proposed a health-only application mode that conflicts with ADR-0009, had asserted an unenforceable cursor-mode distinction, and had left three error-schema `details` shapes looser than the repository errors they mirror. ADR-0024 was revised again (still **Status: Proposed**; the resolved-gap count stays at fifteen — this pass corrects four existing gaps rather than adding new ones) to: (1) complete the `packages/shared`/`packages/graph-model` build configuration with `declaration: true` output and an explicit empty `paths` override in each package's `tsconfig.build.json` during emit builds — an inherited source-alias `paths` mapping left active during a build can pull a dependency's source outside the consuming package's `rootDir`, corrupting the package boundary — extend the identical override to `apps/api/tsconfig.build.json`, and require an explicit clean-build-then-run proof (deleting each package's `dist/`, then `pnpm build`, then `node apps/api/dist/server.js`) as part of S7's own verification evidence, rather than declaring the strategy correct "by construction"; (2) remove the proposed health-only `buildApplication` mode — `buildApplication` now always requires its full `EvidenceStore`/`TopologyGraphStore` dependency pair and always registers `/health` plus all seven v1 routes, with a new `initializeApplication` async factory seeding the concrete stores before constructing it; the existing `app.test.ts` is explicitly scoped as an S7 change (no longer claimed to remain byte-for-byte unchanged), rewritten to exercise the one, fully-assembled application ADR-0009 requires, while tests may still call `buildApplication` directly with deterministic stubs — including stubs that deliberately violate a response schema — to exercise error paths the real in-memory stores cannot be coerced into; (3) correct the cursor-transport policy: the S6 graph-cursor payload binds only the resolved snapshot identity, never whether its originating request was itself `latest` or `pinned`, so the withdrawn claim that "a latest continuation supplying any pin component" is a distinct, cursor-specific rejection case is removed and replaced with the two behaviors the repository actually implements (a `latest` continuation always resolves the cursor-bound identity; a `pinned` continuation must exactly match it), plus an explicit statement that transport-mode history is not cursor-bound and is not inferred; and (4) tighten `errorResponseSchema`'s `details` shapes to reuse the repository's own existing bounded schemas — the closed `CursorMismatchField` enum instead of `z.string()`, the existing `recordedSequenceSchema` for horizon/watermark fields, `derivationVersionSchema` for the unsupported-version token, and exact per-identifier-kind identifier schemas for `UNKNOWN_IDENTIFIER` — and to keep `CURSOR_BINDING_MISMATCH`'s graph and evidence variants as the two exact, disjoint shapes `InvalidReadCoordinateErrorParams` already defines rather than one permissive shape with every field optional, while narrowing the `MALFORMED_REQUEST` claim to failures Fastify's own `setErrorHandler` actually governs, not raw Node-level HTTP parser failures occurring before Fastify receives a request. The seven-route surface (§ 1) and the direct-dependency decision (§ 13) are unchanged by this pass. **No accepted ADR was edited by this pass.** `docs/adr/README.md`'s ADR-0024 index entry was updated to match. **ADR-0024 remains Proposed, not Accepted; S7 remains gated and unauthorized.**

**Progress note (2026-08-11, factual, not an acceptance):** a fourth, narrowly scoped correction on Proposed [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md) § 14 found that its proposed `packages/shared/tsconfig.build.json` and `packages/graph-model/tsconfig.build.json` still fail to build: both packages' existing `tsconfig.json` sets `"allowImportingTsExtensions": true` alongside relative `.ts`-extensioned imports, satisfied today only because that config is `noEmit: true`; a build config setting `noEmit: false` without also setting `rewriteRelativeImportExtensions` (or `emitDeclarationOnly`) satisfies none of `allowImportingTsExtensions`'s three permitted conditions, and `tsc` rejects it outright with TS5096. ADR-0024 § 14 was revised again (still **Status: Proposed**; the resolved-gap count stays at fifteen — this pass narrows § 14 further, no new gap) to add `"rewriteRelativeImportExtensions": true` to both packages' `tsconfig.build.json` files only, never to either package's existing no-emit `tsconfig.json` — the setting simultaneously satisfies `allowImportingTsExtensions`'s constraint during emit and rewrites each package's relative `.ts` import specifiers to `.js` in the emitted output. `apps/api/tsconfig.build.json` needs no equivalent addition, since `apps/api/tsconfig.json` already sets `rewriteRelativeImportExtensions: true` directly and never uses `allowImportingTsExtensions`. The Exact S7 Boundary, the build-strategy narrative, the Alternatives Considered, and the testable invariants for § 14 were updated to match; the seven-route surface (§ 1) is unchanged. **No accepted ADR was edited by this pass.** `docs/adr/README.md`'s ADR-0024 index entry was updated to match. **ADR-0024 remains Proposed, not Accepted; S7 remains gated and unauthorized.**

**Progress note (2026-08-11, factual — acceptance):** **Joseph Carfagno explicitly accepted [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md) on 2026-08-11**, after the complete S7 pre-release architecture and contract review and the three independent correction passes recorded above. ADR-0024's Status changed from Proposed to **Accepted**; its own approval note records the acceptance and its decision content (§§ 1–15) is otherwise unchanged from the fourth-pass revision. **With acceptance, ADR-0024 amends [ADR-0017](docs/adr/0017-m1-query-api-surface.md) (§§ 1–5, 9–13: exact route inventory, per-route parameters, request-coercion, error-mapping, and composition-root specifics) and [ADR-0020](docs/adr/0020-m1-inventory-and-search-semantics.md) (correcting its "entity detail and traversal" phrase to "search and traversal" for Relationship-subject visibility) via metadata-only notices**, recorded in both amended ADRs; every amended ADR's accepted decision text is preserved verbatim. `docs/adr/README.md` now lists ADR-0024 as Accepted (with the pre-acceptance review/correction history preserved as a historical record) and ADR-0017/0020 as amended by ADR-0024. **This acceptance settled the S7 architecture and runtime contract only — it did not itself authorize M1 Slice S7 implementation.** CLAUDE.md, HANDOFF.md, README.md, docs/m1-plan.md, and docs/milestones.md were updated to record ADRs 0014–0024 as Accepted and the S7 design-acceptance/implementation-gated distinction; no S8 or M2+ scope was touched. **The separate, explicit S7 authorization decision is recorded immediately below, the same day.**

- [x] **S7 — Query API v1 routes + integration tests** — **explicitly authorized by Joseph Carfagno on 2026-08-11.** The S7 pre-release architecture and contract review completed first and is recorded above; [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md) — the review packet settling fifteen genuine, implementation-critical HTTP-boundary and build-boundary gaps — was **explicitly accepted by Joseph Carfagno on 2026-08-11**, amending ADR-0017 and ADR-0020 via metadata-only notices (accepted decision text preserved). **ADR-0024 is the binding S7 runtime contract and remains Accepted.** ADR-0024's acceptance did not itself authorize S7; **the separate, explicit S7 authorization decision this entry records was made by Joseph Carfagno on 2026-08-11**, releasing S7 as the **only active authorized implementation slice** — **effective only after the documentation PR recording this authorization merges to `main` and `main` is synchronized locally with a clean working tree; no S7 implementation file may be created before that.** **Authorization did not itself approve future S7 implementation output**: that output required implementation under accepted ADR-0024, independent review, passing local `./scripts/verify.sh` verification, PR review, successful GitHub Actions `verify`, and merge — all now satisfied. **S7 is complete: implemented, independently reviewed, verified, and merged to `main` through PR #28 at `a7624cd` on 2026-08-11 with GitHub Actions `verify` passing in 2m14s** — see the closeout progress note below for exactly what was delivered and its final verification evidence. **Authorized S7 implementation scope** (per [ADR-0024's Exact S7 Boundary](docs/adr/0024-m1-query-api-runtime-contract.md#exact-s7-boundary-restated-for-this-adrs-scope)):
  - `apps/api/src/**` — `GET /health` plus exactly the seven accepted query routes; request/query coercion; the closed error mapping; response validation before every successful send; the asynchronous composition root; colocated `fastify.inject()` integration tests.
  - `apps/api/tsconfig.json` — only the two workspace source-alias `paths` entries (typecheck/test only).
  - `apps/api/tsconfig.build.json` — only the build-time empty `"paths": {}` override.
  - `apps/api/vitest.config.ts` (new) — only the two matching `resolve.alias` entries.
  - `apps/api/package.json` — direct workspace dependencies on `@atlast/shared` and `@atlast/graph-model`.
  - `pnpm-lock.yaml` — only the deterministic importer-block changes those two workspace dependency declarations cause.
  - `packages/shared/src/**` — only the additive HTTP schemas and query-coercion helpers ADR-0024 names; no existing S1/S2 schema, type, or interface may change.
  - `packages/shared/tsconfig.build.json` (new) and `package.json` — only ADR-0024's build plumbing.
  - `packages/graph-model/tsconfig.build.json` (new) and `package.json` — only ADR-0024's identical build plumbing; existing `packages/graph-model` source runtime behavior remains frozen, and its existing `tsconfig.json`/`vitest.config.ts` are not touched.
  - `TASKS.md` — solely for factual S7 progress reporting.

  **S7 must NOT change or implement:** a relationship-detail route; a relationship-scoped evidence-chain route; a bulk Evidence-listing route; an assertion route; any existing S1/S2 domain or repository schema, type, or interface; any change to `packages/graph-model`'s or `packages/shared`'s existing `src/**` runtime behavior beyond the additive schemas named above; the S6 cursor payload shape or its binding semantics; `fixtures/demo-company/**`; `apps/web` or any frontend behavior (**no frontend work is authorized; `apps/web` remains untouched**); authentication, connectors, deployment, infrastructure, or real-system access; any new third-party dependency or version upgrade; `scripts/verify.sh` or `scripts/bootstrap.sh`; `pnpm-workspace.yaml`; or any S8, M2, or later work. S1–S6 remain complete; S8 and M2+ remain gated and unauthorized._

**Progress note (2026-08-11, factual, not an acceptance or authorization — S7 blocked):** an actual M1 Slice S7-B implementation attempt — which had produced uncommitted HTTP support modules (request/response coercion, the closed error mapping, response-validation helpers), all seven route modules, the `app.ts`/`server.ts` composition-root rewrites, and the rewritten health test, with **comprehensive integration tests not yet added** — reproduced a genuine, implementation-critical build-configuration contradiction between accepted ADR-0011 (`apps/api`'s `erasableSyntaxOnly: true`) and accepted ADR-0024 § 14 step 5 (the `@atlast/graph-model` typecheck/test source alias): once `apps/api`'s composition root actually imports `InMemoryEvidenceStore`/`InMemoryTopologyGraphStore`/`Clock`/`assertValidClockReading` from `@atlast/graph-model` — exactly as ADR-0024 § 12's literal `initializeApplication` code requires — TypeScript checks that package's entire source under `apps/api`'s own stricter compiler options, and `packages/graph-model/src/identity-normalization.ts`'s `IdentityNormalizationError` constructor's two parameter properties fail with `TS1294`. Neither ADR-0024's three independent correction passes nor Slice S7-A's own clean-build proof caught this, because no code exercised the alias through an actual import until this attempt. The finding was independently reproduced and its scope exhaustively confirmed: exactly one non-erasable-syntax site exists in the complete `apps/api` source-alias closure across both `packages/graph-model` and `packages/shared` — no enum, `namespace`/`module` runtime code, or require-style syntax exists anywhere in either package's non-test source. A documentation-only implementation-clarification ADR, **[ADR-0025](docs/adr/0025-s7-source-alias-erasable-syntax-compatibility.md) (Status: Proposed, not Accepted)**, was drafted proposing exactly one narrow, behavior-preserving refactor — replacing `IdentityNormalizationError`'s two constructor parameter properties with `declare readonly` fields plus explicit constructor assignment after `super()`, in the exact order the original parameter-property emit already produces (`super()`, then the two field assignments, then `this.name`) — with no change to `apps/api`'s compiler options, ADR-0011, or ADR-0024's source-alias/build strategy. **While Proposed, ADR-0025 authorizes nothing.** If Joseph Carfagno explicitly accepts ADR-0025, that acceptance itself authorizes exactly the named `identity-normalization.ts` refactor and this factual-update convention — no second, separate implementation release, because Slice S7 is already explicitly authorized and active and this ADR only widens S7's own exact boundary for this one prerequisite fix; acceptance would not approve any S7-A/S7-B implementation output and would not authorize S8 or any M2+ work. Current state, recorded honestly: **Slice S7-A** (package build plumbing and additive shared HTTP schemas) was **independently reviewed with no blocking findings** and is **locally checkpoint-committed at `747a32a`** on branch `feat/m1-s7-query-api` in the primary implementation working tree — **not pushed, not PR-approved, and not merged**; **Slice S7-B** is **uncommitted**, paused at this reproduced blocker; **no S7-A or S7-B implementation output is pushed, PR-approved, or merged**. **Slice S7 remains the only active authorized implementation slice and remains blocked** pending independent human review of ADR-0025 — its acceptance, per the above, would itself be the authorization to implement the named fix. **S8 remains gated and unauthorized**, unaffected by this note.

**Progress note (2026-08-11, factual — acceptance):** **Joseph Carfagno explicitly accepted [ADR-0025](docs/adr/0025-s7-source-alias-erasable-syntax-compatibility.md) on 2026-08-11**, after independent review and two correction passes. ADR-0025's Status changed from Proposed to **Accepted**; its own approval note records the acceptance and its decision content is otherwise unchanged. **Acceptance authorizes exactly the one named `packages/graph-model/src/identity-normalization.ts` compatibility refactor (parameter properties replaced with `declare readonly` fields plus explicit constructor assignment, in the exact order the original emit already produces) and a factual `TASKS.md` progress update accompanying it** — no second, separate implementation release, because Slice S7 is already explicitly authorized and active and this ADR only widens S7's own exact boundary for this one prerequisite fix. **With acceptance, ADR-0025 amends [ADR-0022](docs/adr/0022-m1-reconciliation-policy-and-assertion-derivation.md) and [ADR-0024](docs/adr/0024-m1-query-api-runtime-contract.md) via metadata-only notices**, recorded in both amended ADRs; their accepted decision text is preserved verbatim. `docs/adr/README.md` now lists ADR-0025 as Accepted in the M1 ADR table and records ADR-0022/0024 as amended by ADR-0025. **This acceptance does not approve any M1 Slice S7-A or S7-B implementation output, and does not authorize Slice S8 or any M2+ work.** **The authorized refactor is not yet implemented.** Consistent with this project's standing checkpoint discipline, **this acceptance becomes operational, and the refactor may begin, only after the PR recording this acceptance merges to `main` and the primary S7 implementation branch (`feat/m1-s7-query-api`) is rebased or otherwise synchronized onto that merged `main` checkpoint.** Until then, **Slice S7 remains the only active authorized implementation slice, and remains temporarily blocked** on the TS1294 contradiction ADR-0025 documents. CLAUDE.md, HANDOFF.md, README.md, docs/m1-plan.md, and docs/milestones.md were updated to record this acceptance and the current blocked state; no S8 or M2+ scope was touched.

**Progress note (2026-08-11, factual, not a completion claim — S7 remains in progress):** **S7-A — package build plumbing and additive shared HTTP schemas** delivered, within the authorized boundary above. `packages/shared/src/http-contract.ts` (new) adds exactly ADR-0024 §§ 6/7/9's schemas — `snapshotSummaryDataSchema`/`snapshotDetailResultSchema` (route 7's narrowed envelope, deriving `checksum`/`subjectCount` from `snapshotSummarySchema.shape`), `evidenceDetailResultSchema` (route 5's envelope, no `resolvedIdentity`), `cursorMismatchFieldSchema`, `unknownIdentifierDetailsSchema`, `invalidReadCoordinateDetailsSchema` (the exact `z.union` two-variant `CURSOR_BINDING_MISMATCH` split), and the closed `errorResponseSchema` — every mirrored field reusing the existing bounded schema it mirrors (`recordedSequenceSchema`, `derivationVersionSchema`, the identifier schemas), exported additively via `index.ts` with no existing S1/S2 export changed. No query-coercion helper schema was added for ADR-0024 §§ 3–5: § 3's only shared-schema need (`snapshotIdentitySchema`) already exists, and §§ 4–5's coercion rules are inherently per-route (§ 2's parameter matrix), which is `apps/api` route-registration work reserved for S7-B, not a separable shared-schema concern — noted here rather than invented. 42 new focused colocated tests (`http-contract.test.ts`) prove each schema's exact shape, including envelope field narrowing, the closed `code`/`identifierKind`/`reason` vocabularies, the two disjoint `CURSOR_BINDING_MISMATCH` variants never merging, and `INTERNAL_ERROR`'s unconditional `details: {}`. Build plumbing implemented exactly per ADR-0024 § 14: `packages/shared/tsconfig.build.json` and `packages/graph-model/tsconfig.build.json` (new, the latter with `"paths": {}` and both with `"rewriteRelativeImportExtensions": true`); both packages' `package.json` gained a `build` script and `main`/`types`/`exports`; `apps/api/tsconfig.json` gained the two source-alias `paths` entries; `apps/api/tsconfig.build.json` gained the `"paths": {}` override; `apps/api/vitest.config.ts` (new) mirrors `packages/graph-model`'s alias convention; `apps/api/package.json` gained direct `workspace:*` dependencies on `@atlast/graph-model` and `@atlast/shared`, with the resulting six-line deterministic `pnpm-lock.yaml` importer-block change only. **One narrow, necessary addition beyond ADR-0024's literal file list, flagged here per house practice:** `apps/api/tsconfig.json`'s `include` array also gained `"vitest.config.ts"` (mirroring `packages/graph-model/tsconfig.json`'s existing identical entry) — without it, ESLint's `projectService` cannot parse the newly authorized `apps/api/vitest.config.ts` and `pnpm lint` fails; this extends an existing repository-wide convention already used for `packages/graph-model`, and touches only the include list, not `paths` or any authorized-boundary content. **Verification:** 42/42 focused tests pass; full shared suite 353/353 (13 files); full graph-model suite 372/372 (19 files); `apps/api` suite 1/1 (unchanged `/health` test — S7-B rewrites it); clean-build proof executed (`rm -rf` all three `dist/`, then `pnpm build`) with no TS5096 or other error, producing `packages/shared/dist/index.{js,d.ts}`, `packages/graph-model/dist/index.{js,d.ts}`, and `apps/api/dist/server.js`, every emitted relative import in `packages/shared/dist/**`/`packages/graph-model/dist/**` ending in `.js`; `node apps/api/dist/server.js` started and served `GET /health` at `200` before being stopped; `pnpm typecheck`/`pnpm test` pass identically whether `dist/` exists or not, proving the source-alias path (never build output) is what satisfies them; the complete unmodified `./scripts/verify.sh` (all 7 stages, including browser acceptance 2/2) passes. **No Fastify route, application-initialization function, fixture loading, repository wiring, or route integration test was added — that is S7-B**, not yet started. Nothing was committed, pushed, or merged by this work.

**Progress note (2026-08-11, factual, not a completion claim — S7 remains in progress):** ADR-0025's acceptance record merged to `main` through [PR #27](https://github.com/Jayc92/atlast/pull/27) at `2480aa3`, and `feat/m1-s7-query-api` was rebased and synchronized onto that checkpoint. The exact authorized syntax-only refactor was then implemented: `packages/graph-model/src/identity-normalization.ts`'s `IdentityNormalizationError` constructor's two parameter properties were replaced with `declare readonly evidenceIdentifier: string`/`declare readonly failingKey: string` field declarations plus explicit constructor assignment, in the exact order the original emit already produced (`super()`, then the two field assignments, then `this.name`) — no other line in the file, and no other file besides this factual update, was touched. `identity-normalization.test.ts` and every pre-existing S7-B file were confirmed byte-identical (checksum comparison) before and after the edit. **The TS1294 blocker is resolved**: `apps/api` typecheck passes with both `packages/shared`/`packages/graph-model` `dist/` absent and both source aliases active; the 20 focused `identity-normalization.test.ts` cases and the complete graph-model suite (372/372, unchanged count) pass; a clean build (`rm -rf` all three `dist/`, then `pnpm build`) succeeds with every emitted relative import ending in `.js`; the compiled `apps/api/dist/server.js` started and served `GET /health` at `200` before being stopped. Because the uncommitted, still-unformatted S7-B files (`apps/api/src/app.test.ts`, `apps/api/src/app.ts`, `apps/api/src/server.ts`, `apps/api/src/http/**`, `apps/api/src/routes/**`, `packages/shared/src/index.ts`, `packages/shared/src/http-query-coercion.{ts,test.ts}`) were out of this refactor's authorized edit scope and were not to be formatted or otherwise edited, they were temporarily isolated via a second stash (`git stash push -u`, message "temp: isolate ADR-0025 verification") before verification and restored immediately afterward — never formatted, never edited. With S7-B safely isolated, **the complete, unmodified `./scripts/verify.sh` passed all seven stages**: whitespace, formatting, lint, types, tests (shared 353/353 in 13 files, graph-model 372/372 in 19 files, web 5/5, api 1/1), builds, and browser acceptance (2/2). The temporary stash was then applied conflict-free, every restored S7-B file's checksum was re-verified identical to its pre-isolation baseline, and the temporary stash was dropped — the original protected `wip: S7-B before ADR-0025 synchronization` backup remains intact as `stash@{0}`. **S7 remains in progress; neither S7-A nor S7-B implementation output is approved or merged.** **S8 and M2+ remain gated and unauthorized.**

**Progress note (2026-08-11, factual, not a completion claim — S7 remains in progress, not yet reviewed, approved, or merged):** **S7-B — request/query coercion, the closed error mapping, response validation, all seven query routes, and the asynchronous composition root** is now complete and comprehensively tested, within the authorized boundary above, continuing directly from the uncommitted S7-B state ADR-0025's synchronization note left off. An initial audit of that uncommitted state against accepted ADR-0024 found the request/query coercion (`apps/api/src/http/query-coercion.ts`), the closed error mapping (`apps/api/src/http/errors.ts`), response validation (`apps/api/src/http/respond.ts`), all seven route modules (`apps/api/src/routes/*.ts`), and the `buildApplication`/`initializeApplication` composition root (`apps/api/src/app.ts`) and `server.ts` startup already matched ADR-0024 §§ 1–13 exactly on inspection and end-to-end manual verification (real `demo-company` fixture reads over every route, both `node apps/api/src/server.ts` directly and the compiled `apps/api/dist/server.js`) — no route, parameter, error code, or lifecycle behavior required correction. **One genuine gap was found only by exercising the real Fastify pipeline, not by design review**, consistent with this project's established pattern: an unencoded stray `%` in a path segment (`FST_ERR_BAD_URL`) is rejected by Fastify's router **before** `setErrorHandler` is ever consulted — confirmed by direct reproduction (a diagnostic `setErrorHandler` never fired) and by reading Fastify 5.10.0's own `fastify.js` (`onBadUrl`, gated behind the dedicated `frameworkErrors` constructor option, not `setErrorHandler`). ADR-0024 § 9 names exactly this case ("invalid route-parameter decoding") under `MALFORMED_REQUEST` without naming the mechanism that actually receives it. Fixed within the already-authorized `apps/api/src/**` boundary: a new `mapFrameworkError` export in `errors.ts` builds the identical `MALFORMED_REQUEST` body through the existing `sendValidatedError` helper, wired via `fastify({ ...serverOptions, frameworkErrors: mapFrameworkError })` in `app.ts`'s `buildApplication` — no change to `errorResponseSchema`, the route surface, or any other ADR-0024 decision; `MALFORMED_REQUEST`'s code/status/`details: {}` shape is unchanged, only the Fastify hook that reaches it. Formatting was also corrected on the eight previously-unformatted S7-B files the prior checkpoint had deliberately left untouched (`apps/api/src/app.ts`, `apps/api/src/http/errors.ts`, `apps/api/src/http/query-coercion.ts`, `apps/api/src/routes/{entities,search,snapshots,traversal}.ts`, `packages/shared/src/http-query-coercion.test.ts`) — no behavioral change, `pnpm format:check` only.

**Comprehensive colocated `fastify.inject()` integration tests were added** — `apps/api/src/routes/{entities,evidence,search,snapshots,traversal}.test.ts` (new) and `apps/api/src/http/errors.test.ts` (new), plus three test-only support modules under the new `apps/api/src/test-support/` (`demo-company-fixture.ts` — a loader for the real `demo-company` catalog via `initializeApplication`, following the identical `fileURLToPath`/`readFileSync` pattern `server.ts` and `packages/graph-model/src/contract-suite.test.ts` already use; `stub-repositories.ts` — deterministic `EvidenceStore`/`TopologyGraphStore` stub builders for the error paths the real in-memory stores cannot be coerced into, per ADR-0024 § 12's explicit allowance; `parse-response.ts` — a typed `parseJsonBody` helper that parses every response through the exact Zod schema the route promises, so an assertion is checked against real validated types and every test doubles as a response-schema proof, never an `any`-typed `.json()` access). `apps/api/src/app.test.ts`'s existing `/health` test is unchanged from S7-A/B's prior rewrite. Every accepted route is exercised against the real `demo-company` fixture data (11 entities including scenario 3's conflicting `orders` and scenario 5's ambiguous `ledger`/`ledger-api`, and the traversed `calls` relationships), not synthetic stand-ins, except the four error paths (`REFERENTIAL_INTEGRITY`, a generic `INTERNAL_ERROR`, a response-schema violation, and `MALFORMED_REQUEST`) that only a stub or a raw malformed URL can provoke.

**Coverage delivered:** `/health`; every one of the seven accepted routes' success path; latest-then-pinned reproducibility (byte-identical, per ADR-0017 invariant 3) on inventory, entity detail, and the evidence chain; entity-inventory match-by-any-claim filtering with the conflicted `orders` entity appearing under both `entityType=service` and `entityType=database`; identifier search over both subject kinds including a Relationship match and ASCII-only case normalization; bounded traversal mixing reached Entity and traversed Relationship subjects with `minConfidence` wire-to-internal-field mapping; cursor continuation with no duplicate/dropped items (entities, search, and the evidence chain); cursor-binding mismatch naming `identity` (replayed against a different pinned identity) and naming `operation` (replayed cross-route); an undecodable cursor token (`INVALID_CURSOR`); a cursorless `latest` read against an empty store (`EMPTY_EVIDENCE_STORE`); percent-encoded multi-segment Evidence-identifier round-tripping per § 5, and the un-encoded case mis-routing to `ROUTE_NOT_FOUND`; every partial-pin rejection, identically with and without a `cursor` present; unknown/malformed path and query parameters on every route; the route-4/5/7 closed-parameter-set rejections (`limit`/`cursor` on traversal and snapshots, any pin parameter on Evidence lookup); `UNSUPPORTED_DERIVATION_VERSION` via the real store using `catalog.json`'s own seeded `m1-v2` identity; `HORIZON_AFTER_CURRENT_WATERMARK` and `HORIZON_BEFORE_FIRST_EVIDENCE` via the real store using deliberately chosen real Evidence subsets (no stub needed for either); `ROUTE_NOT_FOUND`; the newly-fixed `MALFORMED_REQUEST`; a generic unexpected exception mapped to `INTERNAL_ERROR` with its own message never appearing in the response; a deliberately schema-violating stub repository result mapped to `INTERNAL_ERROR` rather than an invalid `200`; and `REFERENTIAL_INTEGRITY` exposed with its complete typed `details`, never redacted. **Verification:** the new/changed `apps/api` suite is 59 tests passing in 7 files (up from the prior checkpoint's 1); full workspace totals are shared 373/373 (14 files, unchanged since S7-A), graph-model 372/372 (19 files, untouched this slice), web 5/5 (untouched), api 59/59 (7 files), browser acceptance 2/2; `pnpm typecheck` passes repository-wide; `pnpm lint` and `pnpm format:check` are clean repository-wide (the prior checkpoint's eight unformatted S7-B files are now formatted); `git diff --check` against `89b6a08` is clean; a clean-build proof (`rm -rf` all three `dist/`, then `pnpm build`) succeeds with every emitted relative import ending in `.js`; the compiled `apps/api/dist/server.js` and the raw `node apps/api/src/server.ts` dev-loop entrypoint were each started, served `GET /health` and a representative query route (`GET /api/v1/entities/{entityId}`) at `200` with real fixture data, and stopped cleanly on `SIGTERM` (exit `0`); an invalid `ATLAST_API_PORT` was confirmed to route through the unchanged `startServer().catch(...)` fatal path (exit `1`, `atlast-api failed to start: ...`) — verified by direct process invocation rather than an added automated test, because a Vitest-level subprocess test spawning the raw `.ts` entrypoint would depend on `packages/shared`'s/`packages/graph-model`'s built `dist/` for Node's own module resolution (the tsconfig/`vitest.config.ts` source aliases cover typecheck and the Vitest environment only, never a plain `node` child process), directly contradicting ADR-0024 § 14's own testable invariant 12 that `pnpm test` must pass without requiring the clean-build step to have run first; **the complete, unmodified `./scripts/verify.sh` passes all seven stages** (whitespace, formatting, lint, types, tests, builds, browser acceptance 2/2), run from a state with all three packages' `dist/` initially absent.

**Exact changed-file set, confirmed against `git diff --stat 89b6a08` and `git status`, entirely within the authorized boundary above:** modified — `apps/api/src/app.test.ts`, `apps/api/src/app.ts`, `apps/api/src/server.ts`, `packages/shared/src/index.ts` (four additive export lines only, confirmed by diff); new — `apps/api/src/http/{errors,query-coercion,respond}.ts` and `apps/api/src/http/errors.test.ts`; `apps/api/src/routes/{entities,evidence,search,snapshots,traversal}.ts` and their five colocated `.test.ts` files; `apps/api/src/test-support/{demo-company-fixture,parse-response,stub-repositories}.ts`; `packages/shared/src/http-query-coercion.ts` and `.test.ts`. **No change to any package manifest, tsconfig, lockfile, or build-plumbing file this session** — all of that landed already in the prior S7-A/ADR-0025 commits (`f2dd32b`, `89b6a08`); confirmed by `git diff --stat 89b6a08 -- '*.json' '*.yaml'` being empty. **No change to `packages/graph-model/src/**`, `fixtures/demo-company/**`, or `apps/web`** — confirmed empty by `git diff --stat 89b6a08` against each path. **`stash@{0}` (the protected pre-ADR-0025-synchronization backup) was not applied, dropped, or modified** — confirmed a single unchanged entry in `git stash list` throughout.

**Known limitation, recorded honestly:** the real, unmodified `startServer` fatal-startup path and the real fixture-ingestion-then-listen ordering in `server.ts` were verified by direct manual process invocation (documented above) rather than by an added automated regression test, for the structural reason given above (any subprocess-spawning test of the raw source would violate ADR-0024 § 14's own build-independence invariant for `pnpm test`). The `initializeApplication` initialization-ordering and store-isolation invariants themselves are now proven by dedicated automated tests, not merely asserted to hold by construction — see the review-remediation pass immediately below.

**Progress note (2026-08-11, factual, not a completion claim — S7 remains in progress; a focused review-remediation pass on the S7-B output above, not a re-implementation):** an independent review of the S7-B output above found two remaining gaps, both corrected within the already-authorized `apps/api/src/**` boundary, with no change to `errorResponseSchema`, the route surface, `packages/graph-model`, `fixtures/demo-company/**`, or `apps/web`.

**Finding 1 — framework-error misclassification.** `mapFrameworkError` mapped every Fastify `frameworkErrors` callback to `MALFORMED_REQUEST`/400, but Fastify 5.10.0 also routes `FST_ERR_ASYNC_CONSTRAINT` through that same hook (`fastify.js`'s `buildAsyncConstraintCallback`) — an internal Fastify failure, not malformed client input; confirmed by reading `fastify.js` directly: absent `frameworkErrors`, Fastify's own default response for that code is a bare `500`, while `FST_ERR_BAD_URL`/`FST_ERR_MAX_PARAM_LENGTH`'s own defaults are `400`/`414` client-error statuses. `errors.ts`'s `mapFrameworkError` now dispatches on `error.code`: exactly `FST_ERR_BAD_URL` and `FST_ERR_MAX_PARAM_LENGTH` map to the validated `MALFORMED_REQUEST` body at `400`; every other framework-error code — `FST_ERR_ASYNC_CONSTRAINT` included — maps to the fixed, unconditionally redacted `INTERNAL_ERROR` body at `500`, never exposing the framework error's own message, code, or stack. `errors.test.ts` gained two focused tests: a real end-to-end `fastify.inject()` case proving an over-length path parameter (`FST_ERR_MAX_PARAM_LENGTH`, a genuine 200+ character path segment) still maps to `MALFORMED_REQUEST`/400 through the real Fastify pipeline, and a direct unit-level call to `mapFrameworkError` with a `FST_ERR_ASYNC_CONSTRAINT`-shaped error and a minimal capturing `FastifyReply` stub proving `INTERNAL_ERROR`/500 with `details: {}` and no trace of the framework error's own message or code — the narrowest reliable mechanism available, since this application registers no async route-constraint strategy and so cannot provoke that exact code through any real request.

**Finding 2 — initialization-invariant coverage.** `app.test.ts` gained three new automated tests (`describe("initializeApplication")`), each restoring its `vi.spyOn(InMemoryEvidenceStore.prototype, "appendEvidence")` mock in `afterEach`: (1) a deferred-promise gate on `appendEvidence` proves `initializeApplication`'s returned promise stays unsettled — no `FastifyInstance` observable — across a real macrotask tick until the gate resolves, then resolves immediately after and serves `/health`; (2) a rejected `appendEvidence` mock proves `initializeApplication` itself rejects with that exact error and never assigns an application to the caller; (3) two real (unmocked) `initializeApplication` calls, each with one deterministic single-Entity seed Evidence record naming a distinct entity, prove full store isolation and that each seed is visible through a real route (`GET /api/v1/entities`) exclusively in its own application. **Verification:** `apps/api` suite is now 64 tests passing in 7 files (up from 59): `errors.test.ts` 7 (was 5), `app.test.ts` 4 (was 1); full workspace totals unchanged elsewhere — shared 373/373 (14 files), graph-model 372/372 (19 files), web 5/5; `pnpm typecheck` and `pnpm lint` pass repository-wide; `pnpm format:check` is clean; `git diff --check` against `89b6a08` is clean; the complete, unmodified `./scripts/verify.sh` passes all seven stages from a state with all three packages' `dist/` initially absent. **Exact changed-file set for this remediation pass:** `apps/api/src/http/errors.ts` (the `mapFrameworkError` dispatch fix), `apps/api/src/http/errors.test.ts` (two new tests), `apps/api/src/app.test.ts` (three new tests) — no other file touched; confirmed against `git diff --stat 89b6a08` showing only `TASKS.md`, `apps/api/src/app.test.ts`, `apps/api/src/app.ts`, `apps/api/src/server.ts`, and `packages/shared/src/index.ts` as modified (the latter three unchanged since the prior progress note), plus the same previously-reported untracked new files. **`stash@{0}` (the protected pre-ADR-0025-synchronization backup) remains a single unchanged entry throughout.** **S7 remains in progress; this output, including this remediation pass, is not yet independently reviewed, approved, or merged.** **S8 and M2+ remain gated and unauthorized.**

**Checkpoint `m1-s7-query-api-merged` — S7 closeout.** PR #28, "feat: add M1 query API v1," squash-merged to `main` at `a7624cd` on 2026-08-11 with GitHub Actions `verify` passing in 2m14s. S7 delivered, within its authorized ADR-0024/ADR-0025 boundary: the additive shared HTTP contracts and query-coercion helpers plus the package-entry-point build plumbing for `packages/shared`/`packages/graph-model`; the authorized `identity-normalization.ts` erasable-syntax compatibility refactor (ADR-0025); request/query coercion, the closed and unconditionally redacted error-response contract (including the `mapFrameworkError` framework-error dispatch fix distinguishing genuinely malformed client input — `FST_ERR_BAD_URL`/`FST_ERR_MAX_PARAM_LENGTH` — from internal Fastify failures such as `FST_ERR_ASYNC_CONSTRAINT`), mandatory response validation before every successful send, and all seven accepted query routes plus `GET /health`, wired through an asynchronous, fixture-backed `initializeApplication`/`buildApplication` composition root in `apps/api`; and comprehensive colocated `fastify.inject()` integration tests, including the initialization-ordering, store-isolation, and framework-error-redaction proofs added during the review-remediation passes above. Final verification before merge: 64 `apps/api` tests passing in 7 files (unchanged elsewhere: shared 373/373 in 14 files, graph-model 372/372 in 19 files, web 5/5); the complete unmodified `./scripts/verify.sh` passed all seven stages, including browser acceptance 2/2. **S1–S7 are now complete. No implementation slice is currently active.** S8 remains gated and unauthorized, on its own explicit release recorded here — **closing S7 does not authorize S8.**

**This S7 closeout documentation itself subsequently merged to `main` through PR #29 at `9acfefa` on 2026-08-11 with GitHub Actions `verify` passing.** With that merge, conditions 1–4 of the standing checkpoint rule ([HANDOFF.md § 7](HANDOFF.md)) for S7's close were satisfied: the closeout PR was merged, `main` was synchronized with `origin/main` on a clean working tree, verification status was recorded, and HANDOFF.md reflects the merged repository state. **Condition 5 — the next slice receiving explicit human authorization recorded in TASKS.md — was then separately satisfied by Joseph Carfagno's explicit S8 authorization decision, recorded immediately below; PR #29's merge did not itself authorize S8.**

- [x] **S8 — M1 synthetic-boundary re-audit and documentation closeout** — **explicitly authorized by Joseph Carfagno in the conductor conversation on 2026-08-11.** S7's closeout checkpoint completed first and is recorded above (checkpoint `m1-s7-query-api-merged`, merged through [PR #29](https://github.com/Jayc92/atlast/pull/29) at `9acfefa`). **Closing S7 did not authorize S8; this entry records the separate, explicit S8 authorization decision** — releasing S8 as the **only active authorized implementation slice.** That authorization's own documentation PR **merged through [PR #30](https://github.com/Jayc92/atlast/pull/30) at `a4c6a5d` on 2026-08-12**, with `main` synchronized locally and a clean working tree — **the authorization is now effective.** **S8 is now complete: implemented, independently reviewed, verified, and merged to `main` through [PR #31](https://github.com/Jayc92/atlast/pull/31) at `0477cbd` on 2026-08-12 with GitHub Actions `verify` passing in 2m27s** — see the closeout checkpoint immediately below for the post-merge revalidation and final verification evidence. **S1–S8 are now complete. M1 is formally closed.**

  **Authorized S8 implementation scope:**
  1. The M1 synthetic-boundary re-audit, extending [docs/audits/m0-synthetic-boundary-audit.md](docs/audits/m0-synthetic-boundary-audit.md) to cover the M1 fixture data (`fixtures/demo-company/**`) and the complete S1–S7 delta, per that audit's own § 11 re-audit trigger and [docs/m1-plan.md § 9](docs/m1-plan.md#9-boundary-audit-requirements).
  2. Factual M1 documentation and checkpoint closeout — updating this file, [HANDOFF.md](HANDOFF.md), [docs/milestones.md](docs/milestones.md), and [docs/m1-plan.md](docs/m1-plan.md) to record the audit result and the final M1 exit-criteria status.
  3. Browser acceptance additions in `tests/acceptance`, only if the existing M0 shell's displayed status changes as part of that factual closeout. **Prefer no application-shell change if documentation alone can accurately close M1.** If a displayed shell status must change, S8 may update only the minimal existing shell status text (e.g. the heading `tests/acceptance/specs/shell.spec.ts` currently matches, `/M0 — Safe project foundation/`) and its directly corresponding acceptance assertions — nothing broader.

  **S8 must NOT implement:** any topology exploration UI or other M2 behavior; new frontend features, graph visualization, or `apps/web` API consumption; changes to S1–S7 production behavior; changes to domain schemas, repository contracts, fixtures, reconciliation, snapshot/storage behavior, or query API behavior; connectors, authentication, infrastructure, deployment, or real-system access; new dependencies, upgrades, package-manifest changes, or lockfile changes; or changes to `scripts/verify.sh`/`scripts/bootstrap.sh`. S1–S7 remain complete; M2+ remain gated and unauthorized._

**Progress note (2026-08-12, factual, not a completion claim — S8 is a release candidate, not yet reviewed, verified end-to-end, or merged):** within the authorized scope above, on branch `feat/m1-s8-boundary-closeout` (based on merged `main` at `a4c6a5d`, PR #30), the three authorized S8 items were produced: (1) [docs/audits/m0-synthetic-boundary-audit.md § 14](docs/audits/m0-synthetic-boundary-audit.md) — the M1 synthetic-boundary re-audit, covering the complete tracked S1–S7 state plus this candidate's own delta: no new network primitive, no credential or sensitive file, no employer/customer material, no mutation route (the seven v1 routes plus `/health` are all `GET`), no connector/cloud/database/telemetry/LLM/auth SDK, no child-process or shell-execution capability, and exactly one new dependency (`zod`, already justified at S1 per ADR-0005) — with **no findings requiring remediation**; (2) factual documentation closeout in this file, [HANDOFF.md](HANDOFF.md), [docs/milestones.md](docs/milestones.md), and [docs/m1-plan.md](docs/m1-plan.md); (3) a minimal shell-status correction in `apps/web/src/App.tsx` (the milestone route now shows M0 and M1 as **delivered** rather than M1 as gated, and the current-state section now names the delivered fixture-driven model/reconciliation/snapshot/query-API core instead of claiming "no topology exists yet"), with its directly corresponding assertions added in `apps/web/src/App.test.tsx` and `tests/acceptance/specs/shell.spec.ts` — no graph data, search, navigation, routing, visualization, or new `apps/web` API consumption was added; the page still makes only the pre-existing `/api/health` request. **No file outside this authorized scope was touched:** `apps/api`, `packages/shared`, `packages/graph-model`, `fixtures/`, every accepted ADR, every manifest, `pnpm-lock.yaml`, `scripts/verify.sh`, `scripts/bootstrap.sh`, and the CI workflow are all unchanged from `a4c6a5d`. **This candidate is not committed, pushed, reviewed, or merged.** It still requires independent review, the complete local `./scripts/verify.sh` passing, PR approval, passing GitHub Actions `verify`, merge, and the final M1 closeout checkpoint before S8 can be marked complete or M1 called complete.

**Progress note (2026-08-12, factual, not a completion claim — a narrow S8 remediation extension, correcting the candidate above):** Joseph Carfagno explicitly authorized a narrow extension — API integration tests under `apps/api/src/**/*.test.ts` proving every graph fact returned from the complete synthetic fixture catalog has provenance dereferenceable through `GET /api/v1/evidence/{evidenceId}`, no other scope. Delivered: (1) `apps/api/src/routes/evidence.test.ts` gained an exhaustive integration test sweeping 18 read coordinates derived from the complete valid catalog (every distinct `observedAt` paired with the full retained horizon 20, plus the catalog's declared supported snapshot identity), fully paginating `GET /api/v1/entities` and `GET /api/v1/search` at each, collecting every provenance/competing-claim/rule-trace Evidence identifier across 15 distinct visible subjects (both Entity and Relationship kinds) and 21 distinct assertion revisions, and dereferencing all resulting identifiers through `GET /api/v1/evidence/{evidenceId}` — **the union equals, by strict set equality, all 20 valid Evidence records**, with all seven valid scenarios contributing non-vacuously; (2) the prior candidate's exit-criterion-2 evidence (row 137 above) and [docs/audits/m0-synthetic-boundary-audit.md § 14.11](docs/audits/m0-synthetic-boundary-audit.md) row 2 were rewritten to cite this exhaustive proof, with the pre-existing single-entity test and the S2 repository contract suite's traceability case reframed as supplemental, repository-layer evidence, not a substitute. **The remediation pass also corrected three audit-accuracy defects found during independent re-review of the candidate itself**, none of which weaken any finding: (a) § 14.1's absolute "no network requests" claim was replaced with an accurate disclosure that `gh pr view 30` (a project-management precondition check, not an Atlast product/runtime path) was the one non-loopback command run, and that `pnpm install --frozen-lockfile`/the Playwright Chromium download did **not** occur during this audit; (b) § 14.2's incorrect "§ 5 below" cross-reference was corrected to § 14.5, and § 14.8 was rewritten to state truthfully that all 13 `fixtures/demo-company/**` files were read in full, not sampled; (c) **§ 14.5's NUL-byte sweep was rerun with a byte-safe method after its first pass (`grep -P`) proved unsound** — the corrected sweep found `packages/graph-model/src/reconciliation.ts` contains one embedded NUL byte (a `draftKey` separator, the same defect class the S6 review fixed in `snapshot-resolver.ts`), predating this candidate's own delta. **This finding is honestly recorded in § 14.5/§ 14.10 and is NOT fixed by this remediation pass** — `packages/graph-model` is outside this extension's authorized scope — and requires its own separately authorized remediation before the M1 boundary re-audit can be called fully clean. **Verification of this remediation pass:** the new `evidence.test.ts` test passes (`apps/api` 65/65 tests in 7 files); full `./scripts/verify.sh` still passes end-to-end. **This candidate remains uncommitted, unpushed, unreviewed, and unmerged**, and M1 still cannot be called complete.

**Progress note (2026-08-12, factual, not a completion claim — a second, narrow S8 remediation extension, resolving the finding the prior note surfaced):** Joseph Carfagno explicitly authorized a further narrow extension — replacing the single literal NUL byte in `packages/graph-model/src/reconciliation.ts` with the source-text escape `` `\u0000` ``, preserving the exact runtime delimiter and behavior, plus directly corresponding tests and factual S8 audit/checkpoint documentation corrections, no other production behavior or scope. Delivered: (1) `packages/graph-model/src/reconciliation.ts`'s `draftKey` builder now reads `` `${subjectIdentifier}\u0000${claimKey}` `` — a byte-safe scan of the file confirms zero literal `0x00` bytes remain, and a byte-safe scan of the complete 180-file tracked tree confirms zero literal `0x00` bytes anywhere; the source text visibly contains the six ASCII characters `` `\u0000` ``, and extracting that exact expression from the fixed file's own bytes and evaluating it in Node confirms the runtime value is unchanged — `length === 3`, `codePointAt(1) === 0`, and `===` to the equivalent value built with `String.fromCharCode(0)` — a single U+0000 code point between the two segments, not six runtime characters. No test was added or changed in `packages/graph-model`: the existing 372-test suite (unchanged count) already exercises every code path through `draftKey`, and passes unchanged, which is the correct proof of behavior preservation; the byte-safe audit scan is the correct proof of source hygiene, so no source-file introspection was added to a product test. (2) [docs/audits/m0-synthetic-boundary-audit.md § 14.5/§ 14.10](docs/audits/m0-synthetic-boundary-audit.md) were rewritten from "one unresolved, out-of-scope finding" to "one finding, found and resolved within this candidate" — recording the exact fix, the runtime-equivalence proof, and a corrected binary-classification statement (Git's own `git grep -Iz`/`git log --numstat` treated the pre-fix file as text; the external `file` command's `data` result on the same blob was inconclusive and is no longer cited as proof of non-binary content). § 14.11 row 4 and § 14.13 now record the exit criterion as a **clean** release-candidate finding, pending only this candidate's own review, verification, PR, merge, and post-merge SHA revalidation — not "not fully clean." § 14's own candidate-delta inventory (the "Plus:" paragraph) now lists both narrow remediation extensions by name and correctly states `apps/api/src/routes/evidence.test.ts` **is** in the delta (the prior wording incorrectly said no `apps/api` file was). This file's own exit-criterion-4 bullet below is updated to match. **This candidate remains uncommitted, unpushed, unreviewed, and unmerged, and M1 is still not formally complete** — that requires this candidate's own review, verification, PR, merge, and the final M1 closeout checkpoint with post-merge SHA revalidation.

**Checkpoint `m1-complete` — S8 closeout and final M1 closure (2026-08-12).** PR #31, "chore: complete M1 synthetic boundary audit," squash-merged the complete S8 candidate to `main` at `0477cbdb0af40aaa31e03501244780c23313bdd5` on 2026-08-12, with GitHub Actions `verify` passing in 2m27s (run [31616657083](https://github.com/Jayc92/atlast/actions/runs/31616657083)). An independent post-merge revalidation was then performed directly against that real merge commit, per [docs/audits/m0-synthetic-boundary-audit.md § 15](docs/audits/m0-synthetic-boundary-audit.md): a fresh byte-safe NUL sweep of all 180 tracked files found zero literal `0x00` bytes; the merged changed-file scope was independently re-derived and matches exactly the 12 files the pre-merge candidate described (no fixture, ADR, manifest, lockfile, `scripts/verify.sh`, `scripts/bootstrap.sh`, or CI-workflow file changed); the seven-`GET`-route, no-mutation-endpoint finding was reconfirmed against the real commit; and the complete, unmodified `./scripts/verify.sh` passed all seven stages locally at `0477cbd` (shared 373/373 in 14 files, graph-model 372/372 in 19 files, web 6/6, api 65/65 in 7 files; browser acceptance 2/2). **S1–S8 are now complete. No implementation slice is currently active. M1 is formally complete as of 2026-08-12.** **M2 and every later milestone remain gated and unauthorized — this closure does not imply or grant M2 authorization**; M2 requires its own separate, explicit human release, exactly as every prior milestone did.

M1 exit criteria (below) are now all checked and closed as project facts, per [docs/audits/m0-synthetic-boundary-audit.md § 15](docs/audits/m0-synthetic-boundary-audit.md):

- [x] Model and query API run wholly from fixtures in CI with no external dependencies — _closed: [docs/audits/m0-synthetic-boundary-audit.md § 15](docs/audits/m0-synthetic-boundary-audit.md) revalidated against the real merge commit `0477cbd` (the production `apps/api` composition root resolves only `fixtures/demo-company/` through in-memory stores; CI provisions no external service and holds no secret)_
- [x] Every fact in the graph is traceable to its synthetic evidence via the API — _closed: [docs/audits/m0-synthetic-boundary-audit.md § 14.11](docs/audits/m0-synthetic-boundary-audit.md) row 2, reconfirmed in § 15 — proven exhaustively, not on one entity or one coordinate: `apps/api/src/routes/evidence.test.ts`'s new integration test sweeps 18 read coordinates derived from the complete valid fixture catalog (every distinct `observedAt`, full retained horizon, plus the catalog's declared supported snapshot identity), fully paginates `GET /api/v1/entities` and `GET /api/v1/search` at each, collects every provenance/competing-claim/rule-trace Evidence identifier across 15 distinct visible subjects (both Entity and Relationship kinds) and 21 distinct assertion revisions, and dereferences all 20 through `GET /api/v1/evidence/{evidenceId}` — the union equals, by strict set equality, all 20 valid Evidence records in the catalog, with all seven valid scenarios contributing non-vacuously (Journey 1 of [docs/m1-plan.md § 7](docs/m1-plan.md#7-api-journeys-and-acceptance-checks)). The pre-existing single-entity/single-coordinate tests and the S2 repository contract suite's own traceability case remain supplemental, repository-layer evidence, not a substitute for this API-level proof_
- [x] Graph/evidence representation decisions recorded as ADRs and human-approved — _ADRs 0014–0025 Accepted (0014/0015/0016/0017/0018/0019/0020/0022 each amended as recorded in [docs/adr/README.md](docs/adr/README.md))_
- [x] No-real-systems constraint verified: M1 boundary re-audit passes (S8) — _closed: [docs/audits/m0-synthetic-boundary-audit.md § 15](docs/audits/m0-synthetic-boundary-audit.md) independently revalidated the merged S8 state against the real merge commit `0477cbd` and found no synthetic-boundary violation. § 14.5/§ 14.10's one finding — `packages/graph-model/src/reconciliation.ts` had contained one embedded NUL byte (a `draftKey` separator, the same defect class the S6 review fixed in `snapshot-resolver.ts`), predating the S8 delta — was found and resolved within the S8 candidate, under Joseph Carfagno's own separate explicit narrow authorization (the literal byte replaced with the source-text escape `` `\u0000` ``, preserving the exact runtime U+0000 delimiter, verified byte-safe and runtime-equivalent), and § 15 reconfirmed the fix landed intact in the merged commit via a fresh byte-safe sweep (0/180 tracked files contain a NUL byte)._

**M1 exit criteria are all satisfied and closed as project facts. M1 is formally complete as of 2026-08-12 — checkpoint `m1-complete`, recorded in [HANDOFF.md](HANDOFF.md). At that closeout checkpoint, M2 was still gated; M2 was subsequently authorized through the separate decision and PR #33 record below.**

## M2 — Interactive Topology Interface (authorized — planning phase released)

**M2 authorization (2026-08-12):** Joseph Carfagno explicitly authorized M2 after the `m1-complete` checkpoint merged through PR #32 at `cff0545`. The authorization record then merged through [PR #33](https://github.com/Jayc92/atlast/pull/33) at `b547ec2` with GitHub Actions `verify` passing in 2m14s; `main` was synchronized locally with a clean working tree. M2 remained phase-gated while its baseline was independently reviewed.

- [x] **M2-P — implementation plan and pre-release architecture review** — complete. The initial review found one blocking and two non-blocking issues; correction commit `5210622` addressed all findings and residual observations, focused re-review found no remaining blockers, and Joseph explicitly accepted ADRs 0026–0028 and approved [docs/m2-plan.md](docs/m2-plan.md). The record merged through PR #34 at `106b1e7`, CI passed, and `main` was synchronized cleanly.
- [x] **M2-A — browser/API contract and client foundation** — complete. Explicitly authorized by Joseph Carfagno on 2026-08-12 within [the plan's exact M2-A boundary](docs/m2-plan.md#exact-m2-a-authorization-boundary); the authorization record merged through PR #35 at `c3c661a`. The bounded implementation was independently reviewed and remediated, passed the complete local repository verifier, and merged through PR #36 at `fa38812` with GitHub Actions `verify` passing in 2m36s. **At the M2-A merge checkpoint, no implementation slice was active and M2-B+ remained unauthorized; the later M2-B authorization is recorded below.**

  **Historical implementation note (2026-08-12; superseded by checkpoint `m2-a-browser-foundation-merged` below):** within the exact M2-A boundary above, delivered:
  1. **Additive `packages/shared` browser-facing HTTP contract** — `health-contract.ts` (new): `healthCheckResultSchema`/`HealthCheckResult`, validating the exact, unchanged M0 `GET /health` payload (ADR-0004) so the M2-A query client can validate it through the same `@atlast/shared` Zod discipline as every M1 route (ADR-0026 § 3). Exported additively from `index.ts`; no existing S1/S2/S7 schema, type, or export changed.
  2. **The validated query client** (`apps/web/src/api/client.ts`) — one function per accepted route (inventory, entity detail, search, traversal, evidence lookup, entity evidence chain, snapshot summary) plus `/health`, each using `fetch`/`AbortController`, validating success against its exact `@atlast/shared` response schema and failure against the closed `errorResponseSchema` before returning anything, and collapsing every network failure, non-JSON body, or schema mismatch into one redacted `client-internal-failure` (ADR-0026 § 3) — never a raw exception message. Identifier path segments are percent-encoded per-segment (ADR-0024 § 5); pagination cursors are threaded through as opaque strings, never parsed; every pin parameter is typed as the domain `SnapshotIdentity`, so a partial pin cannot be constructed from application code.
  3. **Closed error taxonomy** (`apps/web/src/api/errors.ts`) — the `ClientQueryError`/`ClientQueryResult` discriminated types and portable `isAbortError` (checks `.name`, not `instanceof DOMException`, so it works identically under jsdom, a real browser, and Node's `undici`).
  4. **Bounded in-memory request cache** (`apps/web/src/api/cache.ts`) — a capped, least-recently-set-eviction `Map` wrapper (no caching library, no persistent storage — lifetime ends on page reload) plus a deterministic cache-key builder keyed on operation, complete resolved identity, filters/bounds, and cursor (ADR-0026 § 7).
  5. **Single-flight latest-resolution coordinator** (`apps/web/src/api/session-coordinator.ts`) — one coordinator owns cursorless "latest" resolution: concurrent callers share the same in-flight promise (proven single-flight in `session-coordinator.test.ts`), a resolved identity is reused for the rest of its generation, and `beginNewGeneration()` invalidates any established identity or in-flight resolution so an obsolete generation's eventual response is ignored rather than published (ADR-0026 § 4).
  6. **Complete-pin URL parsing/canonicalization** (`apps/web/src/url/query-state.ts`) — parses the canonical query parameters (`q`, `direction`, `depth`, `minConfidence`, `view`, `selected`, and the `asOf`/`horizon`/`derivationVersion` pin triple); a partial pin, an out-of-range/unrecognized value, or a complete-but-malformed pin (validated through the shared `snapshotIdentitySchema`) is dropped to its safe "absent" meaning rather than half-honored, with `wasCanonicalized` telling the caller whether anything was corrected (Journey F). `serializeTopologyUrlState` round-trips deterministically in one fixed field order.
  7. **Routing foundation** (`apps/web/src/router.tsx`, `apps/web/src/main.tsx`) — React Router wired at the application root via `createBrowserRouter`/`RouterProvider`, with the three canonical M2 routes (`/`, `/topology`, `/entities/:entityId`) real and addressable. `/` renders the existing, unmodified `App` foundation page; `/topology` and `/entities/:entityId` render only a static, inert, clearly labeled "reserved for M2-B" notice — no inventory, search, entity data, or graph viewport of any kind. `App.tsx` itself is untouched.
  8. **Exact Vite proxy correction** (`apps/web/vite.config.ts`) — the prior catch-all `/api` -> "" rewrite (which would have turned `/api/v1/entities` into the nonexistent `/v1/entities`) is replaced with two non-overlapping prefix rules: `/api/health` -> backend `/health`, and `/api/v1` -> backend `/api/v1` unchanged (ADR-0026 § 6).
  9. **ADR-0026 § 5 restricted-import ESLint boundary** (`eslint.config.mjs`) — a `no-restricted-imports` block scoped to `apps/web/src/**` (the built-in rule; no new ESLint plugin dependency) rejecting `@atlast/graph-model`, any deep path into `packages/graph-model`, any path into `fixtures/`, and any path into `apps/api` or `@atlast/api`. `apps/web/src/eslint-boundary.test.ts` directly proves this by spawning the already-installed root ESLint CLI (a subprocess, not a new `eslint` package dependency) against four representative forbidden imports — each rejected with `no-restricted-imports` — and one `@atlast/shared` import, confirmed not rejected.
  10. **Approved dependencies only** — `react-router@8.3.0` (pinned, the latest published stable release at implementation time) and a direct `@atlast/shared` workspace dependency, added to `apps/web/package.json`; `apps/web/tsconfig.json` gained the identical ADR-0024 § 14-style typecheck/test-only source alias for `@atlast/shared` other packages already use, and `apps/web/vite.config.ts` gained the matching `test.alias` (Vitest-only — the production `vite build`/`preview` path is untouched and resolves `@atlast/shared` through its real `dist`/`main`/`types`/`exports`). The resulting `pnpm-lock.yaml` change is exactly the deterministic 28-line importer-block consequence of those two declarations.
  11. **Browser acceptance** (`tests/acceptance/specs/api-proxy.spec.ts`, new) — against the real built web preview and the real built, fixture-backed API server (not a mock or dev-loop stand-in): `GET /api/health` returns the exact deterministic payload through the corrected proxy, and `GET /api/v1/entities` returns the real envelope shape (`items`/`page`/`meta.resolvedIdentity`/`meta.schemaVersion`) with a genuine `application/json` content type — distinguishing it from Vite preview's SPA-fallback `index.html`, which is what the old catch-all rewrite's resulting `/v1/entities` path would otherwise have silently served.

  **Measurements (ADR-0018 storage forcing-point + bundle size, docs/m2-plan.md § 3/§ 11):**
  - **Production browser bundle, before -> after** (`apps/web`, `vite build`, measured via an isolated `git worktree` at `c3c661a` for the "before" baseline, removed after measurement): JS `196.96 kB` (gzip `62.23 kB`) -> `291.38 kB` (gzip `92.68 kB`); CSS unchanged at `5.67 kB` (gzip `1.83 kB`). The **+94.42 kB raw / +30.45 kB gzip** increase is React Router plus `@atlast/shared` (which bundles Zod for runtime response validation) — exactly the growth ADR-0026's own Consequences anticipated ("The browser bundle includes the shared runtime schemas and Zod; bundle size must be measured during M2-A review").
  - **What is measurable now:** the fixture-backed API's real, current state — 20 retained Evidence records (the S3 `demo-company` catalog, unchanged), a current watermark/horizon of `20`, and 11 visible Entities at the latest resolved identity (`GET /api/v1/entities?limit=100`, unfiltered, `hasMore: false`). Loopback round-trip latency against the compiled `apps/api/dist/server.js` (single-request samples, not a load test): `GET /health` ≈ `1.5–1.8 ms`; `GET /api/v1/entities` ≈ `6–7 ms` steady-state (an `18 ms` first call includes one-time reconciliation/snapshot construction). All well within "comfortable local use" — no ADR-0018 change-condition trigger fires from this data.
  - **What is deliberately NOT measurable yet, and is not claimed to be:** M2-A adds the query client, cache, and coordinator, but no M2-B+ visible topology feature exists to generate real interactive load — traversal truncation, snapshot-anchor candidate counts (ADR-0028 is M2-E, unreleased), and sustained multi-panel request patterns have no real usage to measure. The one real round trip this slice's browser acceptance test exercises is a connectivity proof, not an interactive-load sample, and is not represented as one. Per [docs/m2-plan.md § 3](docs/m2-plan.md#adr-0018-storage-forcing-point-re-evaluation), the retain-or-migrate decision itself remains open, pending the M2-A–E measurements this note starts and the independent review required before M2-F closes.

  **Verification:** `pnpm format:check`/`pnpm lint`/`pnpm typecheck` clean repository-wide; `git diff --check` (unstaged and staged) clean; non-browser Vitest totals: shared 384/384 (15 files, +2 for `health-contract.test.ts`), graph-model 372/372 (19 files, unchanged), api 65/65 (7 files, unchanged), web 79/79 (8 files, up from 5 — the new `api/`, `url/`, `router.tsx`, and `eslint-boundary.test.ts` suites, `App.test.tsx` itself unchanged). Independent review remediation closed thrown/rejected latest-resolution failures without leaking details or blocking retries, strengthened canonical URL handling for unknown/repeated/empty parameters, and replaced the routing surrogate with history-delta back/forward plus copied-query preservation coverage. A clean `pnpm build` across all seven buildable packages, including the bundle-size figures above, and the complete, unmodified `./scripts/verify.sh` passed all seven stages locally, including both browser acceptance specs (`shell.spec.ts`, unchanged, and the new `api-proxy.spec.ts`) in both desktop and mobile Chromium projects.

  **Exact changed-file scope, audited against the boundary above:** modified — `apps/web/package.json`, `apps/web/src/main.tsx`, `apps/web/tsconfig.json`, `apps/web/vite.config.ts`, `eslint.config.mjs`, `packages/shared/src/index.ts`, `pnpm-lock.yaml` (the deterministic two-dependency importer-block change only); new — `packages/shared/src/health-contract.ts`/`.test.ts`; `apps/web/src/api/{client,errors,cache,session-coordinator}.ts` and their colocated `.test.ts` files; `apps/web/src/url/query-state.ts`/`.test.ts`; `apps/web/src/router.tsx`/`.test.tsx`; `apps/web/src/eslint-boundary.test.ts`; `tests/acceptance/specs/api-proxy.spec.ts`. **No change to** `apps/api/**`, `packages/graph-model/**`, `fixtures/**`, `apps/web/src/App.tsx` or `App.test.tsx`, any accepted ADR, `scripts/verify.sh`, `scripts/bootstrap.sh`, or `pnpm-workspace.yaml` — confirmed by the merged PR #36 scope. **This candidate implemented only M2-A**: no M2-B topology shell/inventory/search/entity UI, no M2-C graph rendering/layout or `@xyflow/react`/`elkjs`, no M2-D trust inspector, no M2-E snapshot-anchor API/history UI, no M2-F closeout, no `apps/api` production change or new route, and no M3+ work landed.

**Checkpoint `m2-a-browser-foundation-merged` — M2-A closeout (2026-08-12).** PR #36, "feat: add M2 browser client foundation," squash-merged to `main` at `fa38812` after independent review and remediation. The review closed retry handling for thrown/rejected latest-resolution failures, canonicalization of unknown/repeated/empty URL parameters, and genuine history-delta back/forward coverage. Final local verification passed all seven stages: shared 384/384, graph-model 372/372, API 65/65, web 79/79, production builds, and browser acceptance 6/6. GitHub Actions `verify` passed in 2m36s. `main` and `origin/main` were then synchronized at `fa38812` with a clean working tree. **At that checkpoint, M2-A was complete, no implementation slice was active, and closing M2-A did not itself authorize M2-B. Joseph's later explicit M2-B authorization is recorded immediately below.**

- [x] **M2-B — application shell, Entity inventory, identifier search, and canonical UI states** — explicitly authorized by Joseph Carfagno on 2026-08-12 after the M2-A closeout merged through PR #37 at `2bbf13b`; implemented within [the exact M2-B boundary](docs/m2-plan.md#exact-m2-b-authorization-boundary), independently reviewed and remediated, manually approved, and squash-merged through PR #39 at `9dd507b` on 2026-08-12 with GitHub Actions `verify` passing in 3m0s. The delivered surface is restricted to `apps/web/src/**`: visible application shell/routing; query-API-only Entity inventory, identifier search, and entity-focused detail; canonical URL/cache/coordinator foundations; coordinated latest-to-pinned reads; canonical loading/empty/expected-error/redacted-error/retry/invalid-URL states; responsive and keyboard-reachable structured presentation; directly corresponding tests; and factual measurements. **Closing M2-B does not authorize M2-C. M2-C through M2-F and M3+ remain unauthorized.**

**Progress note (2026-08-12, factual, not a completion claim — M2-B is an independently reviewed and locally verified release candidate, not yet committed, pushed, approved, or merged):** on branch `feat/m2-b-topology-shell` (HEAD at the merged authorization commit `aeb35aa`, PR #38), the M2-B application shell was implemented within the exact boundary above, entirely inside `apps/web/src/**`:

1. **Application shell and routing** (`apps/web/src/topology/TopologyShell.tsx`) — a shared masthead/skip-link/main landmark layout for both routes, replacing the M2-A inert `ReservedRouteNotice`/`ReservedEntityRoute` placeholders in `apps/web/src/router.tsx`. The existing `/` foundation page (`App.tsx`) gained exactly one addition: a plain `<a href="/topology">` navigation link into the topology application (a native anchor, not a React Router `Link`, so `App` still renders standalone outside a router exactly as its existing tests already do) — no other change to its content, vision copy, or milestone status table.
2. **Shared identity/session coordination** (`apps/web/src/topology/session.ts`) — a module-singleton `ExplorationSessionCoordinator<SnapshotIdentity>` and bounded request cache (both from the existing M2-A `api/session-coordinator.ts`/`api/cache.ts`, not reimplemented), plus `resolveSnapshotIdentity`: an explicit URL pin resolves with no request; latest mode resolves through the shared coordinator using a minimal real `GET /api/v1/entities?limit=1` probe, so concurrent panels in one exploration generation share exactly one identity-resolving request (ADR-0026 § 4).
3. **A generic query hook** (`apps/web/src/topology/use-async-query.ts`) — the one honest-state/stale-response-suppression/retry/cache mechanism every page reuses: aborts and ignores a superseded request when its query key changes before it resolves, serves a cached value under an unchanged key without a new request, and collapses every outcome into exactly `loading` / `loaded` / `api-error` (the validated `ErrorResponse`) / `internal-error` (redacted).
4. **Pure view projections** (`apps/web/src/topology/entity-projection.ts`) — identifier/entity-type-claim/assertion-count projections for inventory rows, search rows (Entity and Relationship, kept honestly distinct), and entity detail, never collapsing multiple visible entity-type claims into one "winner."
5. **Canonical URL-state wiring** (`apps/web/src/topology/use-topology-url-state.ts`) — built entirely on the existing, unmodified M2-A `url/query-state.ts` parser/serializer: an invalid or partial URL is canonicalized (`replace`d in the address bar) and a sticky, accessible "this link was corrected" notice is shown for the rest of that page's lifetime.
6. **`/topology`** (`TopologyPage.tsx`) — Entity inventory by default; canonical-identifier search (minimum 2 characters, client-validated before any request) when `?q=` is present; entity results link to `/entities/:entityId` preserving `q`/pin, Relationship results render honestly but are not linked (no M2-D detail view exists yet); pagination is a client-side history of opaque cursor tokens (never parsed, never written to the URL), replaying a previously visited cursor from cache rather than re-fetching it.
7. **`/entities/:entityId`** (`EntityDetailPage.tsx`) — structured presentation of the entity's identifier and every visible entity-type claim plus its assertion-revision count; an empty/whitespace identifier canonicalizes to a redirect to `/topology`; a "Back to topology" link preserves `q`/pin.
8. **Test-support fixtures** (`apps/web/src/topology/test-support/{fixtures,stub-fetch}.ts`) — schema-valid response builders and a routable `fetch` stub for the multi-request page tests; not `*.test.ts` files themselves, so Vitest does not collect them as suites.

**Independent review remediation (2026-08-12):** the first independent code and live-browser pass found three blocking behavioral gaps that the original component suite did not expose, all corrected within the authorized `apps/web/src/**` boundary. (1) React Strict Mode's development mount/cleanup/remount cycle aborted the first consumer-owned latest-identity probe, then left the remount joined to that already-aborted single-flight promise and the real application permanently displaying “Resolving…”; the shared probe is now owned and aborted by the exploration session generation rather than an individual mount, with a production-Strict-Mode lifecycle regression test and a successful live fixture-backed `/topology` browser pass. (2) browser history could change `?q=` while the search input retained its previous local draft, leaving the control inconsistent with the URL and rendered results; the form now remounts from canonical URL state whenever `q` changes, with a history-navigation regression test. (3) a schema-valid dependent response whose `meta.resolvedIdentity` differed from the complete requested identity could previously be rendered and cached; `requireResolvedIdentity` now rejects such inventory, search, or detail data as a redacted client-internal failure before publication or caching, with direct helper and page-level regression tests. Follow-up human QA found the original small masthead-adjacent topology link too easy to miss; the same authorized `App.tsx`/`styles.css` surface now presents it as a prominent, responsive hero callout and “Explore topology” action, with its existing route test retargeted to the final accessible name. The review also corrected the stale M2-A-only entry-point comment in `apps/web/src/main.tsx`; no runtime behavior changed from that comment correction.

**Verification:** after the independent remediation, the complete, unmodified `./scripts/verify.sh` passed all seven stages: whitespace, formatting, lint, typecheck, shared **384/384 in 15 files**, graph-model **372/372 in 19 files**, API **65/65 in 7 files**, web **120/120 in 14 files**, clean production builds, and browser acceptance **6/6**. Joseph Carfagno then completed manual browser QA of the visible inventory, entity detail, identifier search, browser Back/Forward synchronization, and direct-refresh behavior; all checks passed. `tests/acceptance/**` remains untouched.

**Bundle-size measurement (ADR-0018/docs/m2-plan.md § 3 obligation), before → after** (`apps/web`, `vite build`; "before" measured via an isolated `git stash` of this candidate's diff at the same `aeb35aa` base, "after" rebuilt following independent remediation and topology-entry discoverability QA): JS `291.38 kB` (gzip `92.68 kB`) → `392.22 kB` (gzip `119.75 kB`), a **+100.84 kB raw / +27.07 kB gzip** increase; CSS `5.67 kB` (gzip `1.83 kB`) → `9.70 kB` (gzip `2.52 kB`), **+4.03 kB raw / +0.69 kB gzip**. No new dependency, package-manifest change, or lockfile change caused any part of this growth — confirmed by `git diff --stat` showing zero touched `package.json`/`pnpm-lock.yaml` files; the entire increase is this slice's own application code (topology shell, pages, hooks, and CSS).

**Merged changed-file scope, confirmed through PR #39 at `9dd507b`:** modified — `apps/web/src/App.tsx`, `apps/web/src/App.test.tsx`, `apps/web/src/main.tsx`, `apps/web/src/router.tsx`, `apps/web/src/router.test.tsx`, `apps/web/src/styles.css`; new — `apps/web/src/topology/{TopologyShell.tsx, TopologyPage.tsx, TopologyPage.test.tsx, EntityDetailPage.tsx, EntityDetailPage.test.tsx, session.ts, session.test.ts, use-async-query.ts, use-async-query.test.tsx, use-topology-url-state.ts, entity-projection.ts, entity-projection.test.ts, QueryStatus.tsx, QueryStatus.test.tsx, test-support/fixtures.ts, test-support/stub-fetch.ts}`. **No other path changed**: no `package.json`, `pnpm-lock.yaml`, `apps/api/**`, `packages/shared/**`, `packages/graph-model/**`, `fixtures/**`, accepted ADR, `scripts/verify.sh`, `scripts/bootstrap.sh`, CI workflow, or `tests/acceptance/**` file was touched. **M2-B implemented only its exact boundary**: no traversal, Relationship graph projection, graph rendering/layout, `@xyflow/react`/`elkjs`, M2-D trust inspector/Evidence dereferencing/conflict-or-ambiguity presentation, M2-E snapshot-anchor API/history playback, M2-F closeout work, connectors, authentication, deployment, or real-system access landed. The release candidate passed independent review, in-scope remediation, the complete local verifier, human browser QA, PR approval, and GitHub Actions `verify`, then merged through PR #39 at `9dd507b`. Checkpoint `m2-b-topology-shell-merged` is recorded in [HANDOFF.md](HANDOFF.md). **No implementation slice is active.**

- [x] **M2-C — traversal workspace, graph adapter/layout, structured equivalent, and selection synchronization** — explicitly authorized by Joseph Carfagno on 2026-08-12, independently reviewed and remediated, manually QA-tested, verified locally, and squash-merged through PR #42 at `a43b0c5` on 2026-08-13 with GitHub Actions `verify` passing in 3m22s. Delivered the query-API-only bounded traversal workspace, deterministic pure projection and ELK layout, lazy React Flow viewport, equivalent keyboard-operable structured view, canonical latest/pinned mode and selection synchronization, responsive/reduced-motion behavior, exact pinned graph dependencies, direct component tests, and desktop/mobile built-preview acceptance. **Checkpoint `m2-c-graph-workspace-merged` is recorded in [HANDOFF.md](HANDOFF.md). Closing M2-C does not authorize M2-D. No implementation slice is active; M2-D through M2-F and M3+ remain unauthorized.**

**Historical implementation note (2026-08-13, superseded by the merged M2-C record above):** on branch `feat/m2-c-graph-workspace` at the merged authorization checkpoint `41ad015`, the candidate added bounded upstream/downstream traversal controls (depth 1–5 and confidence floor 0–1), complete-pin URL coordination, stale-view retention while bounds update, a pure identifier-sorted domain-to-view projection, fixed-option deterministic ELK layout, a lazy-loaded React Flow viewport, and an equivalent keyboard-operable structured view. Every primary and competing Relationship claim remains a separate candidate edge; absent endpoints remain explicit boundary references rather than phantom nodes; truncation is visibly reported; graph/list selection uses one canonical `selected` URL value and deliberate focus transfer. Responsive CSS keeps the structured alternative usable at 390px and disables graph transitions under `prefers-reduced-motion`. The exact pinned dependencies are `@xyflow/react@12.11.3` and `elkjs@0.12.0`; `apps/web/tsconfig.json` uses web-package-only `skipLibCheck` because those pinned libraries' own declarations do not satisfy the repository's `exactOptionalPropertyTypes` setting, while all Atlast source remains fully strict-checked.

**Independent review remediation (2026-08-13):** review found one blocking mode-preservation defect and three significant test/accessibility gaps, all corrected within the authorized M2-C web/test boundary. Traversal bounds, view switches, and selection now preserve the caller's explicit URL mode instead of silently manufacturing a complete pin from an unpinned latest read; a regression test begins from latest mode and proves all three pin fields remain absent, and built-preview acceptance proves the same in desktop and mobile Chromium. The structured view's accessible entity name now includes visible ambiguity, conflicted graph candidates carry an explicit textual `conflicted` edge label in addition to the dashed non-color treatment, and direct `GraphViewport`/`TraversalWorkspace` component suites cover layout failure, selection/focus, conflict/ambiguity semantics, boundary-edge exclusion, all controls, retained-update messaging, and truncation. The previously permissive acceptance URL expression was replaced with exact entity-path and latest-mode assertions. Follow-up manual QA passed graph/structured equivalence, direction changes, selection persistence, URL state, browser Back/Forward, and responsive overflow checks. The tracked lazy graph payload remains a residual performance risk for M2-F, not a hidden M2-C failure.

**Final verification and measurements:** the complete, unmodified `./scripts/verify.sh` passed all seven stages: whitespace, formatting, repository-wide lint, recursive typecheck, shared **384/384**, graph-model **372/372**, API **65/65**, web **133/133 in 19 files** (up from M2-B's 120/120 in 14), production builds, and built-preview Playwright **8/8** across desktop Chromium 1280x720 and mobile Chromium 390x844. M2-C coverage includes projection conflict/boundary tests, repeat-layout determinism, direct graph/workspace component branches, structured focus/selection and ambiguity semantics, latest-mode and complete-pin URL controls, the genuine API-backed graph/structured journey, and zero horizontal overflow. The primary `checkout` downstream/depth-1 journey used the unchanged seven-scenario/20-Evidence synthetic catalog; its traversal returned **2 subjects**, projected to **2 Entity nodes and 1 Relationship candidate edge**, and reported `truncated: false`. Coordinated entity-detail-to-traversal response latency was **110 ms desktop / 105 ms mobile** in the final instrumented run. Sampling resident memory every 20 ms during separate full built-server/browser runs recorded peak API RSS of **91,408 KiB desktop / 91,600 KiB mobile** and aggregate Chromium-process RSS of **408,480 KiB desktop / 331,984 KiB mobile**; Chromium's coarse in-page heap signal reported 10,000,000 bytes in both viewports, so RSS is the retained forcing-point measure and is not mislabeled as JavaScript heap. Snapshot-anchor candidate count remains inapplicable until the still-unauthorized M2-E route exists.

**Bundle measurement, M2-B merged baseline → merged M2-C:** the eagerly loaded JS changed from `392.22 kB` (gzip `119.75 kB`) to `401.81 kB` (gzip `122.32 kB`), **+9.59 kB raw / +2.57 kB gzip**; eager CSS changed from `9.70 kB` (gzip `2.52 kB`) to `13.28 kB` (gzip `3.25 kB`), **+3.58 kB raw / +0.73 kB gzip**. React Flow and ELK are deliberately code-split and fetched only for graph mode as `1,615.03 kB` JS (gzip `501.46 kB`) plus `15.41 kB` CSS (gzip `2.56 kB`); inventory, search, entity summary, and structured mode do not pay that graph chunk on initial load. This sizeable bounded graph chunk is recorded for the mandatory M2-F storage/performance review rather than hidden or treated as a release failure.

- [x] **M2-D — Entity/Relationship trust inspector, Evidence dereferencing, and complete trust-state presentation** — explicitly authorized by Joseph Carfagno on 2026-08-13, independently reviewed and remediated, human-QA approved, verified locally, and squash-merged through PR #45 at `a41d799` on 2026-08-13 with GitHub Actions `verify` passing in 3m6s. Delivered the exact query-API-only M2-D boundary without changing contracts, API behavior, graph-model behavior, fixtures, dependencies, lockfiles, accepted ADRs, verification tooling, or browser acceptance. **Checkpoint `m2-d-trust-inspector-merged` is recorded in [HANDOFF.md](HANDOFF.md). Closing M2-D does not authorize M2-E. No implementation slice is active; M2-E through M2-F and M3+ remain unauthorized.**

  **Merged implementation (2026-08-13; independently reviewed, remediated, and human-QA approved):** the in-scope web candidate adds Entity and Relationship trust entry points, exact Relationship rehydration through identifier search, complete assertion/competing-claim semantics, independently dereferenced and deduplicated Evidence states, honest partial failures and traversal truncation, complete inspectable/copyable snapshot identity, and keyboard focus/return behavior. Review remediation adds direct absent/API-error/internal-error Relationship rehydration coverage, genuine Evidence API-error/retry coverage, explicit latest-mode preservation, and the required confidence label, snapshot-tied freshness explanation, and half-open open-validity wording. Direct and route-level coverage raises the web suite from **133 tests in 19 files to 146 tests in 22 files**. The complete, unmodified `./scripts/verify.sh` passes all seven stages: shared **384/384**, graph-model **372/372**, API **65/65**, web **146/146**, production builds, and browser acceptance **8/8**. The production build changes eager JS from `401.81 kB` (gzip `122.32 kB`) to `412.60 kB` (gzip `124.67 kB`), **+10.79 kB raw / +2.35 kB gzip**, and eager CSS from `13.28 kB` (gzip `3.25 kB`) to `14.83 kB` (gzip `3.56 kB`), **+1.55 kB raw / +0.31 kB gzip**. The lazy graph payload is unchanged at `1,615.03 kB` JS (gzip `501.46 kB`) plus `15.41 kB` CSS (gzip `2.56 kB`). Human browser QA passed Entity and exact-Relationship entry, heading focus, focus return, complete rendered trust semantics, snapshot-identity copy feedback, Evidence loading, and a 390×844 responsive check with no horizontal overflow. Each sampled fixture subject carried **1 unique Evidence identifier**, rendered **1 Evidence card**, and issued no duplicate citation request; direct local Evidence reads measured **1.57 ms / 1.61 ms**. After those reads, local QA-server RSS was **40,112 KiB API / 72,192 KiB Vite**; this bounded server snapshot is not mislabeled as browser heap. The exact-Relationship rehydration fallback remains bounded to the first 100 identifier-search results, sufficient for the current 20-Evidence synthetic catalog but requiring pagination review if catalog scale grows. Implementation, approval, PR/CI, and merge are complete; this checkpoint closeout does not authorize M2-E.

- [x] **M2-E — bounded snapshot-anchor API extension and history playback** — explicitly authorized by Joseph Carfagno on 2026-08-13 after the M2-D closeout merged through PR #46 at `452f60f`; implemented within [the exact M2-E boundary](docs/m2-plan.md#exact-m2-e-authorization-boundary), independently reviewed with no blockers, approved through human desktop/mobile browser QA, verified locally, and squash-merged through PR #49 at `62eb684` on 2026-08-15 with GitHub Actions `verify` passing in 3m16s. **Checkpoint `m2-e-history-playback-merged` was recorded at closeout; PR #50 subsequently released M2-F, which is now complete.**

  **Merged implementation record (2026-08-15):** the bounded M2-E implementation merged through PR #49 at `62eb684` within the exact boundary above. Additive shared schemas validate the capped anchor envelope and complete identities; `GET /api/v1/snapshot-anchors` reads the watermark once, pages retained Evidence at the fixed maximum page size, resolves at most 101 newest distinct observation candidates through the existing snapshot-summary path, and exposes no Evidence or fixture metadata. The query-API-only browser panel loads history on demand without disabling latest exploration on failure, writes complete pins, shows the selected checksum/subject count, preserves copied/reloaded coordinates, exposes invalid requested pins without fallback, and starts a genuinely new latest generation on return. A query-key guard prevents prior-coordinate data from rendering during snapshot changes. Built-preview acceptance discovers coordinates only through the route, reloads a copied historical pin, traces a historical Relationship assertion to directly dereferenced Evidence, and proves an invalid pin remains visible until an explicit return to latest. **Verification:** shared **387/387**, graph-model **372/372**, API **69/69**, web **153/153**, and browser acceptance **12/12** pass through the complete seven-stage `./scripts/verify.sh`; lint, formatting, typecheck, builds, and `git diff --check` are clean. **Measurements:** the unchanged seven-scenario catalog retains **20 Evidence records** with **17 distinct observation anchors**; the route returns all **17**, `truncated: false`, at horizon **20**; direct built-API calls measured **80.44 ms cold** and **1.57–1.87 ms warm** (five-call sample), while the instrumented built-preview desktop journey measured **83 ms** end-to-end and Chromium reported its coarse **10,000,000-byte** in-page heap signal; the sampled built API RSS was **100,496 KiB**. The primary historical acceptance selection returned **1 Relationship** with direct Evidence traceability. Eager production output is **416.01 kB JS / 125.41 kB gzip** and **15.89 kB CSS / 3.74 kB gzip**; the tracked lazy graph payload is unchanged at **1,615.03 kB JS / 501.46 kB gzip** plus **15.41 kB CSS / 2.56 kB gzip**. No dependency, manifest, lockfile, fixture, graph-model/repository/reconciliation/storage, accepted-ADR, script, or CI change was made. A final reviewer-style contract, wiring, URL-state, and scope audit found no blockers, and Joseph Carfagno completed desktop/mobile browser QA of anchor discovery, complete-pin selection, reload persistence, historical graph/detail behavior, invalid-pin honesty, and explicit return to latest; all checks passed. Joseph then explicitly approved this reviewed M2-E output on 2026-08-13. Implementation, approval, PR/CI, merge, and its PR #50 closeout are complete; that closeout released M2-F, which subsequently completed through PR #51.

- [x] **M2-F — browser acceptance, accessibility/responsive hardening, ADR-0018 storage re-evaluation, audit, and M2 closeout** — explicitly pre-authorized by Joseph Carfagno on 2026-08-13; independently reviewed and remediated, fully verified, approved through human keyboard/reduced-motion/failure-recovery/VoiceOver QA, and merged through PR #51 at `5aeb11d` on 2026-08-16 with GitHub Actions `verify` passing in 3m9s. Post-merge revalidation directly against `5aeb11d` passed the complete verifier and the boundary/source-hygiene checks. **Checkpoint `m2-complete` formally closes M2. M3 planning is now active; M3 product implementation and M4+ remain unauthorized.**

  **Merged implementation record (2026-08-16):** within the exact M2-F boundary above, delivered:

  1. **Browser-acceptance expansion.** Two new specs close the primary-journey/trust-inspection/honest-failure/keyboard/reduced-motion gaps the M2-A through M2-E acceptance suite left open: `tests/acceptance/specs/trust-inspection.spec.ts` (entity trust inspection with real dereferenced Evidence and focus return on close; exact-Relationship search rehydration opening the same inspector with competing-claim/rule-trace semantics — the trust inspector had **no** acceptance coverage through M2-D/M2-E) and `tests/acceptance/specs/accessibility-hardening.spec.ts` (keyboard-only graph-node and graph-edge selection with visible focus indicators; a full search-to-trust-inspection journey driven by keyboard alone, with no `.click()`; `prefers-reduced-motion: reduce` disabling the graph viewport's transitions/animations; and an intercepted, deliberately malformed API failure rendering the honest redacted-failure state — never a stale or empty view — then recovering on retry). [docs/m2-plan.md § 11](docs/m2-plan.md#11-verification-strategy) named "Honest failure: API unavailable or deterministic intercepted error renders a visible, non-stale failure state" as a required M2 browser-acceptance obligation; no prior M2 slice's acceptance suite exercised it until this one.
  2. **A genuine keyboard-operability defect, found and fixed.** `@xyflow/react` gives every graph node and edge `tabIndex=0` and its own internal Enter/Space `keydown` handler (confirmed by reading the installed `@xyflow/react@12.11.3` source directly), but that internal handler only updates the library's own selection store — it never calls the `onNodeClick`/`onEdgeClick` callback the M2-C `GraphViewport` relies on to open the trust inspector. A keyboard user who tabbed onto a node or edge and pressed Enter/Space saw nothing happen. The existing M2-C unit-test mock (`GraphViewport.test.tsx`) could not have caught this because it rendered both shapes as native `<button>` elements, while the real library renders HTML `<div>` nodes and SVG `<g>` edges. Fixed with a delegated `keydown` listener on `GraphViewport`'s wrapper that accepts either DOM shape, finds the nearest `data-id`-bearing ancestor, and calls the identical `onSelect` callback the mouse handlers call. Independent review caught that the first implementation accepted only `HTMLElement`, still excluding real SVG edges; remediation changed the guard to `Element`, made the unit mock reproduce the SVG edge shape, and added real-browser edge activation coverage.
  3. **A paired, genuine focus-visibility defect, found and fixed.** `@xyflow/react/dist/style.css` (imported by `GraphViewport.tsx`) suppresses the focus treatment on selectable nodes and edges, so even after fix 2 above, a keyboard user tabbing through the graph lacked a reliable visible indicator. `apps/web/src/styles.css` gained two narrowly scoped treatments: a `3px solid var(--rust)` outline for focused HTML node wrappers and a `3px var(--rust)` stroke for the focused SVG edge path. Both fixes are proven together, against the real (non-mocked) library, by `accessibility-hardening.spec.ts`'s first test, which checks the node outline and the edge-path stroke after focus, then proves Enter and Space respectively open the inspector in both desktop and mobile Chromium.
  4. **No further accessibility/responsive/reduced-motion defect was found** on direct review of `TrustInspector.tsx`, `StructuredTopologyView.tsx`, `SnapshotHistory.tsx`, `TopologyShell.tsx`, `TopologyPage.tsx`, and `EntityDetailPage.tsx`: focus management on open/close, `role="dialog"`/`role="alert"`/`role="status"` usage, the existing `prefers-reduced-motion: reduce` rule disabling `.react-flow__viewport`/`.react-flow__node`/`.react-flow__edge` transitions (M2-C), and the existing narrow-viewport media queries were all already correct and are unchanged by this candidate.
  5. **Final measurements** (docs/m2-plan.md § 3/§ 11 obligation), all taken against a clean local build with all three packages' `dist/` rebuilt from this candidate: fixture cardinality and route behavior are **unchanged** from M2-E — **20** retained Evidence records, **17** distinct snapshot anchors, `truncated: false` at horizon **20**, and **11** visible entities; the `checkout` downstream depth-1 traversal still returns **2 subjects** (**2** items) with `truncated: false`. Direct built-API latency: `GET /health` **1.2–5.7 ms**; `GET /api/v1/entities?limit=100` **~10–30 ms**; browser-measured end-to-end journeys (Playwright annotations) **105–118 ms**, consistent with M2-E's own **83–110 ms** figures within normal run-to-run variance. Sampled built API RSS is **101,072 KiB** (M2-E: 100,496 KiB — unchanged within noise). Chromium's coarse in-page heap signal remains the fixed **10,000,000-byte** value M2-C/M2-E already reported (re-confirmed by rerunning `topology-graph.spec.ts`/`snapshot-history.spec.ts` and reading their existing measurement annotations), not a number this candidate's own unchanged rendering path could move. **Bundle after independent-review remediation**, M2-E merged baseline → this candidate: eager JS unchanged at **416.01 kB / 125.41 kB gzip**; eager CSS **15.89 kB / 3.74 kB gzip → 16.11 kB / 3.77 kB gzip** (+0.22 kB raw / +0.03 kB gzip); the tracked lazy graph chunk **1,615.03 kB / 501.46 kB gzip → 1,615.25 kB / 501.57 kB gzip** (+0.22 kB raw / +0.11 kB gzip). **No dependency, manifest, lockfile, fixture, graph-model/repository/reconciliation/storage, accepted-ADR, script, or CI change was made** — confirmed by `git diff --stat` against `a7e0f21` touching exactly four files plus two new files, none of them a manifest or lockfile.
  6. **The independent ADR-0018 retain-or-migrate storage review**, required before M2-F can close (docs/m2-plan.md § 3): evaluated against [ADR-0018](docs/adr/0018-m1-storage-strategy.md)'s own named change conditions, using the complete M2-A through M2-F measurement record (this entry, plus the M2-A/§, M2-B, M2-C, M2-D, and M2-E entries above). **(a) Scheduled M2 re-evaluation:** this is that review. **(b) Fixture suites or M2 interactive latency measurably exceeding in-memory comfort:** not triggered — the fixture catalog is unchanged at 20 Evidence records/11 entities across all five M2 implementation slices; API RSS has stayed in a narrow 40–101 MiB band with no growth trend attributable to storage pressure (each measured jump tracks a new _browser_ feature's own request pattern, not storage cost); every measured route latency across M2-A–F remains sub-30 ms server-side and comfortably interactive end-to-end. **(c) A committed requirement for durable state beyond regenerable fixtures:** not triggered — no M2 slice introduced any state that is not rebuildable from `fixtures/demo-company/` (the M2-E `snapshot-anchors` route is a pure read composition over the existing public repository interfaces; it adds no storage). **(d) Measured traversal/temporal query patterns demonstrably better served by a relational or graph engine:** not triggered — the largest traversal any M2 journey has exercised remains the 2-subject `checkout` depth-1 neighborhood; nothing at M2 scale has exposed a traversal, temporal, or cardinality pattern a hash-map-backed in-memory store serves worse than SQLite or a graph database would. **Conclusion: Outcome 1 — retain the accepted in-memory implementation for the measured M2 workload.** No storage ADR or migration slice is proposed; [ADR-0018](docs/adr/0018-m1-storage-strategy.md) itself is unedited and remains Accepted, exactly as its own amendment convention requires (decisions are amended by a new ADR, never edited in place) — and no new ADR is warranted here because no condition triggers a change.
  7. **The final synthetic-boundary and no-side-door audit** — [docs/audits/m0-synthetic-boundary-audit.md § 16](docs/audits/m0-synthetic-boundary-audit.md) — covers the complete M2-A through M2-F delta for the first time in one dedicated section (each M2 slice's own scope compliance had previously been recorded narratively in its TASKS.md/HANDOFF.md entries, but not audited together). **No findings requiring remediation**: the query API remains exactly eight `GET` routes plus `/health` with zero mutation endpoints; every `apps/web/src/**` graph-fact read passes through the validated query client and the still-active M2-A ESLint restricted-import boundary (no side door); the three M2 dependency additions (`react-router`, `@xyflow/react`, `elkjs`) carry no external-integration capability and this candidate adds none; no fixture, credential, or employer/customer material exists in the delta; and a byte-safe sweep of the complete tracked tree plus this candidate's five new/changed files finds zero literal NUL bytes. **This is a pre-merge candidate audit, exactly like § 14 was for M1 Slice S8** — § 16.13 records that a brief post-merge revalidation against the real M2-F merge commit remains required before M2 can close on this dimension, per this document's own established § 13/§ 15 revalidation convention.
  8. **This documentation update itself**, plus the factual checklist correction immediately below.

  **Verification and closure:** after independent-review remediation, the complete, unmodified `./scripts/verify.sh` passed all seven stages: shared **387/387** in 15 files, graph-model **372/372** in 19 files, API **69/69** in 7 files, web **154/154** in 23 files, production builds, and browser acceptance **24/24** across both projects. `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, and `git diff --check` were clean. The exact seven-file M2-F scope contained no domain/repository/reconciliation/snapshot/query-API behavior, fixture, accepted-ADR, dependency/manifest/lockfile, script, or CI change. Joseph Carfagno explicitly approved the corrected M2 checklist, ADR-0018 Outcome 1 retain-in-memory conclusion, audit § 16, complete human QA, and publication on 2026-08-16. PR #51 then merged at `5aeb11d` with GitHub Actions passing in 3m9s. Post-merge revalidation repeated the exact-scope review, a byte-safe zero-NUL sweep across all **238 tracked files**, and the full verifier directly on the real merge commit; all passed. [Audit § 17](docs/audits/m0-synthetic-boundary-audit.md) records that closed fact. **M2 is formally complete at checkpoint `m2-complete`; M3 planning may begin, but M3 product implementation remains gated.**

- [x] Graph exploration UI: navigation, search, entity detail
- [x] Provenance/confidence/freshness view for every displayed fact
- [x] Snapshot/history playback
- [x] Browser acceptance checks for primary journeys

  _Correction (2026-08-15, closed 2026-08-16): the first three boxes had remained unchecked after their capabilities merged in M2-B and M2-D; M2-F corrected that stale documentation and completed the acceptance coverage named by the fourth box. Human QA, PR #51, and post-merge revalidation subsequently closed the candidate qualification. The M2 milestone exit criteria are now separately checked in [docs/milestones.md](docs/milestones.md#m2--interactive-topology-interface-complete--2026-08-16)._

**Checkpoint `m2-complete` — M2 milestone closeout (2026-08-16):** all six M2 implementation slices and both milestone exit criteria are complete; PR #51 merged at `5aeb11d`; GitHub Actions and the complete local post-merge verifier passed; audit § 17 confirmed the synthetic boundary, query-API-only browser boundary, exact merged scope, and zero literal NUL bytes across 238 tracked files; ADR-0018 Outcome 1 retains in-memory storage for the measured M2 workload. No M2 implementation slice is active.

## M3 — Operational Health Overlays (M3-E authorized pending activation)

Joseph Carfagno explicitly pre-authorized **M3 planning and pre-release architecture/ADR review only** on 2026-08-13. M2 is formally complete, and the independently reviewed, corrected, and explicitly human-approved M3 baseline merged through PR #53 at `b85be38` on 2026-08-16. M3-A's implementation merged through PR #55 at `e9afcd5`, and its closeout merged through PR #56 at `a767c93`. M3-B's implementation merged through PR #58 at `98beb46`, and its closeout merged through PR #59 at `b932539`. M3-C's implementation merged through PR #61 at `e177fc0`, and its closeout merged through PR #62 at `5f2d038`. M3-D's independently reviewed and human-QA-approved implementation merged through PR #64 at `a2c2d92` on 2026-08-17. Joseph then explicitly authorized **M3-E only** on 2026-08-17, subject to the combined documentation merge and clean synchronization gate. **M3-F and M4+ remain unauthorized.**

**M3-P baseline (2026-08-16):** Joseph Carfagno explicitly accepted ADRs [0029](docs/adr/0029-m3-overlay-model-and-temporal-semantics.md)-[0031](docs/adr/0031-m3-overlay-presentation-and-accessibility.md) and approved [docs/m3-plan.md](docs/m3-plan.md) as the M3 implementation baseline. It defines separate immutable overlay frames; deterministic direct/effective state and latent-risk semantics; explicit topology/frame coordinates; a bounded health-in-context API join; unknown-target gaps; query-only graph/structured UI presentation; six implementation slices; and verification/release gates. The accepted baseline merged through PR #53 at `b85be38`; its acceptance authorized no implementation by itself.

**Independent architecture review and approval (2026-08-16):** review against the merged repository and accepted M1/M2 contracts found and corrected implementation-blocking ambiguity in eight connected areas: traversal-origin projection, revision-level Relationship edge qualification and auditable path steps, complete frame/entry identity and total ordering, redundant in-scope existence reads, the exact strict response envelope and projection/gap unions, browser latest-mode coordination, non-destructive state-emphasis semantics, and canonical URL wire/dependency/history-transition behavior. The corrected baseline preserves the M2 traversal as the browser's base topology source, requires pinned dependent health reads and response-coordinate/subject matching, and leaves topology visible on every overlay failure. No production code, fixture, schema, dependency, or Accepted ADR changed. The unchanged `./scripts/verify.sh` passes all seven stages: shared **387/387**, graph-model **372/372**, API **69/69**, web **154/154**, production builds, and browser acceptance **24/24**.

**M3-A authorization (2026-08-16):** Joseph Carfagno explicitly authorized M3-A after PR #53 merged and local `main` synchronized cleanly. The exact boundary is additive shared overlay/frame/projection/gap/HTTP schemas and tests; the asynchronous read-only overlay-store interface; the separate `fixtures/demo-company/overlays/` synthetic catalog and validation tests; directly corresponding workspace/build plumbing; and factual measurements in this file. No API route, `packages/overlay-model` provider or projection engine, browser behavior, external dependency, graph subject/assertion/Evidence/repository schema change, or topology mutation is authorized. The authorization merged through PR #54 at `e5da808`; local `main` then synchronized cleanly, activating M3-A. M3-B through M3-F and M4+ remain unauthorized.

**M3-A implementation result (2026-08-16):** PR #55 squash-merged at `e9afcd5` with GitHub Actions `verify` passing in 3m27s. It added the strict `atlast-overlay-v1` frame, entry, projection, gap, health-context HTTP, and closed overlay-error schemas; the asynchronous read-only `OperationalOverlayStore` port; and a separate two-frame, seven-entry demo-company overlay catalog. The catalog spans two effective times, covers all five direct conditions, includes the `checkout` healthy to `fulfillment` down propagation seed, and carries exactly one unknown target for later gap behavior. The complete seven-stage local verifier passed: shared **419/419**, graph-model **372/372**, API **69/69**, web **154/154**, production builds, and browser acceptance **24/24**. No API route, projection engine, browser behavior, dependency, lockfile, existing topology schema, or M1 Evidence fixture changed.

**Checkpoint `m3-a-overlay-contracts-fixtures-merged` — M3-A closeout (2026-08-16):** the bounded implementation is merged through PR #55 at `e9afcd5`, GitHub Actions and the complete local verifier pass, and the shared contract/store boundary plus separate overlay fixture catalog are complete. No implementation slice is active. Closing M3-A does not authorize M3-B; M3-B through M3-F and M4+ remain gated and unauthorized.

**M3-B authorization (2026-08-16):** after the M3-A closeout merged through PR #56 at `a767c93` and local `main` synchronized cleanly, Joseph Carfagno explicitly authorized M3-B only. The exact boundary is a new internal `packages/overlay-model` workspace package containing the in-memory implementation of the merged `OperationalOverlayStore` interface and a pure deterministic health projector; exhaustive tests proving immutability, the 100-entry bound, total frame/entry ordering and selection, origin projection, revision-qualified scope-relative latent-risk paths, cycles, missing direct state, and gaps; directly required workspace/build plumbing; and factual measurements in this file. No API route, browser behavior, external dependency, graph subject/assertion/Evidence/repository schema change, topology mutation, or M3-C+ behavior is authorized. The authorization merged through PR #57 at `8213d7d`; local `main` then synchronized cleanly, activating M3-B.

**M3-B implementation candidate (2026-08-16):** the new internal `@atlast/overlay-model` package implements immutable validated in-memory frame storage, deterministic exact and latest-at-or-before selection, and a pure scope-relative health projector. The projector preserves traversal-origin inclusion, confidence-qualified directed Relationship revisions, auditable shortest latent-risk paths with deterministic tie ordering, cycle safety, unreported missing direct state, unknown-target gaps without topology expansion, and truncation propagation. Focused overlay-model tests pass **22/22**. The unchanged complete seven-stage repository verifier passes: shared **419/419**, overlay-model **22/22**, graph-model **372/372**, API **69/69**, web **154/154**, production builds, and browser acceptance **24/24**. No API route, browser behavior, fixture, external dependency, existing graph schema/storage behavior, or topology mutation changed.

**Checkpoint `m3-b-overlay-model-merged` — M3-B closeout (2026-08-16):** the independently reviewed implementation squash-merged through PR #58 at `98beb46`; GitHub Actions `verify` passed in 3m32s; and local `main` synchronized cleanly. The immutable in-memory overlay store, deterministic scope-relative projector, unknown-target gap behavior, and all 22 overlay-model tests are complete. No implementation slice is active. Closing M3-B does not authorize M3-C; M3-C through M3-F and M4+ remain gated and unauthorized.

**M3-C authorization (2026-08-16):** after the M3-B closeout merged through PR #59 at `b932539` and local `main` synchronized cleanly, Joseph Carfagno explicitly authorized M3-C only. The exact boundary is the accepted health-in-context API slice: the exact route, query coercion, response validation, dependency injection, closed errors, and directly corresponding integration tests; proof of one traversal resolution, one frame resolution, no redundant in-scope target reads, at most 100 exact out-of-scope target-existence reads at the same identity, deterministic ordering, historical compatibility, and no topology mutation; directly required build plumbing; and factual measurements in this file. No browser behavior, fixture, external dependency, graph schema/storage change, topology mutation, or M3-D+ behavior is authorized. Activation requires this authorization record to merge and local `main` to synchronize cleanly.

**M3-C implementation candidate (2026-08-16):** the exact `GET /api/v1/entities/{entityId}/health-context` route now composes one bounded topology traversal with one exact or latest-at-or-before immutable overlay frame, validates the strict shared response envelope, preserves the resolved topology identity, and maps unknown, ineligible, future, malformed, and unexpected overlay failures through closed errors. Nineteen direct route tests prove deterministic historical replay, traversal truncation, one traversal call, one frame-resolution call, no in-scope target re-reads, exact pinned reads for the two out-of-scope demo targets, the 100-read schema maximum, unknown-target gaps without phantom topology, and unchanged snapshot checksum and subject count. A compiled fixture-backed request returned a stable **3,131-byte** response; after two warm-up requests, ten local requests measured **5.75 ms median** and **6.45 ms average** latency. The unchanged complete seven-stage repository verifier passes: shared **419/419**, overlay-model **22/22**, graph-model **372/372**, API **88/88**, web **154/154**, production builds, and browser acceptance **24/24**. No browser behavior, fixture, external dependency, graph schema/storage behavior, or topology mutation changed; M3-D through M3-F and M4+ remain unauthorized.

**Checkpoint `m3-c-health-context-api-merged` — M3-C closeout (2026-08-16):** the independently reviewed implementation squash-merged through PR #61 at `e177fc0`; GitHub Actions `verify` passed in 3m5s; and local `main` synchronized cleanly. The strict health-in-context API, closed overlay errors, deterministic historical composition, bounded same-identity target checks, unknown-target gaps, and all 88 API tests are complete. No implementation slice is active. Closing M3-C does not authorize M3-D; M3-D through M3-F and M4+ remain gated and unauthorized.

**M3-D authorization (2026-08-16):** after the M3-C closeout merged through PR #62 at `5f2d038` and local `main` synchronized cleanly, Joseph Carfagno explicitly authorized M3-D only. The exact boundary is the accepted topology-overlay UI slice: validated client support; canonical URL state; graph and structured overlay rendering; state-emphasis filters; explanations and explicit gaps; preservation of the M2 coordinator, trust inspector, history playback, no-side-door lint boundary, complete topology/frame identity, non-color semantics, and fail-honest topology continuity; directly corresponding `apps/web` tests; and factual measurements in this file. No new data source, API behavior, fixture, schema, dependency, overlay-model behavior, topology mutation, browser-acceptance expansion, or M3-E+ behavior is authorized. Activation requires this authorization record to merge and local `main` to synchronize cleanly.

**M3-D implementation result (2026-08-17, independently reviewed, explicitly human-QA-approved, and merged):** within the authorized boundary above, `apps/web/src/**` gained validated client support for the M3-C health-in-context route (`fetchHealthContext` in `api/client.ts`); canonical `health`, `healthStates`, and `overlayFrame` URL state in `url/query-state.ts` with the exact ADR-0031 § 2 field order (`q, direction, depth, minConfidence, view, selected, health, healthStates, asOf, horizon, derivationVersion, overlayFrame`) and dependency-drop canonicalization (`health` invalid drops both dependents; repeated/invalid `healthStates` drops the whole scalar list; `healthStates` requires `health=on`; `overlayFrame` requires `health=on` plus a complete pin); a pure presentation/projection module (`topology/health-overlay-projection.ts`) mapping the already-server-computed `HealthContextResult` into non-color state labels/glyphs/patterns, emphasis-vs-neutral treatment, direct-vs-derived explanation text with the canonical latent-risk path, and the client-side identity/ordered-subject publish gate; a dedicated data hook (`topology/use-health-context.ts`) that issues the dependent pinned health-context read only once the base traversal for the exact same bounds has resolved, and reports `off`/`loading`/`api-error`/`internal-error`/`identity-mismatch`/`ready` distinctly; three new presentational components (`HealthOverlayControls`, `HealthOverlayStatus`, `HealthOverlayGaps`); `GraphViewport.tsx` and `StructuredTopologyView.tsx` extended with an optional `healthOverlay` prop rendering the same view model (graph/structured equivalence); `TraversalWorkspace.tsx` threading that prop through; `EntityDetailPage.tsx` wiring the toggle, emphasis filters, status/gap panels, explicit copied-frame recovery, the historical-frame auto-pin-back effect (ADR-0031 § 2: clears then re-selects then writes back the exact resolved `overlayFrame` on a history-anchor change), and preservation of the M2 coordinator/trust inspector/history playback; and `SnapshotHistory.tsx` clearing `overlayFrame` on anchor selection and on return-to-latest. Independent review corrected repeated-key handling, added an actionable recovery that clears only an invalid exact frame before requesting a compatible historical frame, and made each gap keyboard-focusable with its source frame, direct condition, target, entry, and reason. No `packages/shared`, `packages/overlay-model`, `packages/graph-model`, `apps/api`, `fixtures/`, dependency, lockfile, or `tests/acceptance` file changed. The implementation squash-merged through PR #64 at `a2c2d92` on 2026-08-17.

**Verification (2026-08-16):** 73 new/changed focused `apps/web` tests were added across the client, URL-state, pure-projection, hook, three new components, and the four modified components/pages, bringing the `apps/web` suite to **227 passing in 28 files** (up from 154/24 at the M3-C checkpoint) — no other workspace's test count changed (shared 419/419, overlay-model 22/22, graph-model 372/372, api 88/88). After independent remediation, the complete, unmodified `./scripts/verify.sh` passed all seven stages: whitespace, `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, the six-workspace test run above, production builds, and browser acceptance **24/24** (unchanged; no acceptance spec was added or edited). Production build sizes: eager JS `431.84 kB` / **129.14 kB gzip** (M2-F/M3-C baseline: 416.01 kB / 125.41 kB gzip — M3-D added ≈ 15.8 kB / 3.7 kB gzip to the eager bundle for the overlay controls, status, gaps, and projection code); eager CSS `18.06 kB` / **4.08 kB gzip** (baseline 16.11 kB / 3.77 kB gzip); the lazily-loaded graph chunk is materially unchanged at `1,615.56 kB` / **501.71 kB gzip** (baseline 1,615.25 kB / 501.57 kB gzip — the overlay node/edge rendering added to `GraphViewport.tsx` is small relative to `@xyflow/react`/ELK). The implementation pass also exercised the real built API and real `demo-company` M3-A overlay fixtures (`node apps/api/src/server.ts` plus `vite preview`, screenshotted with Playwright's `chromium` launcher, not a repository script), confirming in a real browser: the `checkout` entity showing `△ Latent downstream risk` with its trigger (`atlast:entity:fulfillment`, `Down`) and canonical path in both the graph and structured views; the real unknown overlay target `atlast:entity:retired-billing` appearing only in the dedicated gaps panel, never as a graph node; and distinct non-color border patterns (double border for `Down`, ridge for `Latent downstream risk`) alongside the text/glyph labels on graph nodes.

**Human browser QA approval (2026-08-17):** Joseph Carfagno explicitly approved M3-D human browser QA after verifying graph and structured-view equivalence; non-color health labels and explanations; keyboard-visible gaps with source frame, direct condition, target, entry, and reason; emphasis without topology filtering; complete-pin history-anchor coordination; topology continuity during an invalid exact-frame failure; and explicit recovery to the compatible historical overlay frame while preserving `health=on` and the topology pin.

**Known limitation, recorded honestly:** the M3-D candidate does not retain a stale health-overlay result while a bounds change is in flight the way M2's traversal retention does — the health panel shows its own "Loading the synthetic operational overlay…" status during a bounds change even though the previously loaded topology view remains visible via the existing traversal retention. This is a deliberate, narrower correctness choice (health may only publish for the exact bounds it was requested with, per ADR-0031 § 1's identity/subject match gate) rather than an oversight, but it means bounds changes are visually less smooth for the overlay than for base topology; it does not violate any binding M3-D invariant and is flagged here for reviewer attention rather than silently accepted as ideal.

**Checkpoint `m3-d-topology-overlay-ui-merged` — M3-D closeout (2026-08-17):** the independently reviewed and human-QA-approved implementation squash-merged through PR #64 at `a2c2d92`; GitHub Actions `verify` passed in 3m52s; and local `main` synchronized cleanly. The validated health-context client, canonical overlay URL state, equivalent graph/structured non-color presentation, explicit keyboard-accessible gaps, historical frame coordination, and fail-honest recovery are complete. The complete verifier passes shared **419/419**, overlay-model **22/22**, graph-model **372/372**, API **88/88**, web **227/227**, and browser acceptance **24/24**, plus formatting, lint, typecheck, builds, and whitespace checks.

**M3-E authorization (2026-08-17):** after M3-D merged and local `main` synchronized cleanly, Joseph Carfagno explicitly authorized M3-E only. The exact boundary is the accepted accessibility, history, and failure-hardening slice: built-preview desktop/mobile journeys for all six states, gaps, historical frames, retry, keyboard operation, structured equivalence, reduced motion, zoom/reflow, and non-color semantics; representative VoiceOver QA; and factual measurements. No new product behavior, data source, API behavior, fixture, schema, dependency, overlay-model behavior, or topology mutation is authorized. Activation requires this combined M3-D closeout/M3-E authorization record to merge and local `main` to synchronize cleanly. M3-F and M4+ remain unauthorized.

- [x] Inventory the completed topology, snapshot, API, browser, accessibility, and synthetic-boundary seams.
- [x] Draft the M3 implementation plan and Proposed ADRs 0029-0031.
- [x] Complete independent architecture review and correction.
- [x] Obtain explicit human approval of the complete baseline - _Joseph Carfagno explicitly accepted ADRs 0029-0031 and approved docs/m3-plan.md on 2026-08-16._
- [x] Separately authorize M3-A after the approval record merges and `main` is synchronized cleanly - _explicitly authorized by Joseph Carfagno on 2026-08-16; authorization merged through PR #54 at `e5da808`, then local `main` synchronized cleanly before implementation._

- [x] Overlay model + synthetic state generator (healthy, degraded, down, disconnected, expiring certificate, latent downstream risk)
- [x] Health-in-context queries and UI overlay toggles
- [x] Unknown-entity overlays surface as gaps, not phantom nodes

## M4 — Change-Impact Simulation (gated — not authorized)

- [ ] Deterministic impact query API with change-type semantics
- [ ] Ranked blast radius with evidence path per claim
- [ ] Impact views in UI
- [ ] Synthetic scenario accuracy harness in CI
- [ ] Gate check: deterministic engine validated **before** any LLM-generated reasoning is considered

## M5 — Read-Only Local Kubernetes Connector (gated — not authorized)

- [ ] Read-only adapter targeting a disposable local cluster (e.g., Kind) only
- [ ] Adapter emits the same normalized evidence format as fixtures
- [ ] Freshness degradation verified when cluster dies/deletes
- [ ] Employer/production cluster connection structurally impossible without spec amendment

---

## Post-M5 (uncommitted)

Predictive AI, multi-cloud integrations, multi-source enterprise reconciliation, and advisory remediation recommendations — see [docs/milestones.md § Post-M5](docs/milestones.md#post-m5-directional-uncommitted). Executing remediation remains a permanent non-goal.

## Parking Lot

Ideas raised but not scheduled. Nothing here is committed; graduation requires a milestone home and spec compliance.

- (empty)
