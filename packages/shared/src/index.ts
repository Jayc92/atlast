/**
 * Package boundary for `@atlast/shared`, created during M0 to establish the
 * workspace structure and type-checking infrastructure only.
 *
 * The shared domain schemas and contracts (Zod single source of truth per
 * ADR-0005) arrive in M1, once that milestone is explicitly authorized.
 */
export {};
